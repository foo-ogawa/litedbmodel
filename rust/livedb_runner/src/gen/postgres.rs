#![allow(dead_code, unused_imports, unused_variables, non_snake_case)]
// GENERATED by behavior-contracts (bc#90, rust-typed-native — the RUNTIME-FREE read de-box; STRUCT-ONLY,
// FULLY-NATIVE). A covered read is a strictly-sequential typed componentRef chain (point reads +
// relation children), emitted ONLY as a STRUCT-returning runner that calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY at each covered node's execution point: native ports struct IN (direct
// construction — every port is a CONCRETE Rust value: String / bool / Vec<&str> projection / i64 limit —
// NO boxed Value, NO by-name accessor); the transport is called with the node's port fields spread and
// returns the node's result WIRE (a value implementing WireValue), and the runner de-boxes it INLINE
// (fully-unrolled probe/match — no decode helper, no output recursion) into the node's outType struct.
// There is NO per-node handler indirection — a leaf is ONE transport, not one method per authored node.
// The covered plane carries NO bc-runtime reference at all — failures use the LOCAL BehaviorError (same
// codes, byte-equal to run_behavior). INLINE sequential (no plan driver) so a relation child reads the
// parent's REAL struct result via direct field access and the child-present decision is made from the real
// parent value — relation / connection converge with run_behavior (fixes #323/#74). The exposed API
// is the 1:1 entry <method>(<positional params>) -> Result<T, BehaviorError> (the InNR_<comp> box removed);
// the consumer supplies the leaf transport symbols (default in-scope names leaf_<comp>, or a leafTransport
// import).
//
// NO COPY TO READ, NO PER-CALL ALLOCATION — the two rules this module is generated under:
//   - Everything fixed at generation time (SQL text, port names, field names) rides as `&'static str`
//     behind `Cow::Borrowed`, so building a payload allocates nothing for it, however often it is called.
//   - De-boxing CONSUMES the transport's wire: every string, row and list is MOVED into the typed value,
//     and a result with a single reader is MOVED out of its cell (`.take()`) rather than copied. Reading
//     one field of a result that has several readers borrows to that field and owns only it — the row is
//     never copied to read part of it.
// The codegen purity gate fails the build on exactly those spellings (a wire classifier that borrows, a
// de-box that clones a list out of the wire, a row cloned out of its cell before a field is taken from the
// copy) and the cost is confirmed by benchmark, not by this comment.
use std::borrow::Cow;
use std::cell::RefCell;

// #165 --shared-types-import: the BC-OWNED wire seam lives in the shared crate/module — import it.
use litedbmodel_runtime::*;

// De-box / op-failure constructors (produce the shared BehaviorError, imported above). Runtime-free.
// op_failed — wrap a leaf failure as the node's Failure under its Error Policy Kind, carrying the
// leaf's structured detail through unchanged (the runner transports the Error Value; it does not
// synthesize or re-interpret it).
#[allow(dead_code)]
fn op_failed(node: &str, policy: &str, e: BehaviorError) -> BehaviorError {
    // The Kind names itself, and 'retry' says it gave up — the attempts are Runtime-owned (scp-error.md:
    // tuning never enters the IR), so what the plan reports is the exhausted retry.
    let exhausted = if policy == "retry" {
        " (exhausted)"
    } else {
        ""
    };
    BehaviorError {
        code: "OP_FAILED".to_string(),
        message: format!(
            "operation '{node}' failed under '{policy}' policy{exhausted}: {}",
            e.message
        ),
        detail: e.detail,
    }
}

// de_type_mismatch / de_missing_field — the de-box mismatch failures the generated decode
// produces (scp-error.md "The Error Value"). Codes are byte-equal to the runtime outType check
// (TYPE_MISMATCH / MISSING_PROP) so the covered read stays equivalent to run_behavior; detail.kind
// distinguishes typeMismatch / missingField.
#[allow(dead_code)]
fn de_type_mismatch(
    model: &str,
    field: &str,
    expected: &str,
    actual_wire: &str,
    raw: Cow<'static, str>,
) -> BehaviorError {
    BehaviorError::with_detail(
        "TYPE_MISMATCH",
        format!("node '{model}': {field}: expected {expected}, got {actual_wire}"),
        ErrorDetail {
            kind: Some(ErrorKind::TypeMismatch),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: Some(actual_wire.to_string()),
            raw_value: Some(raw.into_owned()),
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[allow(dead_code)]
fn de_missing_field(model: &str, field: &str, expected: &str) -> BehaviorError {
    BehaviorError::with_detail(
        "MISSING_PROP",
        format!("node '{model}': {field}: required field is absent"),
        ErrorDetail {
            kind: Some(ErrorKind::MissingField),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: None,
            raw_value: None,
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostsRow {
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostsTopRow {
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PageRow {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostsByIdsRow {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct FeedRow {
    pub author_id: Option<f64>, // "author_id"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct DynamicWherePlan {
    pub frags: Vec<DynamicWhereFrag>, // "frags"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct DynamicWhereFrag {
    pub params: Vec<Option<WireValue>>, // "params"
    pub skipped: bool, // "skipped"
    pub sql: String, // "sql"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithPostsRow {
    pub id: Option<f64>, // "id"
    pub name: Option<String>, // "name"
    pub posts: Vec<UsersWithPostsRow_posts>, // "posts"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithPostsRow_posts {
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub tags: Vec<UsersWithPostsRow_posts_tags>, // "tags"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithPostsRow_posts_tags {
    pub id: Option<f64>, // "id"
    pub label: Option<String>, // "label"
    pub post_id: Option<f64>, // "post_id"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostsWithAuthorRow {
    pub author: Option<PostsWithAuthorRow_author>, // "author"
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostsWithAuthorRow_author {
    pub id: Option<f64>, // "id"
    pub name: Option<String>, // "name"
    pub post_count: Option<f64>, // "post_count"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct CapGuard {
    pub limit: i64, // "limit"
    pub model: Option<String>, // "model"
    pub relation: String, // "relation"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithCappedPostsRow {
    pub cappedPosts: Vec<UsersWithCappedPostsRow_cappedPosts>, // "cappedPosts"
    pub id: Option<f64>, // "id"
    pub name: Option<String>, // "name"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithCappedPostsRow_cappedPosts {
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithUncappedPostsRow {
    pub id: Option<f64>, // "id"
    pub name: Option<String>, // "name"
    pub uncappedPosts: Vec<UsersWithUncappedPostsRow_uncappedPosts>, // "uncappedPosts"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithUncappedPostsRow_uncappedPosts {
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithTopPostsRow {
    pub id: Option<f64>, // "id"
    pub name: Option<String>, // "name"
    pub topPosts: Vec<UsersWithTopPostsRow_topPosts>, // "topPosts"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UsersWithTopPostsRow_topPosts {
    pub author_id: Option<f64>, // "author_id"
    pub created_at: Option<String>, // "created_at"
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct WriteSummary {
    pub changes: i64, // "changes"
    pub lastInsertRowid: i64, // "lastInsertRowid"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct CreatePostReturningRow {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct RenamePostReturningRow {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct RemovePostReturningRow {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct RestatusPostsReturningRow {
    pub id: Option<f64>, // "id"
    pub status: Option<String>, // "status"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct RemovePostsByAuthorReturningRow {
    pub id: Option<f64>, // "id"
    pub title: Option<String>, // "title"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct TypedRowsRow {
    pub flag: Option<f64>, // "flag"
    pub id: Option<f64>, // "id"
    pub label: Option<String>, // "label"
    pub ts: Option<String>, // "ts"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct CreateTagsReturningRow {
    pub id: Option<f64>, // "id"
    pub label: Option<String>, // "label"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct RelabelTagsReturningRow {
    pub id: Option<f64>, // "id"
    pub label: Option<String>, // "label"
}

#[rustfmt::skip]
#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct RemoveTagsReturningRow {
    pub id: Option<f64>, // "id"
    pub label: Option<String>, // "label"
}

#[allow(dead_code)]
fn unknown_component(component: &str) -> BehaviorError {
    BehaviorError::new(
        "UNKNOWN_COMPONENT",
        format!("component '{component}' has no handler (fail-closed)"),
    )
}

// leaf_failure — a failure the LEAF reports (test glue): the runner assigns the node's failure code
// from its Error Policy Kind, so what a leaf carries is its message and its structured Error Value.
#[allow(dead_code)]
fn leaf_failure(message: impl Into<String>) -> BehaviorError {
    BehaviorError::new("LEAF_FAILURE", message)
}

// Combined read runners (STRUCT-returning — the fully de-plumbed CONCRETE path).
#[rustfmt::skip]
// posts — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn posts(
    authorId: i64,
) -> Result<Vec<PostsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<PostsRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(authorId)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE author_id = ? ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => PostsRow {
                        author_id: match probe_float_at(sub1.take("author_id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsRow", "author_id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        created_at: match probe_string_at(sub1.take("created_at")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsRow", "created_at", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        status: match probe_string_at(sub1.take("status")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsRow", "status", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// postsTop — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn postsTop(
) -> Result<Vec<PostsTopRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<PostsTopRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status, created_at FROM conf_posts ORDER BY id ASC LIMIT 2"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => PostsTopRow {
                        author_id: match probe_float_at(sub1.take("author_id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsTopRow", "author_id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        created_at: match probe_string_at(sub1.take("created_at")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsTopRow", "created_at", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsTopRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        status: match probe_string_at(sub1.take("status")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsTopRow", "status", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsTopRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// page — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn page(
    limit: i64,
    offset: i64,
) -> Result<Vec<PageRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<PageRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(limit), WireValue::Int(offset)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, title FROM conf_posts ORDER BY id ASC LIMIT ? OFFSET ?"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => PageRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PageRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PageRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// postsByIds — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn postsByIds(
    ids: Vec<i64>,
) -> Result<Vec<PostsByIdsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<PostsByIdsRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: ids.into_iter().map(WireValue::Int).collect() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, title FROM conf_posts WHERE id = ANY(?) ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => PostsByIdsRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsByIdsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsByIdsRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// feed — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn feed(
    authorId: i64,
    status: Option<String>,
    since: Option<String>,
) -> Result<Vec<FeedRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<FeedRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(authorId)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status FROM conf_posts WHERE author_id = ? ORDER BY id ASC"))), (Cow::Borrowed("whereDynamic"), match Some(DynamicWherePlan { frags: { let __v: Vec<DynamicWhereFrag> = vec![DynamicWhereFrag { params: { let __v: Vec<Option<WireValue>> = vec![status.clone().map(|__ov| WireValue::Str(__ov.into()))]; __v }, skipped: status.is_none(), sql: "status = ?".to_string() }, DynamicWhereFrag { params: { let __v: Vec<Option<WireValue>> = vec![since.clone().map(|__ov| WireValue::Str(__ov.into()))]; __v }, skipped: since.is_none(), sql: "created_at >= ?".to_string() }]; __v } }) { Some(ov0) => WireValue::Row(WireRow { entries: vec![(Cow::Borrowed("frags"), WireValue::List(WireList { items: ov0.frags.into_iter().map(|e1| WireValue::Row(WireRow { entries: vec![(Cow::Borrowed("params"), WireValue::List(WireList { items: e1.params.into_iter().map(|e3| match e3 { Some(ov4) => ov4, _ => WireValue::Null }).collect() })), (Cow::Borrowed("skipped"), WireValue::Bool(e1.skipped)), (Cow::Borrowed("sql"), WireValue::Str(e1.sql.into()))] })).collect() }))] }), _ => WireValue::Null }), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => FeedRow {
                        author_id: match probe_float_at(sub1.take("author_id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("FeedRow", "author_id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("FeedRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        status: match probe_string_at(sub1.take("status")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("FeedRow", "status", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("FeedRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// usersWithPosts — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn usersWithPosts(
) -> Result<Vec<UsersWithPostsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    let cell_n4: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n4 = std::cell::Cell::new(false);
    let _ = &produced_n4;
    let cell_n5: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n5 = std::cell::Cell::new(false);
    let _ = &produced_n5;
    let cell_n6: RefCell<Vec<UsersWithPostsRow>> = RefCell::new(Default::default());
    let produced_n6 = std::cell::Cell::new(false);
    let _ = &produced_n6;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, name FROM conf_users ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => l0.items,
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(value)", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow { entries: vec![(Cow::Borrowed("col"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("rows"), WireValue::List(WireList { items: cell_n0.borrow().clone() }))] };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match probe_list_at(Some(wire_n1)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n1", "n1", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.take() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE conf_posts.author_id = ANY(?::int[]) ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match probe_list_at(Some(wire_n2)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n2", "n2", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (pluck, parent:n2) ──
    if produced_n2.get() {
        let payload_n3 = WireRow { entries: vec![(Cow::Borrowed("col"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("rows"), WireValue::List(WireList { items: cell_n2.borrow().clone() }))] };
        let wire_n3 = match pluck_keys(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match probe_list_at(Some(wire_n3)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(value)")),
        };
        produced_n3.set(true);
    }
    // ── op 'n4' (executeSQL, parent:n3) ──
    if produced_n3.get() {
        let payload_n4 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n3.take() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, post_id, label FROM conf_tags WHERE conf_tags.post_id = ANY(?::int[]) ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
        let wire_n4 = match execute_sql(payload_n4) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n4", "fail", e)),
        };
        *cell_n4.borrow_mut() = match probe_list_at(Some(wire_n4)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n4", "n4", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n4", "n4", "arr(value)")),
        };
        produced_n4.set(true);
    }
    // ── op 'n5' (group, parent:n2) ──
    if produced_n2.get() {
        let payload_n5 = WireRow { entries: vec![(Cow::Borrowed("children"), WireValue::List(WireList { items: cell_n4.take() })), (Cow::Borrowed("fk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["post_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("into"), WireValue::Str(Cow::Borrowed("tags"))), (Cow::Borrowed("parents"), WireValue::List(WireList { items: cell_n2.borrow().clone() })), (Cow::Borrowed("pk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("single"), WireValue::Bool(false))] };
        let wire_n5 = match group_children(payload_n5) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n5", "fail", e)),
        };
        *cell_n5.borrow_mut() = match probe_list_at(Some(wire_n5)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n5", "n5", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n5", "n5", "arr(value)")),
        };
        produced_n5.set(true);
    }
    // ── op 'n6' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n6 = WireRow { entries: vec![(Cow::Borrowed("children"), WireValue::List(WireList { items: cell_n5.take() })), (Cow::Borrowed("fk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["author_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("into"), WireValue::Str(Cow::Borrowed("posts"))), (Cow::Borrowed("parents"), WireValue::List(WireList { items: cell_n0.borrow().clone() })), (Cow::Borrowed("pk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("single"), WireValue::Bool(false))] };
        let wire_n6 = match group_children(payload_n6) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n6", "fail", e)),
        };
        *cell_n6.borrow_mut() = match probe_list_at(Some(wire_n6)) {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.items.len());
                for e0 in l0.items {
                    acc0.push(match probe_row_at(Some(e0)) {
                        Probe::Got(mut sub1) => UsersWithPostsRow {
                            id: match probe_float_at(sub1.take("id")) {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            name: match probe_string_at(sub1.take("name")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            posts: match probe_list_at(sub1.take("posts")) {
                                Probe::Got(l4) => {
                                    let mut acc4 = Vec::with_capacity(l4.items.len());
                                    for e4 in l4.items {
                                        acc4.push(match probe_row_at(Some(e4)) {
                                            Probe::Got(mut sub5) => UsersWithPostsRow_posts {
                                                author_id: match probe_float_at(sub5.take("author_id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "author_id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                created_at: match probe_string_at(sub5.take("created_at")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "created_at", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                id: match probe_float_at(sub5.take("id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                status: match probe_string_at(sub5.take("status")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "status", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                tags: match probe_list_at(sub5.take("tags")) {
                                                    Probe::Got(l10) => {
                                                        let mut acc10 = Vec::with_capacity(l10.items.len());
                                                        for e10 in l10.items {
                                                            acc10.push(match probe_row_at(Some(e10)) {
                                                                Probe::Got(mut sub11) => UsersWithPostsRow_posts_tags {
                                                                    id: match probe_float_at(sub11.take("id")) {
                                                                        Probe::Got(v) => Some(v),
                                                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts_tags", "id", "opt(float)", actual_wire_type, raw_value)),
                                                                        Probe::Null { .. } | Probe::Absent => None,
                                                                    },
                                                                    label: match probe_string_at(sub11.take("label")) {
                                                                        Probe::Got(v) => Some(v.into_owned()),
                                                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts_tags", "label", "opt(string)", actual_wire_type, raw_value)),
                                                                        Probe::Null { .. } | Probe::Absent => None,
                                                                    },
                                                                    post_id: match probe_float_at(sub11.take("post_id")) {
                                                                        Probe::Got(v) => Some(v),
                                                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts_tags", "post_id", "opt(float)", actual_wire_type, raw_value)),
                                                                        Probe::Null { .. } | Probe::Absent => None,
                                                                    },
                                                                },
                                                                Probe::Wrong { actual_wire_type, raw_value }
                                                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "tags", "obj{id:opt(float),label:opt(string),post_id:opt(float)}", actual_wire_type, raw_value)),
                                                                Probe::Absent => return Err(de_missing_field("UsersWithPostsRow_posts", "tags", "obj{id:opt(float),label:opt(string),post_id:opt(float)}")),
                                                            });
                                                        }
                                                        acc10
                                                    },
                                                    Probe::Wrong { actual_wire_type, raw_value }
                                                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "tags", "arr(obj{id:opt(float),label:opt(string),post_id:opt(float)})", actual_wire_type, raw_value)),
                                                    Probe::Absent => return Err(de_missing_field("UsersWithPostsRow_posts", "tags", "arr(obj{id:opt(float),label:opt(string),post_id:opt(float)})")),
                                                },
                                                title: match probe_string_at(sub5.take("title")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow_posts", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow", "posts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UsersWithPostsRow", "posts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)}")),
                                        });
                                    }
                                    acc4
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithPostsRow", "posts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UsersWithPostsRow", "posts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n6", "n6", "obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "arr(obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n6", "n6", "arr(obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})})")),
        };
        produced_n6.set(true);
    }
    let __out = cell_n6.take();
    Ok(__out)
}

#[rustfmt::skip]
// postsWithAuthor — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn postsWithAuthor(
) -> Result<Vec<PostsWithAuthorRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<PostsWithAuthorRow>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status, created_at FROM conf_posts ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => l0.items,
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(value)", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow { entries: vec![(Cow::Borrowed("col"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["author_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("rows"), WireValue::List(WireList { items: cell_n0.borrow().clone() }))] };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match probe_list_at(Some(wire_n1)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n1", "n1", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.take() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, name, post_count FROM conf_users WHERE conf_users.id = ANY(?::int[])"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match probe_list_at(Some(wire_n2)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n2", "n2", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow { entries: vec![(Cow::Borrowed("children"), WireValue::List(WireList { items: cell_n2.take() })), (Cow::Borrowed("fk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("into"), WireValue::Str(Cow::Borrowed("author"))), (Cow::Borrowed("parents"), WireValue::List(WireList { items: cell_n0.borrow().clone() })), (Cow::Borrowed("pk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["author_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("single"), WireValue::Bool(true))] };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match probe_list_at(Some(wire_n3)) {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.items.len());
                for e0 in l0.items {
                    acc0.push(match probe_row_at(Some(e0)) {
                        Probe::Got(mut sub1) => PostsWithAuthorRow {
                            author: match probe_row_at(sub1.take("author")) {
                                Probe::Got(mut sub2) => Some(PostsWithAuthorRow_author {
                                    id: match probe_float_at(sub2.take("id")) {
                                        Probe::Got(v) => Some(v),
                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow_author", "id", "opt(float)", actual_wire_type, raw_value)),
                                        Probe::Null { .. } | Probe::Absent => None,
                                    },
                                    name: match probe_string_at(sub2.take("name")) {
                                        Probe::Got(v) => Some(v.into_owned()),
                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow_author", "name", "opt(string)", actual_wire_type, raw_value)),
                                        Probe::Null { .. } | Probe::Absent => None,
                                    },
                                    post_count: match probe_float_at(sub2.take("post_count")) {
                                        Probe::Got(v) => Some(v),
                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow_author", "post_count", "opt(float)", actual_wire_type, raw_value)),
                                        Probe::Null { .. } | Probe::Absent => None,
                                    },
                                }),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow", "author", "opt(obj{id:opt(float),name:opt(string),post_count:opt(float)})", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            author_id: match probe_float_at(sub1.take("author_id")) {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow", "author_id", "opt(float)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            created_at: match probe_string_at(sub1.take("created_at")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow", "created_at", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            id: match probe_float_at(sub1.take("id")) {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow", "id", "opt(float)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            status: match probe_string_at(sub1.take("status")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow", "status", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            title: match probe_string_at(sub1.take("title")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostsWithAuthorRow", "title", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.take();
    Ok(__out)
}

#[rustfmt::skip]
// usersWithCappedPosts — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn usersWithCappedPosts(
) -> Result<Vec<UsersWithCappedPostsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<UsersWithCappedPostsRow>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, name FROM conf_users ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => l0.items,
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(value)", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow { entries: vec![(Cow::Borrowed("col"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("rows"), WireValue::List(WireList { items: cell_n0.borrow().clone() }))] };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match probe_list_at(Some(wire_n1)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n1", "n1", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("guard"), match Some(CapGuard { limit: 2i64, model: Some("conf_posts".to_string()), relation: "cappedPosts".to_string() }) { Some(ov0) => WireValue::Row(WireRow { entries: vec![(Cow::Borrowed("limit"), WireValue::Int(ov0.limit)), (Cow::Borrowed("model"), match ov0.model { Some(ov1) => WireValue::Str(ov1.into()), _ => WireValue::Null }), (Cow::Borrowed("relation"), WireValue::Str(ov0.relation.into()))] }), _ => WireValue::Null }), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.take() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE conf_posts.author_id = ANY(?::int[]) ORDER BY id ASC"))), (Cow::Borrowed("whereDynamic"), match Some(DynamicWherePlan { frags: { let __v: Vec<DynamicWhereFrag> = vec![]; __v } }) { Some(ov0) => WireValue::Row(WireRow { entries: vec![(Cow::Borrowed("frags"), WireValue::List(WireList { items: ov0.frags.into_iter().map(|e1| WireValue::Row(WireRow { entries: vec![(Cow::Borrowed("params"), WireValue::List(WireList { items: e1.params.into_iter().map(|e3| match e3 { Some(ov4) => ov4, _ => WireValue::Null }).collect() })), (Cow::Borrowed("skipped"), WireValue::Bool(e1.skipped)), (Cow::Borrowed("sql"), WireValue::Str(e1.sql.into()))] })).collect() }))] }), _ => WireValue::Null }), (Cow::Borrowed("write"), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match probe_list_at(Some(wire_n2)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n2", "n2", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow { entries: vec![(Cow::Borrowed("children"), WireValue::List(WireList { items: cell_n2.take() })), (Cow::Borrowed("fk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["author_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("into"), WireValue::Str(Cow::Borrowed("cappedPosts"))), (Cow::Borrowed("parents"), WireValue::List(WireList { items: cell_n0.borrow().clone() })), (Cow::Borrowed("pk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("single"), WireValue::Bool(false))] };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match probe_list_at(Some(wire_n3)) {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.items.len());
                for e0 in l0.items {
                    acc0.push(match probe_row_at(Some(e0)) {
                        Probe::Got(mut sub1) => UsersWithCappedPostsRow {
                            cappedPosts: match probe_list_at(sub1.take("cappedPosts")) {
                                Probe::Got(l2) => {
                                    let mut acc2 = Vec::with_capacity(l2.items.len());
                                    for e2 in l2.items {
                                        acc2.push(match probe_row_at(Some(e2)) {
                                            Probe::Got(mut sub3) => UsersWithCappedPostsRow_cappedPosts {
                                                author_id: match probe_float_at(sub3.take("author_id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow_cappedPosts", "author_id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                created_at: match probe_string_at(sub3.take("created_at")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow_cappedPosts", "created_at", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                id: match probe_float_at(sub3.take("id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow_cappedPosts", "id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                status: match probe_string_at(sub3.take("status")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow_cappedPosts", "status", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                title: match probe_string_at(sub3.take("title")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow_cappedPosts", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow", "cappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UsersWithCappedPostsRow", "cappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")),
                                        });
                                    }
                                    acc2
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow", "cappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UsersWithCappedPostsRow", "cappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")),
                            },
                            id: match probe_float_at(sub1.take("id")) {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            name: match probe_string_at(sub1.take("name")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithCappedPostsRow", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.take();
    Ok(__out)
}

#[rustfmt::skip]
// usersWithUncappedPosts — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn usersWithUncappedPosts(
) -> Result<Vec<UsersWithUncappedPostsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<UsersWithUncappedPostsRow>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, name FROM conf_users ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => l0.items,
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(value)", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow { entries: vec![(Cow::Borrowed("col"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("rows"), WireValue::List(WireList { items: cell_n0.borrow().clone() }))] };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match probe_list_at(Some(wire_n1)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n1", "n1", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.take() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE conf_posts.author_id = ANY(?::int[]) ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match probe_list_at(Some(wire_n2)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n2", "n2", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow { entries: vec![(Cow::Borrowed("children"), WireValue::List(WireList { items: cell_n2.take() })), (Cow::Borrowed("fk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["author_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("into"), WireValue::Str(Cow::Borrowed("uncappedPosts"))), (Cow::Borrowed("parents"), WireValue::List(WireList { items: cell_n0.borrow().clone() })), (Cow::Borrowed("pk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("single"), WireValue::Bool(false))] };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match probe_list_at(Some(wire_n3)) {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.items.len());
                for e0 in l0.items {
                    acc0.push(match probe_row_at(Some(e0)) {
                        Probe::Got(mut sub1) => UsersWithUncappedPostsRow {
                            id: match probe_float_at(sub1.take("id")) {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            name: match probe_string_at(sub1.take("name")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            uncappedPosts: match probe_list_at(sub1.take("uncappedPosts")) {
                                Probe::Got(l4) => {
                                    let mut acc4 = Vec::with_capacity(l4.items.len());
                                    for e4 in l4.items {
                                        acc4.push(match probe_row_at(Some(e4)) {
                                            Probe::Got(mut sub5) => UsersWithUncappedPostsRow_uncappedPosts {
                                                author_id: match probe_float_at(sub5.take("author_id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow_uncappedPosts", "author_id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                created_at: match probe_string_at(sub5.take("created_at")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow_uncappedPosts", "created_at", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                id: match probe_float_at(sub5.take("id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow_uncappedPosts", "id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                status: match probe_string_at(sub5.take("status")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow_uncappedPosts", "status", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                title: match probe_string_at(sub5.take("title")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow_uncappedPosts", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow", "uncappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UsersWithUncappedPostsRow", "uncappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")),
                                        });
                                    }
                                    acc4
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithUncappedPostsRow", "uncappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UsersWithUncappedPostsRow", "uncappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.take();
    Ok(__out)
}

#[rustfmt::skip]
// usersWithTopPosts — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn usersWithTopPosts(
) -> Result<Vec<UsersWithTopPostsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<UsersWithTopPostsRow>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, name FROM conf_users ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => l0.items,
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(value)", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow { entries: vec![(Cow::Borrowed("col"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("rows"), WireValue::List(WireList { items: cell_n0.borrow().clone() }))] };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match probe_list_at(Some(wire_n1)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n1", "n1", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.take() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT conf_posts.* FROM unnest(?::int[]) AS _keys(key) CROSS JOIN LATERAL (SELECT * FROM conf_posts WHERE conf_posts.author_id = _keys.key ORDER BY id ASC LIMIT 1) conf_posts"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match probe_list_at(Some(wire_n2)) {
            Probe::Got(l0) => l0.items,
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n2", "n2", "arr(value)", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow { entries: vec![(Cow::Borrowed("children"), WireValue::List(WireList { items: cell_n2.take() })), (Cow::Borrowed("fk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["author_id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("into"), WireValue::Str(Cow::Borrowed("topPosts"))), (Cow::Borrowed("parents"), WireValue::List(WireList { items: cell_n0.borrow().clone() })), (Cow::Borrowed("pk"), WireValue::List(WireList { items: { let __v: Vec<String> = vec!["id".to_string()]; __v }.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })), (Cow::Borrowed("single"), WireValue::Bool(false))] };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match probe_list_at(Some(wire_n3)) {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.items.len());
                for e0 in l0.items {
                    acc0.push(match probe_row_at(Some(e0)) {
                        Probe::Got(mut sub1) => UsersWithTopPostsRow {
                            id: match probe_float_at(sub1.take("id")) {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            name: match probe_string_at(sub1.take("name")) {
                                Probe::Got(v) => Some(v.into_owned()),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            topPosts: match probe_list_at(sub1.take("topPosts")) {
                                Probe::Got(l4) => {
                                    let mut acc4 = Vec::with_capacity(l4.items.len());
                                    for e4 in l4.items {
                                        acc4.push(match probe_row_at(Some(e4)) {
                                            Probe::Got(mut sub5) => UsersWithTopPostsRow_topPosts {
                                                author_id: match probe_float_at(sub5.take("author_id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow_topPosts", "author_id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                created_at: match probe_string_at(sub5.take("created_at")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow_topPosts", "created_at", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                id: match probe_float_at(sub5.take("id")) {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow_topPosts", "id", "opt(float)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                status: match probe_string_at(sub5.take("status")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow_topPosts", "status", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                title: match probe_string_at(sub5.take("title")) {
                                                    Probe::Got(v) => Some(v.into_owned()),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow_topPosts", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow", "topPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UsersWithTopPostsRow", "topPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")),
                                        });
                                    }
                                    acc4
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UsersWithTopPostsRow", "topPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UsersWithTopPostsRow", "topPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.take();
    Ok(__out)
}

#[rustfmt::skip]
// createPost — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn createPost(
    id: i64,
    authorId: i64,
    title: String,
    status: String,
    createdAt: String,
) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(authorId), WireValue::Str(createdAt.into()), WireValue::Int(id), WireValue::Str(status.into()), WireValue::Str(title.into())]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("INSERT INTO conf_posts (author_id, created_at, id, status, title) VALUES (?, ?, ?, ?, ?)"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => WriteSummary {
                        changes: match probe_int_at(sub1.take("changes")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "changes", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "changes", "int")),
                        },
                        lastInsertRowid: match probe_int_at(sub1.take("lastInsertRowid")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "lastInsertRowid", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "lastInsertRowid", "int")),
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{changes:int,lastInsertRowid:int}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// renamePost — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn renamePost(
    title: String,
    id: i64,
) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(title.into()), WireValue::Int(id)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("UPDATE conf_posts SET title = ? WHERE id = ?"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => WriteSummary {
                        changes: match probe_int_at(sub1.take("changes")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "changes", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "changes", "int")),
                        },
                        lastInsertRowid: match probe_int_at(sub1.take("lastInsertRowid")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "lastInsertRowid", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "lastInsertRowid", "int")),
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{changes:int,lastInsertRowid:int}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// removePost — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn removePost(
    id: i64,
) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(id)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("DELETE FROM conf_posts WHERE id = ?"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => WriteSummary {
                        changes: match probe_int_at(sub1.take("changes")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "changes", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "changes", "int")),
                        },
                        lastInsertRowid: match probe_int_at(sub1.take("lastInsertRowid")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "lastInsertRowid", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "lastInsertRowid", "int")),
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{changes:int,lastInsertRowid:int}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// createPostReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn createPostReturning(
    id: i64,
    authorId: i64,
    title: String,
    status: String,
    createdAt: String,
) -> Result<Vec<CreatePostReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<CreatePostReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(authorId), WireValue::Str(createdAt.into()), WireValue::Int(id), WireValue::Str(status.into()), WireValue::Str(title.into())]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("INSERT INTO conf_posts (author_id, created_at, id, status, title) VALUES (?, ?, ?, ?, ?) RETURNING id, title"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => CreatePostReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CreatePostReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CreatePostReturningRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// renamePostReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn renamePostReturning(
    title: String,
    id: i64,
) -> Result<Vec<RenamePostReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<RenamePostReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(title.into()), WireValue::Int(id)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("UPDATE conf_posts SET title = ? WHERE id = ? RETURNING id, title"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => RenamePostReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RenamePostReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RenamePostReturningRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// removePostReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn removePostReturning(
    id: i64,
) -> Result<Vec<RemovePostReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<RemovePostReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(id)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("DELETE FROM conf_posts WHERE id = ? RETURNING id, title"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => RemovePostReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RemovePostReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RemovePostReturningRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// restatusPostsReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn restatusPostsReturning(
    status: String,
    authorId: i64,
) -> Result<Vec<RestatusPostsReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<RestatusPostsReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(status.into()), WireValue::Int(authorId)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("UPDATE conf_posts SET status = ? WHERE author_id = ? RETURNING id, status"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => RestatusPostsReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RestatusPostsReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        status: match probe_string_at(sub1.take("status")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RestatusPostsReturningRow", "status", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),status:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),status:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),status:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),status:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// removePostsByAuthorReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn removePostsByAuthorReturning(
    authorId: i64,
) -> Result<Vec<RemovePostsByAuthorReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<RemovePostsByAuthorReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Int(authorId)]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("DELETE FROM conf_posts WHERE author_id = ? RETURNING id, title"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => RemovePostsByAuthorReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RemovePostsByAuthorReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        title: match probe_string_at(sub1.take("title")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RemovePostsByAuthorReturningRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// typedRows — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn typedRows(
) -> Result<Vec<TypedRowsRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<TypedRowsRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("SELECT id, ts, flag, label FROM conf_typed ORDER BY id ASC"))), (Cow::Borrowed("write"), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => TypedRowsRow {
                        flag: match probe_float_at(sub1.take("flag")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TypedRowsRow", "flag", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TypedRowsRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        label: match probe_string_at(sub1.take("label")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TypedRowsRow", "label", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        ts: match probe_string_at(sub1.take("ts")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TypedRowsRow", "ts", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// createTags — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn createTags(
    rows_id: Vec<i64>,
    rows_post_id: Vec<i64>,
    rows_label: Vec<String>,
) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: rows_id.into_iter().map(WireValue::Int).collect() }), WireValue::List(WireList { items: rows_label.into_iter().map(|e0| WireValue::Str(e0.into())).collect() }), WireValue::List(WireList { items: rows_post_id.into_iter().map(WireValue::Int).collect() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("INSERT INTO conf_tags (id, label, post_id) SELECT v.id, v.label, v.post_id FROM UNNEST(?::int[], ?::text[], ?::int[]) AS v(id, label, post_id)"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => WriteSummary {
                        changes: match probe_int_at(sub1.take("changes")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "changes", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "changes", "int")),
                        },
                        lastInsertRowid: match probe_int_at(sub1.take("lastInsertRowid")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "lastInsertRowid", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "lastInsertRowid", "int")),
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{changes:int,lastInsertRowid:int}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// removeTags — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn removeTags(
    ids: Vec<i64>,
) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: ids.into_iter().map(WireValue::Int).collect() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(false)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("DELETE FROM conf_tags WHERE id = ANY(?)"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => WriteSummary {
                        changes: match probe_int_at(sub1.take("changes")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "changes", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "changes", "int")),
                        },
                        lastInsertRowid: match probe_int_at(sub1.take("lastInsertRowid")) {
                            Probe::Got(v) => v,
                            Probe::Wrong { actual_wire_type, raw_value }
                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("WriteSummary", "lastInsertRowid", "int", actual_wire_type, raw_value)),
                            Probe::Absent => return Err(de_missing_field("WriteSummary", "lastInsertRowid", "int")),
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{changes:int,lastInsertRowid:int}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// createTagsReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn createTagsReturning(
    rows_id: Vec<i64>,
    rows_post_id: Vec<i64>,
    rows_label: Vec<String>,
) -> Result<Vec<CreateTagsReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<CreateTagsReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: rows_id.into_iter().map(WireValue::Int).collect() }), WireValue::List(WireList { items: rows_label.into_iter().map(|e0| WireValue::Str(e0.into())).collect() }), WireValue::List(WireList { items: rows_post_id.into_iter().map(WireValue::Int).collect() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("INSERT INTO conf_tags (id, label, post_id) SELECT v.id, v.label, v.post_id FROM UNNEST(?::int[], ?::text[], ?::int[]) AS v(id, label, post_id) RETURNING id, label"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => CreateTagsReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CreateTagsReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        label: match probe_string_at(sub1.take("label")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CreateTagsReturningRow", "label", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),label:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// relabelTagsReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn relabelTagsReturning(
    rows_id: Vec<i64>,
    rows_label: Vec<String>,
) -> Result<Vec<RelabelTagsReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<RelabelTagsReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: rows_id.into_iter().map(WireValue::Int).collect() }), WireValue::List(WireList { items: rows_label.into_iter().map(|e0| WireValue::Str(e0.into())).collect() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("UPDATE conf_tags AS t SET label = v.label FROM UNNEST(?::int[], ?::text[]) AS v(id, label) WHERE t.id = v.id RETURNING t.id, t.label"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => RelabelTagsReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RelabelTagsReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        label: match probe_string_at(sub1.take("label")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RelabelTagsReturningRow", "label", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),label:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// removeTagsReturning — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn removeTagsReturning(
    ids: Vec<i64>,
) -> Result<Vec<RemoveTagsReturningRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<RemoveTagsReturningRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![(Cow::Borrowed("bigint"), WireValue::Bool(false)), (Cow::Borrowed("params"), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: ids.into_iter().map(WireValue::Int).collect() })]; __v } })), (Cow::Borrowed("returning"), WireValue::Bool(true)), (Cow::Borrowed("sql"), WireValue::Str(Cow::Borrowed("DELETE FROM conf_tags WHERE id = ANY(?) RETURNING id, label"))), (Cow::Borrowed("write"), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match probe_list_at(Some(wire_n0)) {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.items.len());
            for e0 in l0.items {
                acc0.push(match probe_row_at(Some(e0)) {
                    Probe::Got(mut sub1) => RemoveTagsReturningRow {
                        id: match probe_float_at(sub1.take("id")) {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RemoveTagsReturningRow", "id", "opt(float)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        label: match probe_string_at(sub1.take("label")) {
                            Probe::Got(v) => Some(v.into_owned()),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("RemoveTagsReturningRow", "label", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:opt(float),label:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.take();
    Ok(__out)
}

#[rustfmt::skip]
// COMPONENT_NAMES_NATIVE_RAW — covered reads exposed on the combined struct-native path. Each is a
// 1:1 entry <method>(<positional params>) -> Result<T, BehaviorError>: a STRUCT return (the consumer calls
// it with the authored args + supplies the op-agnostic leaf transport symbols the entry calls).
// See INTEGRATION.md §6.
pub const COMPONENT_NAMES_NATIVE_RAW: [&str; 24] = ["posts", "postsTop", "page", "postsByIds", "feed", "usersWithPosts", "postsWithAuthor", "usersWithCappedPosts", "usersWithUncappedPosts", "usersWithTopPosts", "createPost", "renamePost", "removePost", "createPostReturning", "renamePostReturning", "removePostReturning", "restatusPostsReturning", "removePostsByAuthorReturning", "typedRows", "createTags", "removeTags", "createTagsReturning", "relabelTagsReturning", "removeTagsReturning"];
