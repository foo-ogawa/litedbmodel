#![allow(dead_code, unused_imports, unused_variables, non_snake_case)]
// GENERATED by behavior-contracts (bc#90, rust-typed-native — the RUNTIME-FREE read de-box; STRUCT-ONLY,
// FULLY-NATIVE). A covered read is a strictly-sequential typed componentRef chain (point reads +
// relation children), emitted ONLY as a STRUCT-returning runner that calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY at each covered node's execution point: native ports struct IN (direct
// construction — every port is a CONCRETE Rust value: String / bool / Vec<&str> projection / i64 limit —
// NO boxed Value, NO by-name accessor); the transport is called with the node's port fields spread and
// returns the node's result WIRE (a value implementing WireValue), and the runner de-boxes it INLINE
// (fully-unrolled probe/match — no decode helper, no output recursion) into the node's outType struct.
// There is NO per-node handler indirection — a leaf is ONE transport, not one method per authored node.
// The covered plane carries NO bc-runtime reference at all — failures use the LOCAL BehaviorError (same
// codes, byte-equal to run_behavior). INLINE sequential (no plan driver) so a relation child reads the
// parent's REAL struct result via direct field access and the child-present decision is made from the real
// parent value — relation / connection converge with run_behavior (fixes #323/#74). The exposed API
// is the 1:1 entry <method>(<positional params>) -> Result<T, BehaviorError> (the InNR_<comp> box removed);
// the consumer supplies the leaf transport symbols (default in-scope names leaf_<comp>, or a leafTransport
// import). This module
// is FULLY NATIVE: a naive grep for boxing primitives OR for the bc-runtime crate finds nothing, by design.
use std::cell::RefCell;

// #165 --shared-types-import: the BC-OWNED wire seam lives in the shared crate/module — import it.
use litedbmodel_runtime::*;

// De-box / op-failure constructors (produce the shared BehaviorError, imported above). Runtime-free.
// op_failed — wrap a leaf failure as the node's Failure under its Error Policy Kind, carrying the
// leaf's structured detail through unchanged (the runner transports the Error Value; it does not
// synthesize or re-interpret it).
#[allow(dead_code)]
fn op_failed(node: &str, policy: &str, e: BehaviorError) -> BehaviorError {
    // The Kind names itself, and 'retry' says it gave up — the attempts are Runtime-owned (scp-error.md:
    // tuning never enters the IR), so what the plan reports is the exhausted retry.
    let exhausted = if policy == "retry" {
        " (exhausted)"
    } else {
        ""
    };
    BehaviorError {
        code: "OP_FAILED".to_string(),
        message: format!(
            "operation '{node}' failed under '{policy}' policy{exhausted}: {}",
            e.message
        ),
        detail: e.detail,
    }
}

// de_type_mismatch / de_missing_field / de_overflow — the de-box mismatch failures the generated decode
// produces (scp-error.md "The Error Value"). Codes are byte-equal to the runtime outType check
// (TYPE_MISMATCH / MISSING_PROP) so the covered read stays equivalent to run_behavior; detail.kind
// distinguishes typeMismatch / missingField / overflow.
#[allow(dead_code)]
fn de_type_mismatch(
    model: &str,
    field: &str,
    expected: &str,
    actual_wire: String,
    raw: String,
) -> BehaviorError {
    BehaviorError::with_detail(
        "TYPE_MISMATCH",
        format!("node '{model}': {field}: expected {expected}, got {actual_wire}"),
        ErrorDetail {
            kind: Some(ErrorKind::TypeMismatch),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: Some(actual_wire),
            raw_value: Some(raw),
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[allow(dead_code)]
fn de_missing_field(model: &str, field: &str, expected: &str) -> BehaviorError {
    BehaviorError::with_detail(
        "MISSING_PROP",
        format!("node '{model}': {field}: required field is absent"),
        ErrorDetail {
            kind: Some(ErrorKind::MissingField),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: None,
            raw_value: None,
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[allow(dead_code)]
fn de_overflow(
    model: &str,
    field: &str,
    expected: &str,
    actual_wire: String,
    raw: String,
) -> BehaviorError {
    let message = format!("node '{model}': {field}: {raw} is outside the range of {expected}");
    BehaviorError::with_detail(
        "TYPE_MISMATCH",
        message,
        ErrorDetail {
            kind: Some(ErrorKind::Overflow),
            model: Some(model.to_string()),
            field: Some(field.to_string()),
            expected_type: Some(expected.to_string()),
            actual_wire_type: Some(actual_wire),
            raw_value: Some(raw),
            context: std::collections::BTreeMap::new(),
        },
    )
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UserRow {
    pub email: Option<String>, // "email"
    pub id: i64,               // "id"
    pub name: Option<String>,  // "name"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostFullRow {
    pub author_id: Option<i64>,     // "author_id"
    pub content: Option<String>,    // "content"
    pub created_at: Option<String>, // "created_at"
    pub id: i64,                    // "id"
    pub published: Option<i64>,     // "published"
    pub title: Option<String>,      // "title"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UserWithPosts {
    pub email: Option<String>, // "email"
    pub id: i64,               // "id"
    pub name: Option<String>,  // "name"
    pub posts: Vec<PostRow>,   // "posts"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostRow {
    pub author_id: Option<i64>, // "author_id"
    pub id: i64,                // "id"
    pub title: Option<String>,  // "title"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UserWithPostsAndComments {
    pub email: Option<String>,        // "email"
    pub id: i64,                      // "id"
    pub name: Option<String>,         // "name"
    pub posts: Vec<PostWithComments>, // "posts"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct PostWithComments {
    pub author_id: Option<i64>,    // "author_id"
    pub comments: Vec<CommentRow>, // "comments"
    pub id: i64,                   // "id"
    pub title: Option<String>,     // "title"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct CommentRow {
    pub body: Option<String>, // "body"
    pub id: Option<i64>,      // "id"
    pub post_id: Option<i64>, // "post_id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct TenantUserWithPosts {
    pub name: Option<String>,               // "name"
    pub posts: Vec<TenantPostWithComments>, // "posts"
    pub tenant_id: Option<i64>,             // "tenant_id"
    pub user_id: Option<i64>,               // "user_id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct TenantPostWithComments {
    pub comments: Vec<TenantCommentRow>, // "comments"
    pub post_id: Option<i64>,            // "post_id"
    pub tenant_id: Option<i64>,          // "tenant_id"
    pub title: Option<String>,           // "title"
    pub user_id: Option<i64>,            // "user_id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct TenantCommentRow {
    pub body: Option<String>,    // "body"
    pub comment_id: Option<i64>, // "comment_id"
    pub post_id: Option<i64>,    // "post_id"
    pub tenant_id: Option<i64>,  // "tenant_id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct WriteSummary {
    pub changes: i64,         // "changes"
    pub lastInsertRowid: i64, // "lastInsertRowid"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct IdRow {
    pub id: i64, // "id"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct NewUser {
    pub email: String, // "email"
    pub name: String,  // "name"
}

#[derive(Clone, Default)]
#[allow(dead_code)]
pub struct UserPatch {
    pub id: i64,      // "id"
    pub name: String, // "name"
}

#[allow(dead_code)]
fn unknown_component(component: &str) -> BehaviorError {
    BehaviorError::new(
        "UNKNOWN_COMPONENT",
        format!("component '{component}' has no handler (fail-closed)"),
    )
}

// leaf_failure — a failure the LEAF reports (test glue): the runner assigns the node's failure code
// from its Error Policy Kind, so what a leaf carries is its message and its structured Error Value.
#[allow(dead_code)]
fn leaf_failure(message: impl Into<String>) -> BehaviorError {
    BehaviorError::new("LEAF_FAILURE", message)
}

// Combined read runners (STRUCT-returning — the fully de-plumbed CONCRETE path).
// findAll — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn findAll() -> Result<Vec<UserRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<UserRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str(
                    "SELECT id, email, name FROM benchmark_users ORDER BY id ASC LIMIT 100"
                        .to_string(),
                ),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => UserRow {
                        email: match sub1.probe_string("email") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "email",
                                    "opt(string)",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "UserRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("UserRow", "id", "int"))
                            }
                        },
                        name: match sub1.probe_string("name") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "name",
                                    "opt(string)",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{email:opt(string),id:int,name:opt(string)}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{email:opt(string),id:int,name:opt(string)}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{email:opt(string),id:int,name:opt(string)})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{email:opt(string),id:int,name:opt(string)})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// filterPaginateSort — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn filterPaginateSort(published: i64) -> Result<Vec<PostFullRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<PostFullRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::int(published)]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT id, title, content, published, author_id, created_at FROM benchmark_posts WHERE published = ? ORDER BY created_at DESC LIMIT 20 OFFSET 10".to_string())), ("write".to_string(), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => PostFullRow {
                        author_id: match sub1.probe_number("author_id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("PostFullRow", "author_id", "opt(int)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostFullRow", "author_id", "opt(int)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        content: match sub1.probe_string("content") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostFullRow", "content", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        created_at: match sub1.probe_string("created_at") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostFullRow", "created_at", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match sub1.probe_number("id") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => return Err(de_overflow("PostFullRow", "id", "int", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value }
                            | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostFullRow", "id", "int", actual_wire_type, raw_value)),
                            NumProbe::Absent => return Err(de_missing_field("PostFullRow", "id", "int")),
                        },
                        published: match sub1.probe_number("published") {
                            NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                Ok(n) => Some(n),
                                Err(_) => return Err(de_overflow("PostFullRow", "published", "opt(int)", actual_wire_type, raw)),
                            },
                            NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostFullRow", "published", "opt(int)", actual_wire_type, raw_value)),
                            NumProbe::Null { .. } | NumProbe::Absent => None,
                        },
                        title: match sub1.probe_string("title") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostFullRow", "title", "opt(string)", actual_wire_type, raw_value)),
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong { actual_wire_type, raw_value }
                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "obj{author_id:opt(int),content:opt(string),created_at:opt(string),id:int,published:opt(int),title:opt(string)}", actual_wire_type, raw_value)),
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{author_id:opt(int),content:opt(string),created_at:opt(string),id:int,published:opt(int),title:opt(string)}")),
                });
            }
            acc0
        },
        Probe::Wrong { actual_wire_type, raw_value }
        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n0", "n0", "arr(obj{author_id:opt(int),content:opt(string),created_at:opt(string),id:int,published:opt(int),title:opt(string)})", actual_wire_type, raw_value)),
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{author_id:opt(int),content:opt(string),created_at:opt(string),id:int,published:opt(int),title:opt(string)})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// findFirst — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn findFirst(name: String) -> Result<Vec<UserRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<UserRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![WireValue::Str(name.clone())];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str(
                    "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1"
                        .to_string(),
                ),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => UserRow {
                        email: match sub1.probe_string("email") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "email",
                                    "opt(string)",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "UserRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("UserRow", "id", "int"))
                            }
                        },
                        name: match sub1.probe_string("name") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "name",
                                    "opt(string)",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{email:opt(string),id:int,name:opt(string)}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{email:opt(string),id:int,name:opt(string)}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{email:opt(string),id:int,name:opt(string)})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{email:opt(string),id:int,name:opt(string)})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// findUnique — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn findUnique(email: String) -> Result<Vec<UserRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<UserRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![WireValue::Str(email.clone())];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str(
                    "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1"
                        .to_string(),
                ),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => UserRow {
                        email: match sub1.probe_string("email") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "email",
                                    "opt(string)",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "UserRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("UserRow", "id", "int"))
                            }
                        },
                        name: match sub1.probe_string("name") {
                            Probe::Got(v) => Some(v),
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "UserRow",
                                    "name",
                                    "opt(string)",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Null { .. } | Probe::Absent => None,
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{email:opt(string),id:int,name:opt(string)}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{email:opt(string),id:int,name:opt(string)}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{email:opt(string),id:int,name:opt(string)})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{email:opt(string),id:int,name:opt(string)})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// nestedFindAll — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedFindAll() -> Result<Vec<UserWithPosts>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<UserWithPosts>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str("SELECT id, email, name FROM benchmark_users LIMIT 100".to_string()),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => l0.items.clone(),
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(value)",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match wire_n1.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n1",
                    "n1",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match wire_n2.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n2",
                    "n2",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["author_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("posts".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => UserWithPosts {
                            email: match sub1.probe_string("email") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "email", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            id: match sub1.probe_number("id") {
                                NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                    Ok(n) => n,
                                    Err(_) => return Err(de_overflow("UserWithPosts", "id", "int", actual_wire_type, raw)),
                                },
                                NumProbe::Wrong { actual_wire_type, raw_value }
                                | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "id", "int", actual_wire_type, raw_value)),
                                NumProbe::Absent => return Err(de_missing_field("UserWithPosts", "id", "int")),
                            },
                            name: match sub1.probe_string("name") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            posts: match sub1.probe_list("posts") {
                                Probe::Got(l5) => {
                                    let mut acc5 = Vec::with_capacity(l5.len());
                                    for i5 in 0..l5.len() {
                                        acc5.push(match l5.elem_row(i5) {
                                            Probe::Got(sub6) => PostRow {
                                                author_id: match sub6.probe_number("author_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("PostRow", "author_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "author_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                                id: match sub6.probe_number("id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => n,
                                                        Err(_) => return Err(de_overflow("PostRow", "id", "int", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value }
                                                    | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "id", "int", actual_wire_type, raw_value)),
                                                    NumProbe::Absent => return Err(de_missing_field("PostRow", "id", "int")),
                                                },
                                                title: match sub6.probe_string("title") {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "posts", "obj{author_id:opt(int),id:int,title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UserWithPosts", "posts", "obj{author_id:opt(int),id:int,title:opt(string)}")),
                                        });
                                    }
                                    acc5
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "posts", "arr(obj{author_id:opt(int),id:int,title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UserWithPosts", "posts", "arr(obj{author_id:opt(int),id:int,title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}

// nestedFindFirst — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedFindFirst(name: String) -> Result<Vec<UserWithPosts>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<UserWithPosts>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![WireValue::Str(name.clone())];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str(
                    "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1"
                        .to_string(),
                ),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => l0.items.clone(),
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(value)",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match wire_n1.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n1",
                    "n1",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match wire_n2.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n2",
                    "n2",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["author_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("posts".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => UserWithPosts {
                            email: match sub1.probe_string("email") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "email", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            id: match sub1.probe_number("id") {
                                NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                    Ok(n) => n,
                                    Err(_) => return Err(de_overflow("UserWithPosts", "id", "int", actual_wire_type, raw)),
                                },
                                NumProbe::Wrong { actual_wire_type, raw_value }
                                | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "id", "int", actual_wire_type, raw_value)),
                                NumProbe::Absent => return Err(de_missing_field("UserWithPosts", "id", "int")),
                            },
                            name: match sub1.probe_string("name") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            posts: match sub1.probe_list("posts") {
                                Probe::Got(l5) => {
                                    let mut acc5 = Vec::with_capacity(l5.len());
                                    for i5 in 0..l5.len() {
                                        acc5.push(match l5.elem_row(i5) {
                                            Probe::Got(sub6) => PostRow {
                                                author_id: match sub6.probe_number("author_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("PostRow", "author_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "author_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                                id: match sub6.probe_number("id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => n,
                                                        Err(_) => return Err(de_overflow("PostRow", "id", "int", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value }
                                                    | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "id", "int", actual_wire_type, raw_value)),
                                                    NumProbe::Absent => return Err(de_missing_field("PostRow", "id", "int")),
                                                },
                                                title: match sub6.probe_string("title") {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "posts", "obj{author_id:opt(int),id:int,title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UserWithPosts", "posts", "obj{author_id:opt(int),id:int,title:opt(string)}")),
                                        });
                                    }
                                    acc5
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "posts", "arr(obj{author_id:opt(int),id:int,title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UserWithPosts", "posts", "arr(obj{author_id:opt(int),id:int,title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}

// nestedFindUnique — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedFindUnique(email: String) -> Result<Vec<UserWithPosts>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<UserWithPosts>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![WireValue::Str(email.clone())];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str(
                    "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1"
                        .to_string(),
                ),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => l0.items.clone(),
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(value)",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match wire_n1.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n1",
                    "n1",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match wire_n2.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n2",
                    "n2",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n3 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["author_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("posts".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n3 = match group_children(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => UserWithPosts {
                            email: match sub1.probe_string("email") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "email", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            id: match sub1.probe_number("id") {
                                NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                    Ok(n) => n,
                                    Err(_) => return Err(de_overflow("UserWithPosts", "id", "int", actual_wire_type, raw)),
                                },
                                NumProbe::Wrong { actual_wire_type, raw_value }
                                | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "id", "int", actual_wire_type, raw_value)),
                                NumProbe::Absent => return Err(de_missing_field("UserWithPosts", "id", "int")),
                            },
                            name: match sub1.probe_string("name") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            posts: match sub1.probe_list("posts") {
                                Probe::Got(l5) => {
                                    let mut acc5 = Vec::with_capacity(l5.len());
                                    for i5 in 0..l5.len() {
                                        acc5.push(match l5.elem_row(i5) {
                                            Probe::Got(sub6) => PostRow {
                                                author_id: match sub6.probe_number("author_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("PostRow", "author_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "author_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                                id: match sub6.probe_number("id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => n,
                                                        Err(_) => return Err(de_overflow("PostRow", "id", "int", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value }
                                                    | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "id", "int", actual_wire_type, raw_value)),
                                                    NumProbe::Absent => return Err(de_missing_field("PostRow", "id", "int")),
                                                },
                                                title: match sub6.probe_string("title") {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostRow", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "posts", "obj{author_id:opt(int),id:int,title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UserWithPosts", "posts", "obj{author_id:opt(int),id:int,title:opt(string)}")),
                                        });
                                    }
                                    acc5
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPosts", "posts", "arr(obj{author_id:opt(int),id:int,title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UserWithPosts", "posts", "arr(obj{author_id:opt(int),id:int,title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n3", "n3", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n3", "n3", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),id:int,title:opt(string)})})")),
        };
        produced_n3.set(true);
    }
    let __out = cell_n3.borrow().clone();
    Ok(__out)
}

// nestedRelations — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedRelations() -> Result<Vec<UserWithPostsAndComments>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    let cell_n4: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n4 = std::cell::Cell::new(false);
    let _ = &produced_n4;
    let cell_n5: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n5 = std::cell::Cell::new(false);
    let _ = &produced_n5;
    let cell_n6: RefCell<Vec<UserWithPostsAndComments>> = RefCell::new(Default::default());
    let produced_n6 = std::cell::Cell::new(false);
    let _ = &produced_n6;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> = vec![];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str("SELECT id, email, name FROM benchmark_users LIMIT 100".to_string()),
            ),
            ("write".to_string(), WireValue::Bool(false)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => l0.items.clone(),
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(value)",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match wire_n1.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n1",
                    "n1",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match wire_n2.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n2",
                    "n2",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (pluck, parent:n2) ──
    if produced_n2.get() {
        let payload_n3 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n3 = match pluck_keys(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n3",
                    "n3",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(value)")),
        };
        produced_n3.set(true);
    }
    // ── op 'n4' (executeSQL, parent:n3) ──
    if produced_n3.get() {
        let payload_n4 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n3.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT id, body, post_id FROM benchmark_comments WHERE post_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n4 = match execute_sql(payload_n4) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n4", "fail", e)),
        };
        *cell_n4.borrow_mut() = match wire_n4.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n4",
                    "n4",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n4", "n4", "arr(value)")),
        };
        produced_n4.set(true);
    }
    // ── op 'n5' (group, parent:n2) ──
    if produced_n2.get() {
        let payload_n5 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n4.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["post_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("comments".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n5 = match group_children(payload_n5) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n5", "fail", e)),
        };
        *cell_n5.borrow_mut() = match wire_n5.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n5",
                    "n5",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n5", "n5", "arr(value)")),
        };
        produced_n5.set(true);
    }
    // ── op 'n6' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n6 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n5.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["author_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("posts".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> = vec!["id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n6 = match group_children(payload_n6) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n6", "fail", e)),
        };
        *cell_n6.borrow_mut() = match wire_n6.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => UserWithPostsAndComments {
                            email: match sub1.probe_string("email") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPostsAndComments", "email", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            id: match sub1.probe_number("id") {
                                NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                    Ok(n) => n,
                                    Err(_) => return Err(de_overflow("UserWithPostsAndComments", "id", "int", actual_wire_type, raw)),
                                },
                                NumProbe::Wrong { actual_wire_type, raw_value }
                                | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPostsAndComments", "id", "int", actual_wire_type, raw_value)),
                                NumProbe::Absent => return Err(de_missing_field("UserWithPostsAndComments", "id", "int")),
                            },
                            name: match sub1.probe_string("name") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPostsAndComments", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            posts: match sub1.probe_list("posts") {
                                Probe::Got(l5) => {
                                    let mut acc5 = Vec::with_capacity(l5.len());
                                    for i5 in 0..l5.len() {
                                        acc5.push(match l5.elem_row(i5) {
                                            Probe::Got(sub6) => PostWithComments {
                                                author_id: match sub6.probe_number("author_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("PostWithComments", "author_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostWithComments", "author_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                                comments: match sub6.probe_list("comments") {
                                                    Probe::Got(l8) => {
                                                        let mut acc8 = Vec::with_capacity(l8.len());
                                                        for i8 in 0..l8.len() {
                                                            acc8.push(match l8.elem_row(i8) {
                                                                Probe::Got(sub9) => CommentRow {
                                                                    body: match sub9.probe_string("body") {
                                                                        Probe::Got(v) => Some(v),
                                                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CommentRow", "body", "opt(string)", actual_wire_type, raw_value)),
                                                                        Probe::Null { .. } | Probe::Absent => None,
                                                                    },
                                                                    id: match sub9.probe_number("id") {
                                                                        NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                                            Ok(n) => Some(n),
                                                                            Err(_) => return Err(de_overflow("CommentRow", "id", "opt(int)", actual_wire_type, raw)),
                                                                        },
                                                                        NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CommentRow", "id", "opt(int)", actual_wire_type, raw_value)),
                                                                        NumProbe::Null { .. } | NumProbe::Absent => None,
                                                                    },
                                                                    post_id: match sub9.probe_number("post_id") {
                                                                        NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                                            Ok(n) => Some(n),
                                                                            Err(_) => return Err(de_overflow("CommentRow", "post_id", "opt(int)", actual_wire_type, raw)),
                                                                        },
                                                                        NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("CommentRow", "post_id", "opt(int)", actual_wire_type, raw_value)),
                                                                        NumProbe::Null { .. } | NumProbe::Absent => None,
                                                                    },
                                                                },
                                                                Probe::Wrong { actual_wire_type, raw_value }
                                                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostWithComments", "comments", "obj{body:opt(string),id:opt(int),post_id:opt(int)}", actual_wire_type, raw_value)),
                                                                Probe::Absent => return Err(de_missing_field("PostWithComments", "comments", "obj{body:opt(string),id:opt(int),post_id:opt(int)}")),
                                                            });
                                                        }
                                                        acc8
                                                    },
                                                    Probe::Wrong { actual_wire_type, raw_value }
                                                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostWithComments", "comments", "arr(obj{body:opt(string),id:opt(int),post_id:opt(int)})", actual_wire_type, raw_value)),
                                                    Probe::Absent => return Err(de_missing_field("PostWithComments", "comments", "arr(obj{body:opt(string),id:opt(int),post_id:opt(int)})")),
                                                },
                                                id: match sub6.probe_number("id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => n,
                                                        Err(_) => return Err(de_overflow("PostWithComments", "id", "int", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value }
                                                    | NumProbe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostWithComments", "id", "int", actual_wire_type, raw_value)),
                                                    NumProbe::Absent => return Err(de_missing_field("PostWithComments", "id", "int")),
                                                },
                                                title: match sub6.probe_string("title") {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("PostWithComments", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPostsAndComments", "posts", "obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("UserWithPostsAndComments", "posts", "obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)}")),
                                        });
                                    }
                                    acc5
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("UserWithPostsAndComments", "posts", "arr(obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("UserWithPostsAndComments", "posts", "arr(obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)})")),
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)})}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n6", "n6", "obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)})}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)})})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n6", "n6", "arr(obj{email:opt(string),id:int,name:opt(string),posts:arr(obj{author_id:opt(int),comments:arr(obj{body:opt(string),id:opt(int),post_id:opt(int)}),id:int,title:opt(string)})})")),
        };
        produced_n6.set(true);
    }
    let __out = cell_n6.borrow().clone();
    Ok(__out)
}

// compositeRelations — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn compositeRelations() -> Result<Vec<TenantUserWithPosts>, BehaviorError> {
    let cell_n0: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    let cell_n2: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n2 = std::cell::Cell::new(false);
    let _ = &produced_n2;
    let cell_n3: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n3 = std::cell::Cell::new(false);
    let _ = &produced_n3;
    let cell_n4: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n4 = std::cell::Cell::new(false);
    let _ = &produced_n4;
    let cell_n5: RefCell<Vec<WireValue>> = RefCell::new(Default::default());
    let produced_n5 = std::cell::Cell::new(false);
    let _ = &produced_n5;
    let cell_n6: RefCell<Vec<TenantUserWithPosts>> = RefCell::new(Default::default());
    let produced_n6 = std::cell::Cell::new(false);
    let _ = &produced_n6;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT tenant_id, user_id, name FROM benchmark_tenant_users ORDER BY user_id ASC LIMIT 100".to_string())), ("write".to_string(), WireValue::Bool(false))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => l0.items.clone(),
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(value)",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(value)")),
    };
    produced_n0.set(true);
    // ── op 'n1' (pluck, parent:n0) ──
    if produced_n0.get() {
        let payload_n1 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> =
                                vec!["tenant_id".to_string(), "user_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n1 = match pluck_keys(payload_n1) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n1", "fail", e)),
        };
        *cell_n1.borrow_mut() = match wire_n1.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n1",
                    "n1",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n1", "n1", "arr(value)")),
        };
        produced_n1.set(true);
    }
    // ── op 'n2' (executeSQL, parent:n1) ──
    if produced_n1.get() {
        let payload_n2 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n1.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT tenant_id, post_id, user_id, title FROM benchmark_tenant_posts WHERE (benchmark_tenant_posts.tenant_id, benchmark_tenant_posts.user_id) IN (SELECT JSON_UNQUOTE(c0), JSON_UNQUOTE(c1) FROM JSON_TABLE(?, '$[*]' COLUMNS(c0 JSON PATH '$[0]', c1 JSON PATH '$[1]')) jt) ORDER BY post_id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n2 = match execute_sql(payload_n2) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n2", "fail", e)),
        };
        *cell_n2.borrow_mut() = match wire_n2.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n2",
                    "n2",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n2", "n2", "arr(value)")),
        };
        produced_n2.set(true);
    }
    // ── op 'n3' (pluck, parent:n2) ──
    if produced_n2.get() {
        let payload_n3 = WireRow {
            entries: vec![
                (
                    "col".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> =
                                vec!["tenant_id".to_string(), "post_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                (
                    "rows".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
            ],
        };
        let wire_n3 = match pluck_keys(payload_n3) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n3", "fail", e)),
        };
        *cell_n3.borrow_mut() = match wire_n3.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n3",
                    "n3",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n3", "n3", "arr(value)")),
        };
        produced_n3.set(true);
    }
    // ── op 'n4' (executeSQL, parent:n3) ──
    if produced_n3.get() {
        let payload_n4 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: cell_n3.borrow().clone() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("SELECT tenant_id, comment_id, post_id, body FROM benchmark_tenant_comments WHERE (benchmark_tenant_comments.tenant_id, benchmark_tenant_comments.post_id) IN (SELECT JSON_UNQUOTE(c0), JSON_UNQUOTE(c1) FROM JSON_TABLE(?, '$[*]' COLUMNS(c0 JSON PATH '$[0]', c1 JSON PATH '$[1]')) jt) ORDER BY comment_id ASC".to_string())), ("write".to_string(), WireValue::Bool(false))] };
        let wire_n4 = match execute_sql(payload_n4) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n4", "fail", e)),
        };
        *cell_n4.borrow_mut() = match wire_n4.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n4",
                    "n4",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n4", "n4", "arr(value)")),
        };
        produced_n4.set(true);
    }
    // ── op 'n5' (group, parent:n2) ──
    if produced_n2.get() {
        let payload_n5 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n4.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> =
                                vec!["tenant_id".to_string(), "post_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("comments".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n2.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> =
                                vec!["tenant_id".to_string(), "post_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n5 = match group_children(payload_n5) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n5", "fail", e)),
        };
        *cell_n5.borrow_mut() = match wire_n5.as_list() {
            Probe::Got(l0) => l0.items.clone(),
            Probe::Wrong {
                actual_wire_type,
                raw_value,
            }
            | Probe::Null {
                actual_wire_type,
                raw_value,
            } => {
                return Err(de_type_mismatch(
                    "n5",
                    "n5",
                    "arr(value)",
                    actual_wire_type,
                    raw_value,
                ))
            }
            Probe::Absent => return Err(de_missing_field("n5", "n5", "arr(value)")),
        };
        produced_n5.set(true);
    }
    // ── op 'n6' (group, parent:n0) ──
    if produced_n0.get() {
        let payload_n6 = WireRow {
            entries: vec![
                (
                    "children".to_string(),
                    WireValue::List(WireList {
                        items: cell_n5.borrow().clone(),
                    }),
                ),
                (
                    "fk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> =
                                vec!["tenant_id".to_string(), "user_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("into".to_string(), WireValue::Str("posts".to_string())),
                (
                    "parents".to_string(),
                    WireValue::List(WireList {
                        items: cell_n0.borrow().clone(),
                    }),
                ),
                (
                    "pk".to_string(),
                    WireValue::List(WireList {
                        items: {
                            let __v: Vec<String> =
                                vec!["tenant_id".to_string(), "user_id".to_string()];
                            __v
                        }
                        .into_iter()
                        .map(WireValue::Str)
                        .collect(),
                    }),
                ),
                ("single".to_string(), WireValue::Bool(false)),
            ],
        };
        let wire_n6 = match group_children(payload_n6) {
            Ok(r) => r,
            Err(e) => return Err(op_failed("n6", "fail", e)),
        };
        *cell_n6.borrow_mut() = match wire_n6.as_list() {
            Probe::Got(l0) => {
                let mut acc0 = Vec::with_capacity(l0.len());
                for i0 in 0..l0.len() {
                    acc0.push(match l0.elem_row(i0) {
                        Probe::Got(sub1) => TenantUserWithPosts {
                            name: match sub1.probe_string("name") {
                                Probe::Got(v) => Some(v),
                                Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantUserWithPosts", "name", "opt(string)", actual_wire_type, raw_value)),
                                Probe::Null { .. } | Probe::Absent => None,
                            },
                            posts: match sub1.probe_list("posts") {
                                Probe::Got(l3) => {
                                    let mut acc3 = Vec::with_capacity(l3.len());
                                    for i3 in 0..l3.len() {
                                        acc3.push(match l3.elem_row(i3) {
                                            Probe::Got(sub4) => TenantPostWithComments {
                                                comments: match sub4.probe_list("comments") {
                                                    Probe::Got(l5) => {
                                                        let mut acc5 = Vec::with_capacity(l5.len());
                                                        for i5 in 0..l5.len() {
                                                            acc5.push(match l5.elem_row(i5) {
                                                                Probe::Got(sub6) => TenantCommentRow {
                                                                    body: match sub6.probe_string("body") {
                                                                        Probe::Got(v) => Some(v),
                                                                        Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantCommentRow", "body", "opt(string)", actual_wire_type, raw_value)),
                                                                        Probe::Null { .. } | Probe::Absent => None,
                                                                    },
                                                                    comment_id: match sub6.probe_number("comment_id") {
                                                                        NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                                            Ok(n) => Some(n),
                                                                            Err(_) => return Err(de_overflow("TenantCommentRow", "comment_id", "opt(int)", actual_wire_type, raw)),
                                                                        },
                                                                        NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantCommentRow", "comment_id", "opt(int)", actual_wire_type, raw_value)),
                                                                        NumProbe::Null { .. } | NumProbe::Absent => None,
                                                                    },
                                                                    post_id: match sub6.probe_number("post_id") {
                                                                        NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                                            Ok(n) => Some(n),
                                                                            Err(_) => return Err(de_overflow("TenantCommentRow", "post_id", "opt(int)", actual_wire_type, raw)),
                                                                        },
                                                                        NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantCommentRow", "post_id", "opt(int)", actual_wire_type, raw_value)),
                                                                        NumProbe::Null { .. } | NumProbe::Absent => None,
                                                                    },
                                                                    tenant_id: match sub6.probe_number("tenant_id") {
                                                                        NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                                            Ok(n) => Some(n),
                                                                            Err(_) => return Err(de_overflow("TenantCommentRow", "tenant_id", "opt(int)", actual_wire_type, raw)),
                                                                        },
                                                                        NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantCommentRow", "tenant_id", "opt(int)", actual_wire_type, raw_value)),
                                                                        NumProbe::Null { .. } | NumProbe::Absent => None,
                                                                    },
                                                                },
                                                                Probe::Wrong { actual_wire_type, raw_value }
                                                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantPostWithComments", "comments", "obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}", actual_wire_type, raw_value)),
                                                                Probe::Absent => return Err(de_missing_field("TenantPostWithComments", "comments", "obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}")),
                                                            });
                                                        }
                                                        acc5
                                                    },
                                                    Probe::Wrong { actual_wire_type, raw_value }
                                                    | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantPostWithComments", "comments", "arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)})", actual_wire_type, raw_value)),
                                                    Probe::Absent => return Err(de_missing_field("TenantPostWithComments", "comments", "arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)})")),
                                                },
                                                post_id: match sub4.probe_number("post_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("TenantPostWithComments", "post_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantPostWithComments", "post_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                                tenant_id: match sub4.probe_number("tenant_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("TenantPostWithComments", "tenant_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantPostWithComments", "tenant_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                                title: match sub4.probe_string("title") {
                                                    Probe::Got(v) => Some(v),
                                                    Probe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantPostWithComments", "title", "opt(string)", actual_wire_type, raw_value)),
                                                    Probe::Null { .. } | Probe::Absent => None,
                                                },
                                                user_id: match sub4.probe_number("user_id") {
                                                    NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                                        Ok(n) => Some(n),
                                                        Err(_) => return Err(de_overflow("TenantPostWithComments", "user_id", "opt(int)", actual_wire_type, raw)),
                                                    },
                                                    NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantPostWithComments", "user_id", "opt(int)", actual_wire_type, raw_value)),
                                                    NumProbe::Null { .. } | NumProbe::Absent => None,
                                                },
                                            },
                                            Probe::Wrong { actual_wire_type, raw_value }
                                            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantUserWithPosts", "posts", "obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)}", actual_wire_type, raw_value)),
                                            Probe::Absent => return Err(de_missing_field("TenantUserWithPosts", "posts", "obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)}")),
                                        });
                                    }
                                    acc3
                                },
                                Probe::Wrong { actual_wire_type, raw_value }
                                | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantUserWithPosts", "posts", "arr(obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)})", actual_wire_type, raw_value)),
                                Probe::Absent => return Err(de_missing_field("TenantUserWithPosts", "posts", "arr(obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)})")),
                            },
                            tenant_id: match sub1.probe_number("tenant_id") {
                                NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                    Ok(n) => Some(n),
                                    Err(_) => return Err(de_overflow("TenantUserWithPosts", "tenant_id", "opt(int)", actual_wire_type, raw)),
                                },
                                NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantUserWithPosts", "tenant_id", "opt(int)", actual_wire_type, raw_value)),
                                NumProbe::Null { .. } | NumProbe::Absent => None,
                            },
                            user_id: match sub1.probe_number("user_id") {
                                NumProbe::Got { raw, actual_wire_type } => match raw.parse::<i64>() {
                                    Ok(n) => Some(n),
                                    Err(_) => return Err(de_overflow("TenantUserWithPosts", "user_id", "opt(int)", actual_wire_type, raw)),
                                },
                                NumProbe::Wrong { actual_wire_type, raw_value } => return Err(de_type_mismatch("TenantUserWithPosts", "user_id", "opt(int)", actual_wire_type, raw_value)),
                                NumProbe::Null { .. } | NumProbe::Absent => None,
                            },
                        },
                        Probe::Wrong { actual_wire_type, raw_value }
                        | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "obj{name:opt(string),posts:arr(obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)}),tenant_id:opt(int),user_id:opt(int)}", actual_wire_type, raw_value)),
                        Probe::Absent => return Err(de_missing_field("n6", "n6", "obj{name:opt(string),posts:arr(obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)}),tenant_id:opt(int),user_id:opt(int)}")),
                    });
                }
                acc0
            },
            Probe::Wrong { actual_wire_type, raw_value }
            | Probe::Null { actual_wire_type, raw_value } => return Err(de_type_mismatch("n6", "n6", "arr(obj{name:opt(string),posts:arr(obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)}),tenant_id:opt(int),user_id:opt(int)})", actual_wire_type, raw_value)),
            Probe::Absent => return Err(de_missing_field("n6", "n6", "arr(obj{name:opt(string),posts:arr(obj{comments:arr(obj{body:opt(string),comment_id:opt(int),post_id:opt(int),tenant_id:opt(int)}),post_id:opt(int),tenant_id:opt(int),title:opt(string),user_id:opt(int)}),tenant_id:opt(int),user_id:opt(int)})")),
        };
        produced_n6.set(true);
    }
    let __out = cell_n6.borrow().clone();
    Ok(__out)
}

// create — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn create(email: String, name: String) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> =
                            vec![WireValue::Str(email.clone()), WireValue::Str(name.clone())];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str(
                    "INSERT INTO benchmark_users (email, name) VALUES (?, ?)".to_string(),
                ),
            ),
            ("write".to_string(), WireValue::Bool(true)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => WriteSummary {
                        changes: match sub1.probe_number("changes") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "changes",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "changes",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("WriteSummary", "changes", "int"))
                            }
                        },
                        lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "lastInsertRowid",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                ))
                            }
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// update — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn update(id: i64, name: String) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow {
        entries: vec![
            ("bigint".to_string(), WireValue::Bool(false)),
            (
                "params".to_string(),
                WireValue::List(WireList {
                    items: {
                        let __v: Vec<WireValue> =
                            vec![WireValue::Str(name.clone()), WireValue::int(id)];
                        __v
                    },
                }),
            ),
            ("returning".to_string(), WireValue::Bool(false)),
            (
                "sql".to_string(),
                WireValue::Str("UPDATE benchmark_users SET name = ? WHERE id = ?".to_string()),
            ),
            ("write".to_string(), WireValue::Bool(true)),
        ],
    };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => WriteSummary {
                        changes: match sub1.probe_number("changes") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "changes",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "changes",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("WriteSummary", "changes", "int"))
                            }
                        },
                        lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "lastInsertRowid",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                ))
                            }
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// upsert — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn upsert(email: String, name: String) -> Result<Vec<IdRow>, BehaviorError> {
    let cell_n0: RefCell<Vec<IdRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(email.clone()), WireValue::Str(name.clone())]; __v } })), ("returning".to_string(), WireValue::Bool(true)), ("sql".to_string(), WireValue::Str("INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON DUPLICATE KEY UPDATE email = VALUES(email), name = VALUES(name) RETURNING id /*scp:pk=id;ai=id;conflict=email*/".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => IdRow {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "IdRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "IdRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => return Err(de_missing_field("IdRow", "id", "int")),
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{id:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:int}")),
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{id:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:int})")),
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// createMany — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn createMany(rows: Vec<NewUser>) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: rows.clone().into_iter().map(|e0| WireValue::Row(WireRow { entries: vec![("email".to_string(), WireValue::Str(e0.email)), ("name".to_string(), WireValue::Str(e0.name))] })).collect() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("INSERT INTO benchmark_users (email, name) SELECT JSON_UNQUOTE(jt.email), JSON_UNQUOTE(jt.name) FROM JSON_TABLE(?, '$[*]' COLUMNS(email JSON PATH '$.email', name JSON PATH '$.name')) jt".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => WriteSummary {
                        changes: match sub1.probe_number("changes") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "changes",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "changes",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("WriteSummary", "changes", "int"))
                            }
                        },
                        lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "lastInsertRowid",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                ))
                            }
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// upsertMany — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn upsertMany(rows: Vec<NewUser>) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: rows.clone().into_iter().map(|e0| WireValue::Row(WireRow { entries: vec![("email".to_string(), WireValue::Str(e0.email)), ("name".to_string(), WireValue::Str(e0.name))] })).collect() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("INSERT INTO benchmark_users (email, name) SELECT JSON_UNQUOTE(jt.email), JSON_UNQUOTE(jt.name) FROM JSON_TABLE(?, '$[*]' COLUMNS(email JSON PATH '$.email', name JSON PATH '$.name')) jt ON DUPLICATE KEY UPDATE email = VALUES(email), name = VALUES(name)".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => WriteSummary {
                        changes: match sub1.probe_number("changes") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "changes",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "changes",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("WriteSummary", "changes", "int"))
                            }
                        },
                        lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "lastInsertRowid",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                ))
                            }
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// updateMany — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn updateMany(rows: Vec<UserPatch>) -> Result<Vec<WriteSummary>, BehaviorError> {
    let cell_n0: RefCell<Vec<WriteSummary>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::List(WireList { items: rows.clone().into_iter().map(|e0| WireValue::Row(WireRow { entries: vec![("id".to_string(), WireValue::int(e0.id)), ("name".to_string(), WireValue::Str(e0.name))] })).collect() })]; __v } })), ("returning".to_string(), WireValue::Bool(false)), ("sql".to_string(), WireValue::Str("UPDATE benchmark_users AS u JOIN JSON_TABLE(?, '$[*]' COLUMNS(id JSON PATH '$.id', name JSON PATH '$.name')) AS v ON u.id = JSON_UNQUOTE(v.id) SET u.name = JSON_UNQUOTE(v.name)".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => WriteSummary {
                        changes: match sub1.probe_number("changes") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "changes",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "changes",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field("WriteSummary", "changes", "int"))
                            }
                        },
                        lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "WriteSummary",
                                        "lastInsertRowid",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => {
                                return Err(de_missing_field(
                                    "WriteSummary",
                                    "lastInsertRowid",
                                    "int",
                                ))
                            }
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => {
                        return Err(de_missing_field(
                            "n0",
                            "n0",
                            "obj{changes:int,lastInsertRowid:int}",
                        ))
                    }
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => {
            return Err(de_missing_field(
                "n0",
                "n0",
                "arr(obj{changes:int,lastInsertRowid:int})",
            ))
        }
    };
    produced_n0.set(true);
    let __out = cell_n0.borrow().clone();
    Ok(__out)
}

// nestedCreate — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedCreate(
    email: String,
    name: String,
    title: String,
) -> Result<Vec<Vec<WriteSummary>>, BehaviorError> {
    let cell_n0: RefCell<Vec<IdRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<Vec<WriteSummary>>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(email.clone()), WireValue::Str(name.clone())]; __v } })), ("returning".to_string(), WireValue::Bool(true)), ("sql".to_string(), WireValue::Str("INSERT INTO benchmark_users (email, name) VALUES (?, ?) RETURNING id /*scp:pk=id;ai=id*/".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => IdRow {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "IdRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "IdRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => return Err(de_missing_field("IdRow", "id", "int")),
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{id:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:int}")),
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{id:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:int})")),
    };
    produced_n0.set(true);
    // ── map 'n1' (executeSQL, per-element, into:null, parent:n0) ──
    if produced_n0.get() {
        let over_n1 = cell_n0.borrow().clone();
        let mut built_n1: Vec<Vec<WriteSummary>> = Vec::with_capacity(over_n1.len());
        for oel_n1 in over_n1.iter() {
            let ep_n1 = WireRow {
                entries: vec![
                    ("bigint".to_string(), WireValue::Bool(false)),
                    (
                        "params".to_string(),
                        WireValue::List(WireList {
                            items: {
                                let __v: Vec<WireValue> =
                                    vec![WireValue::int(oel_n1.id), WireValue::Str(title.clone())];
                                __v
                            },
                        }),
                    ),
                    ("returning".to_string(), WireValue::Bool(false)),
                    (
                        "sql".to_string(),
                        WireValue::Str(
                            "INSERT INTO benchmark_posts (author_id, title) VALUES (?, ?)"
                                .to_string(),
                        ),
                    ),
                    ("write".to_string(), WireValue::Bool(true)),
                ],
            };
            let er_n1 = match execute_sql(ep_n1) {
                Ok(r) => r,
                Err(e) => return Err(op_failed("n1", "fail", e)),
            };
            let dec_n1 = match er_n1.as_list() {
                Probe::Got(l0) => {
                    let mut acc0 = Vec::with_capacity(l0.len());
                    for i0 in 0..l0.len() {
                        acc0.push(match l0.elem_row(i0) {
                            Probe::Got(sub1) => WriteSummary {
                                changes: match sub1.probe_number("changes") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "changes",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                        ))
                                    }
                                },
                                lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "lastInsertRowid",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                        ))
                                    }
                                },
                            },
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | Probe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Absent => {
                                return Err(de_missing_field(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                ))
                            }
                        });
                    }
                    acc0
                }
                Probe::Wrong {
                    actual_wire_type,
                    raw_value,
                }
                | Probe::Null {
                    actual_wire_type,
                    raw_value,
                } => {
                    return Err(de_type_mismatch(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                        actual_wire_type,
                        raw_value,
                    ))
                }
                Probe::Absent => {
                    return Err(de_missing_field(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                    ))
                }
            };
            let _ = &oel_n1;
            built_n1.push(dec_n1);
        }
        *cell_n1.borrow_mut() = built_n1;
        produced_n1.set(true);
    }
    let __out = cell_n1.borrow().clone();
    Ok(__out)
}

// nestedUpsert — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedUpsert(
    email: String,
    name: String,
    title: String,
) -> Result<Vec<Vec<WriteSummary>>, BehaviorError> {
    let cell_n0: RefCell<Vec<IdRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<Vec<WriteSummary>>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(email.clone()), WireValue::Str(name.clone())]; __v } })), ("returning".to_string(), WireValue::Bool(true)), ("sql".to_string(), WireValue::Str("INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON DUPLICATE KEY UPDATE email = VALUES(email), name = VALUES(name) RETURNING id /*scp:pk=id;ai=id;conflict=email*/".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => IdRow {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "IdRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "IdRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => return Err(de_missing_field("IdRow", "id", "int")),
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{id:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:int}")),
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{id:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:int})")),
    };
    produced_n0.set(true);
    // ── map 'n1' (executeSQL, per-element, into:null, parent:n0) ──
    if produced_n0.get() {
        let over_n1 = cell_n0.borrow().clone();
        let mut built_n1: Vec<Vec<WriteSummary>> = Vec::with_capacity(over_n1.len());
        for oel_n1 in over_n1.iter() {
            let ep_n1 = WireRow {
                entries: vec![
                    ("bigint".to_string(), WireValue::Bool(false)),
                    (
                        "params".to_string(),
                        WireValue::List(WireList {
                            items: {
                                let __v: Vec<WireValue> =
                                    vec![WireValue::int(oel_n1.id), WireValue::Str(title.clone())];
                                __v
                            },
                        }),
                    ),
                    ("returning".to_string(), WireValue::Bool(false)),
                    (
                        "sql".to_string(),
                        WireValue::Str(
                            "INSERT INTO benchmark_posts (author_id, title) VALUES (?, ?)"
                                .to_string(),
                        ),
                    ),
                    ("write".to_string(), WireValue::Bool(true)),
                ],
            };
            let er_n1 = match execute_sql(ep_n1) {
                Ok(r) => r,
                Err(e) => return Err(op_failed("n1", "fail", e)),
            };
            let dec_n1 = match er_n1.as_list() {
                Probe::Got(l0) => {
                    let mut acc0 = Vec::with_capacity(l0.len());
                    for i0 in 0..l0.len() {
                        acc0.push(match l0.elem_row(i0) {
                            Probe::Got(sub1) => WriteSummary {
                                changes: match sub1.probe_number("changes") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "changes",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                        ))
                                    }
                                },
                                lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "lastInsertRowid",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                        ))
                                    }
                                },
                            },
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | Probe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Absent => {
                                return Err(de_missing_field(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                ))
                            }
                        });
                    }
                    acc0
                }
                Probe::Wrong {
                    actual_wire_type,
                    raw_value,
                }
                | Probe::Null {
                    actual_wire_type,
                    raw_value,
                } => {
                    return Err(de_type_mismatch(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                        actual_wire_type,
                        raw_value,
                    ))
                }
                Probe::Absent => {
                    return Err(de_missing_field(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                    ))
                }
            };
            let _ = &oel_n1;
            built_n1.push(dec_n1);
        }
        *cell_n1.borrow_mut() = built_n1;
        produced_n1.set(true);
    }
    let __out = cell_n1.borrow().clone();
    Ok(__out)
}

// nestedUpdate — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn nestedUpdate(
    id: i64,
    name: String,
    title: String,
) -> Result<Vec<Vec<WriteSummary>>, BehaviorError> {
    let cell_n0: RefCell<Vec<IdRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<Vec<WriteSummary>>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(name.clone()), WireValue::int(id)]; __v } })), ("returning".to_string(), WireValue::Bool(true)), ("sql".to_string(), WireValue::Str("UPDATE benchmark_users SET name = ? WHERE id = ? RETURNING id /*scp:pk=id;ai=id*/".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => IdRow {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "IdRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "IdRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => return Err(de_missing_field("IdRow", "id", "int")),
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{id:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:int}")),
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{id:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:int})")),
    };
    produced_n0.set(true);
    // ── map 'n1' (executeSQL, per-element, into:null, parent:n0) ──
    if produced_n0.get() {
        let over_n1 = cell_n0.borrow().clone();
        let mut built_n1: Vec<Vec<WriteSummary>> = Vec::with_capacity(over_n1.len());
        for oel_n1 in over_n1.iter() {
            let ep_n1 = WireRow {
                entries: vec![
                    ("bigint".to_string(), WireValue::Bool(false)),
                    (
                        "params".to_string(),
                        WireValue::List(WireList {
                            items: {
                                let __v: Vec<WireValue> =
                                    vec![WireValue::Str(title.clone()), WireValue::int(oel_n1.id)];
                                __v
                            },
                        }),
                    ),
                    ("returning".to_string(), WireValue::Bool(false)),
                    (
                        "sql".to_string(),
                        WireValue::Str(
                            "UPDATE benchmark_posts SET title = ? WHERE author_id = ?".to_string(),
                        ),
                    ),
                    ("write".to_string(), WireValue::Bool(true)),
                ],
            };
            let er_n1 = match execute_sql(ep_n1) {
                Ok(r) => r,
                Err(e) => return Err(op_failed("n1", "fail", e)),
            };
            let dec_n1 = match er_n1.as_list() {
                Probe::Got(l0) => {
                    let mut acc0 = Vec::with_capacity(l0.len());
                    for i0 in 0..l0.len() {
                        acc0.push(match l0.elem_row(i0) {
                            Probe::Got(sub1) => WriteSummary {
                                changes: match sub1.probe_number("changes") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "changes",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                        ))
                                    }
                                },
                                lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "lastInsertRowid",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                        ))
                                    }
                                },
                            },
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | Probe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Absent => {
                                return Err(de_missing_field(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                ))
                            }
                        });
                    }
                    acc0
                }
                Probe::Wrong {
                    actual_wire_type,
                    raw_value,
                }
                | Probe::Null {
                    actual_wire_type,
                    raw_value,
                } => {
                    return Err(de_type_mismatch(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                        actual_wire_type,
                        raw_value,
                    ))
                }
                Probe::Absent => {
                    return Err(de_missing_field(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                    ))
                }
            };
            let _ = &oel_n1;
            built_n1.push(dec_n1);
        }
        *cell_n1.borrow_mut() = built_n1;
        produced_n1.set(true);
    }
    let __out = cell_n1.borrow().clone();
    Ok(__out)
}

// delete — the STRUCT-RETURNING combined read (bc#77/#87/#94): the fully
// de-plumbed CONCRETE path. At each covered node's execution point it calls the op-agnostic leaf
// TRANSPORT symbol DIRECTLY (the node's port fields spread — no per-node handler indirection) and
// de-boxes the returned wire INLINE (match wire.as_*() { … } fully unrolled — no decode helper) into
// the node's outType struct cell — no boxed handler result, no generic enum crossing, no dispatch on a dynamic value on the covered plane. Node
// results are typed struct cells; a relation child reads the parent's REAL struct result via
// direct field access (child-present decision from the real parent value — relation /
// connection converge). A real-concurrency stage (bc#87) is static parallel orchestration —
// scoped worker threads (bounded by the static plan.concurrency) call the transport; preflight +
// interpret are committed in ascending index order so the value / op multiset / failure
// precedence byte-match run_behavior. The output is a typed struct/value assembled by struct
// literal + field access — the consumer keeps it native.
pub fn delete(email: String, name: String) -> Result<Vec<Vec<WriteSummary>>, BehaviorError> {
    let cell_n0: RefCell<Vec<IdRow>> = RefCell::new(Default::default());
    let produced_n0 = std::cell::Cell::new(false);
    let _ = &produced_n0;
    let cell_n1: RefCell<Vec<Vec<WriteSummary>>> = RefCell::new(Default::default());
    let produced_n1 = std::cell::Cell::new(false);
    let _ = &produced_n1;
    // ── op 'n0' (executeSQL) ──
    let payload_n0 = WireRow { entries: vec![("bigint".to_string(), WireValue::Bool(false)), ("params".to_string(), WireValue::List(WireList { items: { let __v: Vec<WireValue> = vec![WireValue::Str(email.clone()), WireValue::Str(name.clone())]; __v } })), ("returning".to_string(), WireValue::Bool(true)), ("sql".to_string(), WireValue::Str("INSERT INTO benchmark_users (email, name) VALUES (?, ?) RETURNING id /*scp:pk=id;ai=id*/".to_string())), ("write".to_string(), WireValue::Bool(true))] };
    let wire_n0 = match execute_sql(payload_n0) {
        Ok(r) => r,
        Err(e) => return Err(op_failed("n0", "fail", e)),
    };
    *cell_n0.borrow_mut() = match wire_n0.as_list() {
        Probe::Got(l0) => {
            let mut acc0 = Vec::with_capacity(l0.len());
            for i0 in 0..l0.len() {
                acc0.push(match l0.elem_row(i0) {
                    Probe::Got(sub1) => IdRow {
                        id: match sub1.probe_number("id") {
                            NumProbe::Got {
                                raw,
                                actual_wire_type,
                            } => match raw.parse::<i64>() {
                                Ok(n) => n,
                                Err(_) => {
                                    return Err(de_overflow(
                                        "IdRow",
                                        "id",
                                        "int",
                                        actual_wire_type,
                                        raw,
                                    ))
                                }
                            },
                            NumProbe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | NumProbe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "IdRow",
                                    "id",
                                    "int",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            NumProbe::Absent => return Err(de_missing_field("IdRow", "id", "int")),
                        },
                    },
                    Probe::Wrong {
                        actual_wire_type,
                        raw_value,
                    }
                    | Probe::Null {
                        actual_wire_type,
                        raw_value,
                    } => {
                        return Err(de_type_mismatch(
                            "n0",
                            "n0",
                            "obj{id:int}",
                            actual_wire_type,
                            raw_value,
                        ))
                    }
                    Probe::Absent => return Err(de_missing_field("n0", "n0", "obj{id:int}")),
                });
            }
            acc0
        }
        Probe::Wrong {
            actual_wire_type,
            raw_value,
        }
        | Probe::Null {
            actual_wire_type,
            raw_value,
        } => {
            return Err(de_type_mismatch(
                "n0",
                "n0",
                "arr(obj{id:int})",
                actual_wire_type,
                raw_value,
            ))
        }
        Probe::Absent => return Err(de_missing_field("n0", "n0", "arr(obj{id:int})")),
    };
    produced_n0.set(true);
    // ── map 'n1' (executeSQL, per-element, into:null, parent:n0) ──
    if produced_n0.get() {
        let over_n1 = cell_n0.borrow().clone();
        let mut built_n1: Vec<Vec<WriteSummary>> = Vec::with_capacity(over_n1.len());
        for oel_n1 in over_n1.iter() {
            let ep_n1 = WireRow {
                entries: vec![
                    ("bigint".to_string(), WireValue::Bool(false)),
                    (
                        "params".to_string(),
                        WireValue::List(WireList {
                            items: {
                                let __v: Vec<WireValue> = vec![WireValue::int(oel_n1.id)];
                                __v
                            },
                        }),
                    ),
                    ("returning".to_string(), WireValue::Bool(false)),
                    (
                        "sql".to_string(),
                        WireValue::Str("DELETE FROM benchmark_users WHERE id = ?".to_string()),
                    ),
                    ("write".to_string(), WireValue::Bool(true)),
                ],
            };
            let er_n1 = match execute_sql(ep_n1) {
                Ok(r) => r,
                Err(e) => return Err(op_failed("n1", "fail", e)),
            };
            let dec_n1 = match er_n1.as_list() {
                Probe::Got(l0) => {
                    let mut acc0 = Vec::with_capacity(l0.len());
                    for i0 in 0..l0.len() {
                        acc0.push(match l0.elem_row(i0) {
                            Probe::Got(sub1) => WriteSummary {
                                changes: match sub1.probe_number("changes") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "changes",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "changes",
                                            "int",
                                        ))
                                    }
                                },
                                lastInsertRowid: match sub1.probe_number("lastInsertRowid") {
                                    NumProbe::Got {
                                        raw,
                                        actual_wire_type,
                                    } => match raw.parse::<i64>() {
                                        Ok(n) => n,
                                        Err(_) => {
                                            return Err(de_overflow(
                                                "WriteSummary",
                                                "lastInsertRowid",
                                                "int",
                                                actual_wire_type,
                                                raw,
                                            ))
                                        }
                                    },
                                    NumProbe::Wrong {
                                        actual_wire_type,
                                        raw_value,
                                    }
                                    | NumProbe::Null {
                                        actual_wire_type,
                                        raw_value,
                                    } => {
                                        return Err(de_type_mismatch(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                            actual_wire_type,
                                            raw_value,
                                        ))
                                    }
                                    NumProbe::Absent => {
                                        return Err(de_missing_field(
                                            "WriteSummary",
                                            "lastInsertRowid",
                                            "int",
                                        ))
                                    }
                                },
                            },
                            Probe::Wrong {
                                actual_wire_type,
                                raw_value,
                            }
                            | Probe::Null {
                                actual_wire_type,
                                raw_value,
                            } => {
                                return Err(de_type_mismatch(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                    actual_wire_type,
                                    raw_value,
                                ))
                            }
                            Probe::Absent => {
                                return Err(de_missing_field(
                                    "n1",
                                    "n1",
                                    "obj{changes:int,lastInsertRowid:int}",
                                ))
                            }
                        });
                    }
                    acc0
                }
                Probe::Wrong {
                    actual_wire_type,
                    raw_value,
                }
                | Probe::Null {
                    actual_wire_type,
                    raw_value,
                } => {
                    return Err(de_type_mismatch(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                        actual_wire_type,
                        raw_value,
                    ))
                }
                Probe::Absent => {
                    return Err(de_missing_field(
                        "n1",
                        "n1",
                        "arr(obj{changes:int,lastInsertRowid:int})",
                    ))
                }
            };
            let _ = &oel_n1;
            built_n1.push(dec_n1);
        }
        *cell_n1.borrow_mut() = built_n1;
        produced_n1.set(true);
    }
    let __out = cell_n1.borrow().clone();
    Ok(__out)
}

// COMPONENT_NAMES_NATIVE_RAW — covered reads exposed on the combined struct-native path. Each is a
// 1:1 entry <method>(<positional params>) -> Result<T, BehaviorError>: a STRUCT return (the consumer calls
// it with the authored args + supplies the op-agnostic leaf transport symbols the entry calls).
// See INTEGRATION.md §6.
pub const COMPONENT_NAMES_NATIVE_RAW: [&str; 19] = [
    "findAll",
    "filterPaginateSort",
    "findFirst",
    "findUnique",
    "nestedFindAll",
    "nestedFindFirst",
    "nestedFindUnique",
    "nestedRelations",
    "compositeRelations",
    "create",
    "update",
    "upsert",
    "createMany",
    "upsertMany",
    "updateMany",
    "nestedCreate",
    "nestedUpsert",
    "nestedUpdate",
    "delete",
];
