// GENERATED by behavior-contracts common generator (bc#37/#42/#75, straight-line) — DO NOT EDIT.
// Input: portable component-graph IR only (scp-ir-architecture.md §5/§7.4).
// This is the STRAIGHT-LINE codegen endpoint (typed-codegen.md §4.1): each component is
// emitted as STATIC native code (bc#75). Strictly-sequential plans compile to plain
// statements in dependency order — no RunPlan, no ops table, no per-op ID search, no
// per-expression scope snapshots. Statically-wired refs read locals directly; static
// concat is a flat slCat(...) over already-evaluated values. Only plans with REAL bounded
// concurrency (multi-op stages) drive the runPlan/runPlanAsync primitive (semantics SSoT),
// and there the per-op exec is a pre-resolved function table. Fail-closed operator
// semantics (overflow arithmetic, strict-bool short-circuit and/or/cond/coalesce, obj/arr
// construction, checked literals) still route through the A0 expression-operator
// primitives (#36) — the semantics SSoT stays single-sourced. Handlers are resolved ONCE
// at bind() time and invoked through the resolved reference (UNKNOWN_COMPONENT stays
// fail-closed at op execution). Handlers are ALWAYS injected at the boundary; they are
// never generated. The portable IR is NOT embedded: this module carries only
// EXPECTED_SPEC_VERSIONS + COMPONENT_NAMES. The IR fingerprint is a BUILD-TIME return on
// GenerateResult.fingerprint (NOT baked into the module, 0.5.0); the fail-closed skew gate
// lives on the consumer/build side (compare it against the fingerprint of the live IR).
import { SPEC_VERSIONS, BehaviorFailure, PlanFailure, codegenPrimitives as cgp, conformResultToOutType } from "behavior-contracts/runtime";
/** Spec versions baked at generation time (fail-closed constant comparison at load). */
export const EXPECTED_SPEC_VERSIONS = { behavior: 5, expression: 2, plan: 1 };
/** Component names exposed by bind(), in IR declaration order. */
export const COMPONENT_NAMES = ["findAll", "filterPaginateSort", "findFirst", "findUnique", "nestedFindAll", "nestedFindFirst", "nestedFindUnique", "nestedRelations", "compositeRelations", "create", "update", "upsert", "createMany", "upsertMany", "updateMany", "nestedCreate", "nestedUpsert", "nestedUpdate", "delete"];
// ── load-time fail-closed checks (#208 prepared-artifact discipline, checked ONCE).
// IR-independent checks only (spec-version envelope pin) — no embedded IR to self-check. ──
for (const [k, want] of Object.entries(EXPECTED_SPEC_VERSIONS)) {
    const got = SPEC_VERSIONS[k];
    if (got !== want)
        throw new Error(`behavior-contracts generated module: spec-version skew for '${k}' (generated=${want}, runtime=${got}) — regenerate against this runtime (fail-closed)`);
}
// ── straight-line component functions (no runBehavior tree-walk) ───────────────────
function run_findAll(h$executeSQL, input) {
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users ORDER BY id ASC LIMIT 100",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "UserRow", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" } } } }, false);
    return r_n0;
}
async function run_findAll_async(h$executeSQL, input) {
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users ORDER BY id ASC LIMIT 100",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "UserRow", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" } } } }, false);
    return r_n0;
}
function run_filterPaginateSort(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["published"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, content, published, author_id, created_at FROM benchmark_posts WHERE published = ? ORDER BY created_at DESC LIMIT 20 OFFSET 10",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "PostFullRow", "obj": { "author_id": { "opt": "int" }, "content": { "opt": "string" }, "created_at": { "opt": "string" }, "id": "int", "published": { "opt": "int" }, "title": { "opt": "string" } } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_filterPaginateSort_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["published"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, content, published, author_id, created_at FROM benchmark_posts WHERE published = ? ORDER BY created_at DESC LIMIT 20 OFFSET 10",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "PostFullRow", "obj": { "author_id": { "opt": "int" }, "content": { "opt": "string" }, "created_at": { "opt": "string" }, "id": "int", "published": { "opt": "int" }, "title": { "opt": "string" } } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_findFirst(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "UserRow", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" } } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_findFirst_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "UserRow", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" } } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_findUnique(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "UserRow", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" } } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_findUnique_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "UserRow", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" } } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_nestedFindAll(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users LIMIT 100",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n3 = {
        "children": r_n2,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n3 = h$group(ports_n3, { nodeId: "n3", component: "group" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": { "name": "UserWithPosts", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostRow", "obj": { "author_id": { "opt": "int" }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n3"] = r_n3;
    return r_n3;
}
async function run_nestedFindAll_async(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users LIMIT 100",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = await h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = await h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n3 = {
        "children": r_n2,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n3 = await h$group(ports_n3, { nodeId: "n3", component: "group" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": { "name": "UserWithPosts", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostRow", "obj": { "author_id": { "opt": "int" }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n3"] = r_n3;
    return r_n3;
}
function run_nestedFindFirst(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n3 = {
        "children": r_n2,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n3 = h$group(ports_n3, { nodeId: "n3", component: "group" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": { "name": "UserWithPosts", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostRow", "obj": { "author_id": { "opt": "int" }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n3"] = r_n3;
    return r_n3;
}
async function run_nestedFindFirst_async(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE name LIKE ? LIMIT 1",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = await h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = await h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n3 = {
        "children": r_n2,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n3 = await h$group(ports_n3, { nodeId: "n3", component: "group" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": { "name": "UserWithPosts", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostRow", "obj": { "author_id": { "opt": "int" }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n3"] = r_n3;
    return r_n3;
}
function run_nestedFindUnique(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n3 = {
        "children": r_n2,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n3 = h$group(ports_n3, { nodeId: "n3", component: "group" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": { "name": "UserWithPosts", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostRow", "obj": { "author_id": { "opt": "int" }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n3"] = r_n3;
    return r_n3;
}
async function run_nestedFindUnique_async(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope)]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users WHERE email = ? LIMIT 1",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = await h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = await h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n3 = {
        "children": r_n2,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n3 = await h$group(ports_n3, { nodeId: "n3", component: "group" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": { "name": "UserWithPosts", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostRow", "obj": { "author_id": { "opt": "int" }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n3"] = r_n3;
    return r_n3;
}
function run_nestedRelations(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users LIMIT 100",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n3 = {
        "col": cgp.arr(["id"]),
        "rows": r_n2,
    };
    const o_n3 = h$pluck(ports_n3, { nodeId: "n3", component: "pluck" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": "value" }, false, true);
    scope["n3"] = r_n3;
    // ── op 'n4' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n4 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n3"], scope)]),
        "returning": false,
        "sql": "SELECT id, body, post_id FROM benchmark_comments WHERE post_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n4 = h$executeSQL(ports_n4, { nodeId: "n4", component: "executeSQL" });
    if ("error" in o_n4)
        throw new PlanFailure("OP_FAILED", `operation 'n4' failed under 'fail' policy: ${o_n4.error}`);
    const r_n4 = conformResultToOutType("n4", o_n4.ok, { "arr": "value" }, false, true);
    scope["n4"] = r_n4;
    // ── op 'n5' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n5 = {
        "children": r_n4,
        "fk": cgp.arr(["post_id"]),
        "into": "comments",
        "parents": r_n2,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n5 = h$group(ports_n5, { nodeId: "n5", component: "group" });
    if ("error" in o_n5)
        throw new PlanFailure("OP_FAILED", `operation 'n5' failed under 'fail' policy: ${o_n5.error}`);
    const r_n5 = conformResultToOutType("n5", o_n5.ok, { "arr": "value" }, false, true);
    scope["n5"] = r_n5;
    // ── op 'n6' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n6 = {
        "children": r_n5,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n6 = h$group(ports_n6, { nodeId: "n6", component: "group" });
    if ("error" in o_n6)
        throw new PlanFailure("OP_FAILED", `operation 'n6' failed under 'fail' policy: ${o_n6.error}`);
    const r_n6 = conformResultToOutType("n6", o_n6.ok, { "arr": { "name": "UserWithPostsAndComments", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostWithComments", "obj": { "author_id": { "opt": "int" }, "comments": { "arr": { "name": "CommentRow", "obj": { "body": { "opt": "string" }, "id": { "opt": "int" }, "post_id": { "opt": "int" } } } }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n6"] = r_n6;
    return r_n6;
}
async function run_nestedRelations_async(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT id, email, name FROM benchmark_users LIMIT 100",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["id"]),
        "rows": r_n0,
    };
    const o_n1 = await h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT id, title, author_id FROM benchmark_posts WHERE author_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n2 = await h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n3 = {
        "col": cgp.arr(["id"]),
        "rows": r_n2,
    };
    const o_n3 = await h$pluck(ports_n3, { nodeId: "n3", component: "pluck" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": "value" }, false, true);
    scope["n3"] = r_n3;
    // ── op 'n4' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n4 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n3"], scope)]),
        "returning": false,
        "sql": "SELECT id, body, post_id FROM benchmark_comments WHERE post_id IN (SELECT value FROM json_each(?)) ORDER BY id ASC",
        "write": false,
    };
    const o_n4 = await h$executeSQL(ports_n4, { nodeId: "n4", component: "executeSQL" });
    if ("error" in o_n4)
        throw new PlanFailure("OP_FAILED", `operation 'n4' failed under 'fail' policy: ${o_n4.error}`);
    const r_n4 = conformResultToOutType("n4", o_n4.ok, { "arr": "value" }, false, true);
    scope["n4"] = r_n4;
    // ── op 'n5' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n5 = {
        "children": r_n4,
        "fk": cgp.arr(["post_id"]),
        "into": "comments",
        "parents": r_n2,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n5 = await h$group(ports_n5, { nodeId: "n5", component: "group" });
    if ("error" in o_n5)
        throw new PlanFailure("OP_FAILED", `operation 'n5' failed under 'fail' policy: ${o_n5.error}`);
    const r_n5 = conformResultToOutType("n5", o_n5.ok, { "arr": "value" }, false, true);
    scope["n5"] = r_n5;
    // ── op 'n6' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n6 = {
        "children": r_n5,
        "fk": cgp.arr(["author_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["id"]),
        "single": false,
    };
    const o_n6 = await h$group(ports_n6, { nodeId: "n6", component: "group" });
    if ("error" in o_n6)
        throw new PlanFailure("OP_FAILED", `operation 'n6' failed under 'fail' policy: ${o_n6.error}`);
    const r_n6 = conformResultToOutType("n6", o_n6.ok, { "arr": { "name": "UserWithPostsAndComments", "obj": { "email": { "opt": "string" }, "id": "int", "name": { "opt": "string" }, "posts": { "arr": { "name": "PostWithComments", "obj": { "author_id": { "opt": "int" }, "comments": { "arr": { "name": "CommentRow", "obj": { "body": { "opt": "string" }, "id": { "opt": "int" }, "post_id": { "opt": "int" } } } }, "id": "int", "title": { "opt": "string" } } } } } } }, false);
    scope["n6"] = r_n6;
    return r_n6;
}
function run_compositeRelations(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT tenant_id, user_id, name FROM benchmark_tenant_users ORDER BY user_id ASC LIMIT 100",
        "write": false,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["tenant_id", "user_id"]),
        "rows": r_n0,
    };
    const o_n1 = h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT tenant_id, post_id, user_id, title FROM benchmark_tenant_posts WHERE EXISTS (SELECT 1 FROM json_each(?) je WHERE json_extract(je.value, '$[0]') = benchmark_tenant_posts.tenant_id AND json_extract(je.value, '$[1]') = benchmark_tenant_posts.user_id) ORDER BY post_id ASC",
        "write": false,
    };
    const o_n2 = h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n3 = {
        "col": cgp.arr(["tenant_id", "post_id"]),
        "rows": r_n2,
    };
    const o_n3 = h$pluck(ports_n3, { nodeId: "n3", component: "pluck" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": "value" }, false, true);
    scope["n3"] = r_n3;
    // ── op 'n4' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n4 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n3"], scope)]),
        "returning": false,
        "sql": "SELECT tenant_id, comment_id, post_id, body FROM benchmark_tenant_comments WHERE EXISTS (SELECT 1 FROM json_each(?) je WHERE json_extract(je.value, '$[0]') = benchmark_tenant_comments.tenant_id AND json_extract(je.value, '$[1]') = benchmark_tenant_comments.post_id) ORDER BY comment_id ASC",
        "write": false,
    };
    const o_n4 = h$executeSQL(ports_n4, { nodeId: "n4", component: "executeSQL" });
    if ("error" in o_n4)
        throw new PlanFailure("OP_FAILED", `operation 'n4' failed under 'fail' policy: ${o_n4.error}`);
    const r_n4 = conformResultToOutType("n4", o_n4.ok, { "arr": "value" }, false, true);
    scope["n4"] = r_n4;
    // ── op 'n5' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n5 = {
        "children": r_n4,
        "fk": cgp.arr(["tenant_id", "post_id"]),
        "into": "comments",
        "parents": r_n2,
        "pk": cgp.arr(["tenant_id", "post_id"]),
        "single": false,
    };
    const o_n5 = h$group(ports_n5, { nodeId: "n5", component: "group" });
    if ("error" in o_n5)
        throw new PlanFailure("OP_FAILED", `operation 'n5' failed under 'fail' policy: ${o_n5.error}`);
    const r_n5 = conformResultToOutType("n5", o_n5.ok, { "arr": "value" }, false, true);
    scope["n5"] = r_n5;
    // ── op 'n6' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n6 = {
        "children": r_n5,
        "fk": cgp.arr(["tenant_id", "user_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["tenant_id", "user_id"]),
        "single": false,
    };
    const o_n6 = h$group(ports_n6, { nodeId: "n6", component: "group" });
    if ("error" in o_n6)
        throw new PlanFailure("OP_FAILED", `operation 'n6' failed under 'fail' policy: ${o_n6.error}`);
    const r_n6 = conformResultToOutType("n6", o_n6.ok, { "arr": { "name": "TenantUserWithPosts", "obj": { "name": { "opt": "string" }, "posts": { "arr": { "name": "TenantPostWithComments", "obj": { "comments": { "arr": { "name": "TenantCommentRow", "obj": { "body": { "opt": "string" }, "comment_id": { "opt": "int" }, "post_id": { "opt": "int" }, "tenant_id": { "opt": "int" } } } }, "post_id": { "opt": "int" }, "tenant_id": { "opt": "int" }, "title": { "opt": "string" }, "user_id": { "opt": "int" } } } }, "tenant_id": { "opt": "int" }, "user_id": { "opt": "int" } } } }, false);
    scope["n6"] = r_n6;
    return r_n6;
}
async function run_compositeRelations_async(h$executeSQL, h$pluck, h$group, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([]),
        "returning": false,
        "sql": "SELECT tenant_id, user_id, name FROM benchmark_tenant_users ORDER BY user_id ASC LIMIT 100",
        "write": false,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": "value" }, false, true);
    scope["n0"] = r_n0;
    // ── op 'n1' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n1 = {
        "col": cgp.arr(["tenant_id", "user_id"]),
        "rows": r_n0,
    };
    const o_n1 = await h$pluck(ports_n1, { nodeId: "n1", component: "pluck" });
    if ("error" in o_n1)
        throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${o_n1.error}`);
    const r_n1 = conformResultToOutType("n1", o_n1.ok, { "arr": "value" }, false, true);
    scope["n1"] = r_n1;
    // ── op 'n2' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n2 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n1"], scope)]),
        "returning": false,
        "sql": "SELECT tenant_id, post_id, user_id, title FROM benchmark_tenant_posts WHERE EXISTS (SELECT 1 FROM json_each(?) je WHERE json_extract(je.value, '$[0]') = benchmark_tenant_posts.tenant_id AND json_extract(je.value, '$[1]') = benchmark_tenant_posts.user_id) ORDER BY post_id ASC",
        "write": false,
    };
    const o_n2 = await h$executeSQL(ports_n2, { nodeId: "n2", component: "executeSQL" });
    if ("error" in o_n2)
        throw new PlanFailure("OP_FAILED", `operation 'n2' failed under 'fail' policy: ${o_n2.error}`);
    const r_n2 = conformResultToOutType("n2", o_n2.ok, { "arr": "value" }, false, true);
    scope["n2"] = r_n2;
    // ── op 'n3' (pluck) ──
    if (!h$pluck)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'pluck' has no handler (fail-closed)");
    const ports_n3 = {
        "col": cgp.arr(["tenant_id", "post_id"]),
        "rows": r_n2,
    };
    const o_n3 = await h$pluck(ports_n3, { nodeId: "n3", component: "pluck" });
    if ("error" in o_n3)
        throw new PlanFailure("OP_FAILED", `operation 'n3' failed under 'fail' policy: ${o_n3.error}`);
    const r_n3 = conformResultToOutType("n3", o_n3.ok, { "arr": "value" }, false, true);
    scope["n3"] = r_n3;
    // ── op 'n4' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n4 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["n3"], scope)]),
        "returning": false,
        "sql": "SELECT tenant_id, comment_id, post_id, body FROM benchmark_tenant_comments WHERE EXISTS (SELECT 1 FROM json_each(?) je WHERE json_extract(je.value, '$[0]') = benchmark_tenant_comments.tenant_id AND json_extract(je.value, '$[1]') = benchmark_tenant_comments.post_id) ORDER BY comment_id ASC",
        "write": false,
    };
    const o_n4 = await h$executeSQL(ports_n4, { nodeId: "n4", component: "executeSQL" });
    if ("error" in o_n4)
        throw new PlanFailure("OP_FAILED", `operation 'n4' failed under 'fail' policy: ${o_n4.error}`);
    const r_n4 = conformResultToOutType("n4", o_n4.ok, { "arr": "value" }, false, true);
    scope["n4"] = r_n4;
    // ── op 'n5' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n5 = {
        "children": r_n4,
        "fk": cgp.arr(["tenant_id", "post_id"]),
        "into": "comments",
        "parents": r_n2,
        "pk": cgp.arr(["tenant_id", "post_id"]),
        "single": false,
    };
    const o_n5 = await h$group(ports_n5, { nodeId: "n5", component: "group" });
    if ("error" in o_n5)
        throw new PlanFailure("OP_FAILED", `operation 'n5' failed under 'fail' policy: ${o_n5.error}`);
    const r_n5 = conformResultToOutType("n5", o_n5.ok, { "arr": "value" }, false, true);
    scope["n5"] = r_n5;
    // ── op 'n6' (group) ──
    if (!h$group)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'group' has no handler (fail-closed)");
    const ports_n6 = {
        "children": r_n5,
        "fk": cgp.arr(["tenant_id", "user_id"]),
        "into": "posts",
        "parents": r_n0,
        "pk": cgp.arr(["tenant_id", "user_id"]),
        "single": false,
    };
    const o_n6 = await h$group(ports_n6, { nodeId: "n6", component: "group" });
    if ("error" in o_n6)
        throw new PlanFailure("OP_FAILED", `operation 'n6' failed under 'fail' policy: ${o_n6.error}`);
    const r_n6 = conformResultToOutType("n6", o_n6.ok, { "arr": { "name": "TenantUserWithPosts", "obj": { "name": { "opt": "string" }, "posts": { "arr": { "name": "TenantPostWithComments", "obj": { "comments": { "arr": { "name": "TenantCommentRow", "obj": { "body": { "opt": "string" }, "comment_id": { "opt": "int" }, "post_id": { "opt": "int" }, "tenant_id": { "opt": "int" } } } }, "post_id": { "opt": "int" }, "tenant_id": { "opt": "int" }, "title": { "opt": "string" }, "user_id": { "opt": "int" } } } }, "tenant_id": { "opt": "int" }, "user_id": { "opt": "int" } } } }, false);
    scope["n6"] = r_n6;
    return r_n6;
}
function run_create(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": false,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?)",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_create_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": false,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?)",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_update(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope), cgp.ref(["id"], scope)]),
        "returning": false,
        "sql": "UPDATE benchmark_users SET name = ? WHERE id = ?",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_update_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope), cgp.ref(["id"], scope)]),
        "returning": false,
        "sql": "UPDATE benchmark_users SET name = ? WHERE id = ?",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_upsert(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name RETURNING id",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_upsert_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name RETURNING id",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_createMany(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["rows"], scope)]),
        "returning": false,
        "sql": "INSERT INTO benchmark_users (email, name) SELECT json_extract(value, '$.email'), json_extract(value, '$.name') FROM json_each(?)",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_createMany_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["rows"], scope)]),
        "returning": false,
        "sql": "INSERT INTO benchmark_users (email, name) SELECT json_extract(value, '$.email'), json_extract(value, '$.name') FROM json_each(?)",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_upsertMany(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["rows"], scope)]),
        "returning": false,
        "sql": "INSERT INTO benchmark_users (email, name) SELECT json_extract(value, '$.email'), json_extract(value, '$.name') FROM json_each(?) WHERE true ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_upsertMany_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["rows"], scope)]),
        "returning": false,
        "sql": "INSERT INTO benchmark_users (email, name) SELECT json_extract(value, '$.email'), json_extract(value, '$.name') FROM json_each(?) WHERE true ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_updateMany(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["rows"], scope), cgp.ref(["rows"], scope)]),
        "returning": false,
        "sql": "UPDATE benchmark_users SET name = (SELECT json_extract(je.value, '$.name') FROM json_each(?) je WHERE json_extract(je.value, '$.id') = benchmark_users.id LIMIT 1) WHERE id IN (SELECT json_extract(value, '$.id') FROM json_each(?))",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
async function run_updateMany_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["rows"], scope), cgp.ref(["rows"], scope)]),
        "returning": false,
        "sql": "UPDATE benchmark_users SET name = (SELECT json_extract(je.value, '$.name') FROM json_each(?) je WHERE json_extract(je.value, '$.id') = benchmark_users.id LIMIT 1) WHERE id IN (SELECT json_extract(value, '$.id') FROM json_each(?))",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, false);
    scope["n0"] = r_n0;
    return r_n0;
}
function run_nestedCreate(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) RETURNING id",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["$e0", "id"], mscope_n1), cgp.ref(["title"], mscope_n1)]),
            "returning": false,
            "sql": "INSERT INTO benchmark_posts (author_id, title) VALUES (?, ?)",
            "write": true,
        };
        const mo_n1 = h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
async function run_nestedCreate_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) RETURNING id",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["$e0", "id"], mscope_n1), cgp.ref(["title"], mscope_n1)]),
            "returning": false,
            "sql": "INSERT INTO benchmark_posts (author_id, title) VALUES (?, ?)",
            "write": true,
        };
        const mo_n1 = await h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
function run_nestedUpsert(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name RETURNING id",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["$e0", "id"], mscope_n1), cgp.ref(["title"], mscope_n1)]),
            "returning": false,
            "sql": "INSERT INTO benchmark_posts (author_id, title) VALUES (?, ?)",
            "write": true,
        };
        const mo_n1 = h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
async function run_nestedUpsert_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) ON CONFLICT (email) DO UPDATE SET email = excluded.email, name = excluded.name RETURNING id",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["$e0", "id"], mscope_n1), cgp.ref(["title"], mscope_n1)]),
            "returning": false,
            "sql": "INSERT INTO benchmark_posts (author_id, title) VALUES (?, ?)",
            "write": true,
        };
        const mo_n1 = await h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
function run_nestedUpdate(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope), cgp.ref(["id"], scope)]),
        "returning": true,
        "sql": "UPDATE benchmark_users SET name = ? WHERE id = ? RETURNING id",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["title"], mscope_n1), cgp.ref(["$e0", "id"], mscope_n1)]),
            "returning": false,
            "sql": "UPDATE benchmark_posts SET title = ? WHERE author_id = ?",
            "write": true,
        };
        const mo_n1 = h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
async function run_nestedUpdate_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["name"], scope), cgp.ref(["id"], scope)]),
        "returning": true,
        "sql": "UPDATE benchmark_users SET name = ? WHERE id = ? RETURNING id",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["title"], mscope_n1), cgp.ref(["$e0", "id"], mscope_n1)]),
            "returning": false,
            "sql": "UPDATE benchmark_posts SET title = ? WHERE author_id = ?",
            "write": true,
        };
        const mo_n1 = await h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
function run_delete(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) RETURNING id",
        "write": true,
    };
    const o_n0 = h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["$e0", "id"], mscope_n1)]),
            "returning": false,
            "sql": "DELETE FROM benchmark_users WHERE id = ?",
            "write": true,
        };
        const mo_n1 = h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
async function run_delete_async(h$executeSQL, input) {
    const scope = input;
    // ── op 'n0' (executeSQL) ──
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    const ports_n0 = {
        "bigint": false,
        "params": cgp.arr([cgp.ref(["email"], scope), cgp.ref(["name"], scope)]),
        "returning": true,
        "sql": "INSERT INTO benchmark_users (email, name) VALUES (?, ?) RETURNING id",
        "write": true,
    };
    const o_n0 = await h$executeSQL(ports_n0, { nodeId: "n0", component: "executeSQL" });
    if ("error" in o_n0)
        throw new PlanFailure("OP_FAILED", `operation 'n0' failed under 'fail' policy: ${o_n0.error}`);
    const r_n0 = conformResultToOutType("n0", o_n0.ok, { "arr": { "name": "IdRow", "obj": { "id": "int" } } }, false);
    scope["n0"] = r_n0;
    // ── op 'n1' (map executeSQL) ──
    const over_n1 = r_n0;
    if (!Array.isArray(over_n1))
        throw new BehaviorFailure("MAP_OVER_NOT_ARRAY", "map 'n1': 'over' did not evaluate to an array");
    const kept_n1 = [];
    const collected_n1 = [];
    if (!h$executeSQL)
        throw new BehaviorFailure("UNKNOWN_COMPONENT", "component 'executeSQL' has no handler (fail-closed)");
    for (let mi_n1 = 0; mi_n1 < over_n1.length; mi_n1++) {
        const el_n1 = over_n1[mi_n1];
        const mscope_n1 = { ...scope, ["$e0"]: el_n1 };
        const ports_n1 = {
            "bigint": false,
            "params": cgp.arr([cgp.ref(["$e0", "id"], mscope_n1)]),
            "returning": false,
            "sql": "DELETE FROM benchmark_users WHERE id = ?",
            "write": true,
        };
        const mo_n1 = await h$executeSQL(ports_n1, { nodeId: "n1", component: "executeSQL", bound: el_n1 });
        if ("error" in mo_n1)
            throw new PlanFailure("OP_FAILED", `operation 'n1' failed under 'fail' policy: ${mo_n1.error}`);
        collected_n1.push(mo_n1.ok);
        kept_n1.push(mi_n1);
    }
    const r_n1 = conformResultToOutType("n1", collected_n1, { "arr": { "name": "WriteSummary", "obj": { "changes": "int", "lastInsertRowid": "int" } } }, true);
    scope["n1"] = r_n1;
    return r_n1;
}
function bind_findAll(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_findAll(h$executeSQL, { ...input });
}
function bindAsync_findAll(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_findAll_async(h$executeSQL, { ...input });
}
function bind_filterPaginateSort(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_filterPaginateSort(h$executeSQL, { ...input });
}
function bindAsync_filterPaginateSort(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_filterPaginateSort_async(h$executeSQL, { ...input });
}
function bind_findFirst(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_findFirst(h$executeSQL, { ...input });
}
function bindAsync_findFirst(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_findFirst_async(h$executeSQL, { ...input });
}
function bind_findUnique(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_findUnique(h$executeSQL, { ...input });
}
function bindAsync_findUnique(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_findUnique_async(h$executeSQL, { ...input });
}
function bind_nestedFindAll(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedFindAll(h$executeSQL, h$pluck, h$group, { ...input });
}
function bindAsync_nestedFindAll(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedFindAll_async(h$executeSQL, h$pluck, h$group, { ...input });
}
function bind_nestedFindFirst(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedFindFirst(h$executeSQL, h$pluck, h$group, { ...input });
}
function bindAsync_nestedFindFirst(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedFindFirst_async(h$executeSQL, h$pluck, h$group, { ...input });
}
function bind_nestedFindUnique(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedFindUnique(h$executeSQL, h$pluck, h$group, { ...input });
}
function bindAsync_nestedFindUnique(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedFindUnique_async(h$executeSQL, h$pluck, h$group, { ...input });
}
function bind_nestedRelations(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedRelations(h$executeSQL, h$pluck, h$group, { ...input });
}
function bindAsync_nestedRelations(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_nestedRelations_async(h$executeSQL, h$pluck, h$group, { ...input });
}
function bind_compositeRelations(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_compositeRelations(h$executeSQL, h$pluck, h$group, { ...input });
}
function bindAsync_compositeRelations(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    const h$pluck = handlers["pluck"];
    const h$group = handlers["group"];
    return (input = {}) => run_compositeRelations_async(h$executeSQL, h$pluck, h$group, { ...input });
}
function bind_create(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_create(h$executeSQL, { ...input });
}
function bindAsync_create(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_create_async(h$executeSQL, { ...input });
}
function bind_update(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_update(h$executeSQL, { ...input });
}
function bindAsync_update(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_update_async(h$executeSQL, { ...input });
}
function bind_upsert(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_upsert(h$executeSQL, { ...input });
}
function bindAsync_upsert(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_upsert_async(h$executeSQL, { ...input });
}
function bind_createMany(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_createMany(h$executeSQL, { ...input });
}
function bindAsync_createMany(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_createMany_async(h$executeSQL, { ...input });
}
function bind_upsertMany(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_upsertMany(h$executeSQL, { ...input });
}
function bindAsync_upsertMany(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_upsertMany_async(h$executeSQL, { ...input });
}
function bind_updateMany(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_updateMany(h$executeSQL, { ...input });
}
function bindAsync_updateMany(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_updateMany_async(h$executeSQL, { ...input });
}
function bind_nestedCreate(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_nestedCreate(h$executeSQL, { ...input });
}
function bindAsync_nestedCreate(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_nestedCreate_async(h$executeSQL, { ...input });
}
function bind_nestedUpsert(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_nestedUpsert(h$executeSQL, { ...input });
}
function bindAsync_nestedUpsert(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_nestedUpsert_async(h$executeSQL, { ...input });
}
function bind_nestedUpdate(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_nestedUpdate(h$executeSQL, { ...input });
}
function bindAsync_nestedUpdate(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_nestedUpdate_async(h$executeSQL, { ...input });
}
function bind_delete(handlers) {
    // handler 解決は bind 時に 1 回（catalog 名 → 実装）。呼び出しは解決済み参照の直呼び。
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_delete(h$executeSQL, { ...input });
}
function bindAsync_delete(handlers) {
    const h$executeSQL = handlers["executeSQL"];
    return (input = {}) => run_delete_async(h$executeSQL, { ...input });
}
/**
 * bind — inject handlers (boundary injection; catalog name → implementation) and get one
 * callable per component. Handler resolution happens HERE, once; each callable runs the
 * static straight-line function for that component through the resolved references — NOT
 * the runBehavior tree-walk. Observationally equivalent to the runBehavior path:
 * same values, same emitted op sequence, same Failure code/message.
 */
export function bind(handlers) {
    return {
        "findAll": bind_findAll(handlers),
        "filterPaginateSort": bind_filterPaginateSort(handlers),
        "findFirst": bind_findFirst(handlers),
        "findUnique": bind_findUnique(handlers),
        "nestedFindAll": bind_nestedFindAll(handlers),
        "nestedFindFirst": bind_nestedFindFirst(handlers),
        "nestedFindUnique": bind_nestedFindUnique(handlers),
        "nestedRelations": bind_nestedRelations(handlers),
        "compositeRelations": bind_compositeRelations(handlers),
        "create": bind_create(handlers),
        "update": bind_update(handlers),
        "upsert": bind_upsert(handlers),
        "createMany": bind_createMany(handlers),
        "upsertMany": bind_upsertMany(handlers),
        "updateMany": bind_updateMany(handlers),
        "nestedCreate": bind_nestedCreate(handlers),
        "nestedUpsert": bind_nestedUpsert(handlers),
        "nestedUpdate": bind_nestedUpdate(handlers),
        "delete": bind_delete(handlers),
    };
}
/**
 * bindAsync — as bind, but async handlers are awaited. Components with a real-concurrency
 * plan drive the runPlanAsync bounded-parallel primitive (same in-flight behavior as the
 * async ir path); strictly-sequential components run as awaited straight statements.
 */
export function bindAsync(handlers) {
    return {
        "findAll": bindAsync_findAll(handlers),
        "filterPaginateSort": bindAsync_filterPaginateSort(handlers),
        "findFirst": bindAsync_findFirst(handlers),
        "findUnique": bindAsync_findUnique(handlers),
        "nestedFindAll": bindAsync_nestedFindAll(handlers),
        "nestedFindFirst": bindAsync_nestedFindFirst(handlers),
        "nestedFindUnique": bindAsync_nestedFindUnique(handlers),
        "nestedRelations": bindAsync_nestedRelations(handlers),
        "compositeRelations": bindAsync_compositeRelations(handlers),
        "create": bindAsync_create(handlers),
        "update": bindAsync_update(handlers),
        "upsert": bindAsync_upsert(handlers),
        "createMany": bindAsync_createMany(handlers),
        "upsertMany": bindAsync_upsertMany(handlers),
        "updateMany": bindAsync_updateMany(handlers),
        "nestedCreate": bindAsync_nestedCreate(handlers),
        "nestedUpsert": bindAsync_nestedUpsert(handlers),
        "nestedUpdate": bindAsync_nestedUpdate(handlers),
        "delete": bindAsync_delete(handlers),
    };
}
// marshalUserRow — materialize a boxed Value into UserRow by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalUserRow(__v) {
    const __o = __v;
    return {
        "email": (__o["email"] === null ? null : __o["email"]),
        "id": __o["id"],
        "name": (__o["name"] === null ? null : __o["name"]),
    };
}
// marshalPostFullRow — materialize a boxed Value into PostFullRow by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalPostFullRow(__v) {
    const __o = __v;
    return {
        "author_id": (__o["author_id"] === null ? null : __o["author_id"]),
        "content": (__o["content"] === null ? null : __o["content"]),
        "created_at": (__o["created_at"] === null ? null : __o["created_at"]),
        "id": __o["id"],
        "published": (__o["published"] === null ? null : __o["published"]),
        "title": (__o["title"] === null ? null : __o["title"]),
    };
}
// marshalUserWithPosts — materialize a boxed Value into UserWithPosts by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalUserWithPosts(__v) {
    const __o = __v;
    return {
        "email": (__o["email"] === null ? null : __o["email"]),
        "id": __o["id"],
        "name": (__o["name"] === null ? null : __o["name"]),
        "posts": __o["posts"].map((__e) => marshalPostRow(__e)),
    };
}
// marshalPostRow — materialize a boxed Value into PostRow by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalPostRow(__v) {
    const __o = __v;
    return {
        "author_id": (__o["author_id"] === null ? null : __o["author_id"]),
        "id": __o["id"],
        "title": (__o["title"] === null ? null : __o["title"]),
    };
}
// marshalUserWithPostsAndComments — materialize a boxed Value into UserWithPostsAndComments by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalUserWithPostsAndComments(__v) {
    const __o = __v;
    return {
        "email": (__o["email"] === null ? null : __o["email"]),
        "id": __o["id"],
        "name": (__o["name"] === null ? null : __o["name"]),
        "posts": __o["posts"].map((__e) => marshalPostWithComments(__e)),
    };
}
// marshalPostWithComments — materialize a boxed Value into PostWithComments by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalPostWithComments(__v) {
    const __o = __v;
    return {
        "author_id": (__o["author_id"] === null ? null : __o["author_id"]),
        "comments": __o["comments"].map((__e) => marshalCommentRow(__e)),
        "id": __o["id"],
        "title": (__o["title"] === null ? null : __o["title"]),
    };
}
// marshalCommentRow — materialize a boxed Value into CommentRow by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalCommentRow(__v) {
    const __o = __v;
    return {
        "body": (__o["body"] === null ? null : __o["body"]),
        "id": (__o["id"] === null ? null : __o["id"]),
        "post_id": (__o["post_id"] === null ? null : __o["post_id"]),
    };
}
// marshalTenantUserWithPosts — materialize a boxed Value into TenantUserWithPosts by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalTenantUserWithPosts(__v) {
    const __o = __v;
    return {
        "name": (__o["name"] === null ? null : __o["name"]),
        "posts": __o["posts"].map((__e) => marshalTenantPostWithComments(__e)),
        "tenant_id": (__o["tenant_id"] === null ? null : __o["tenant_id"]),
        "user_id": (__o["user_id"] === null ? null : __o["user_id"]),
    };
}
// marshalTenantPostWithComments — materialize a boxed Value into TenantPostWithComments by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalTenantPostWithComments(__v) {
    const __o = __v;
    return {
        "comments": __o["comments"].map((__e) => marshalTenantCommentRow(__e)),
        "post_id": (__o["post_id"] === null ? null : __o["post_id"]),
        "tenant_id": (__o["tenant_id"] === null ? null : __o["tenant_id"]),
        "title": (__o["title"] === null ? null : __o["title"]),
        "user_id": (__o["user_id"] === null ? null : __o["user_id"]),
    };
}
// marshalTenantCommentRow — materialize a boxed Value into TenantCommentRow by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalTenantCommentRow(__v) {
    const __o = __v;
    return {
        "body": (__o["body"] === null ? null : __o["body"]),
        "comment_id": (__o["comment_id"] === null ? null : __o["comment_id"]),
        "post_id": (__o["post_id"] === null ? null : __o["post_id"]),
        "tenant_id": (__o["tenant_id"] === null ? null : __o["tenant_id"]),
    };
}
// marshalWriteSummary — materialize a boxed Value into WriteSummary by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalWriteSummary(__v) {
    const __o = __v;
    return {
        "changes": __o["changes"],
        "lastInsertRowid": __o["lastInsertRowid"],
    };
}
// marshalIdRow — materialize a boxed Value into IdRow by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalIdRow(__v) {
    const __o = __v;
    return {
        "id": __o["id"],
    };
}
// marshalNewUser — materialize a boxed Value into NewUser by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalNewUser(__v) {
    const __o = __v;
    return {
        "email": __o["email"],
        "name": __o["name"],
    };
}
// marshalUserPatch — materialize a boxed Value into UserPatch by reading each declared field (de-interpreted
// shape walk; the leaf `as` is the handler Value boundary — a no-op under TS erasure).
export function marshalUserPatch(__v) {
    const __o = __v;
    return {
        "id": __o["id"],
        "name": __o["name"],
    };
}
// marshalOutput_findAll — materialize component 'findAll' output into its declared outputType (UserRow[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_findAll(__raw) {
    return __raw.map((__e) => marshalUserRow(__e));
}
// marshalOutput_filterPaginateSort — materialize component 'filterPaginateSort' output into its declared outputType (PostFullRow[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_filterPaginateSort(__raw) {
    return __raw.map((__e) => marshalPostFullRow(__e));
}
// marshalOutput_findFirst — materialize component 'findFirst' output into its declared outputType (UserRow[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_findFirst(__raw) {
    return __raw.map((__e) => marshalUserRow(__e));
}
// marshalOutput_findUnique — materialize component 'findUnique' output into its declared outputType (UserRow[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_findUnique(__raw) {
    return __raw.map((__e) => marshalUserRow(__e));
}
// marshalOutput_nestedFindAll — materialize component 'nestedFindAll' output into its declared outputType (UserWithPosts[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedFindAll(__raw) {
    return __raw.map((__e) => marshalUserWithPosts(__e));
}
// marshalOutput_nestedFindFirst — materialize component 'nestedFindFirst' output into its declared outputType (UserWithPosts[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedFindFirst(__raw) {
    return __raw.map((__e) => marshalUserWithPosts(__e));
}
// marshalOutput_nestedFindUnique — materialize component 'nestedFindUnique' output into its declared outputType (UserWithPosts[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedFindUnique(__raw) {
    return __raw.map((__e) => marshalUserWithPosts(__e));
}
// marshalOutput_nestedRelations — materialize component 'nestedRelations' output into its declared outputType (UserWithPostsAndComments[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedRelations(__raw) {
    return __raw.map((__e) => marshalUserWithPostsAndComments(__e));
}
// marshalOutput_compositeRelations — materialize component 'compositeRelations' output into its declared outputType (TenantUserWithPosts[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_compositeRelations(__raw) {
    return __raw.map((__e) => marshalTenantUserWithPosts(__e));
}
// marshalOutput_create — materialize component 'create' output into its declared outputType (WriteSummary[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_create(__raw) {
    return __raw.map((__e) => marshalWriteSummary(__e));
}
// marshalOutput_update — materialize component 'update' output into its declared outputType (WriteSummary[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_update(__raw) {
    return __raw.map((__e) => marshalWriteSummary(__e));
}
// marshalOutput_upsert — materialize component 'upsert' output into its declared outputType (IdRow[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_upsert(__raw) {
    return __raw.map((__e) => marshalIdRow(__e));
}
// marshalOutput_createMany — materialize component 'createMany' output into its declared outputType (WriteSummary[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_createMany(__raw) {
    return __raw.map((__e) => marshalWriteSummary(__e));
}
// marshalOutput_upsertMany — materialize component 'upsertMany' output into its declared outputType (WriteSummary[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_upsertMany(__raw) {
    return __raw.map((__e) => marshalWriteSummary(__e));
}
// marshalOutput_updateMany — materialize component 'updateMany' output into its declared outputType (WriteSummary[]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_updateMany(__raw) {
    return __raw.map((__e) => marshalWriteSummary(__e));
}
// marshalOutput_nestedCreate — materialize component 'nestedCreate' output into its declared outputType (WriteSummary[][]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedCreate(__raw) {
    return __raw.map((__e) => __e.map((__e) => marshalWriteSummary(__e)));
}
// marshalOutput_nestedUpsert — materialize component 'nestedUpsert' output into its declared outputType (WriteSummary[][]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedUpsert(__raw) {
    return __raw.map((__e) => __e.map((__e) => marshalWriteSummary(__e)));
}
// marshalOutput_nestedUpdate — materialize component 'nestedUpdate' output into its declared outputType (WriteSummary[][]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_nestedUpdate(__raw) {
    return __raw.map((__e) => __e.map((__e) => marshalWriteSummary(__e)));
}
// marshalOutput_delete — materialize component 'delete' output into its declared outputType (WriteSummary[][]).
// The straight-line body already produced the canonical Value; this retypes it (tsc-checked shape
// walk). Runtime-equivalent under erasure (same value, retyped) — the equivalence pin holds.
export function marshalOutput_delete(__raw) {
    return __raw.map((__e) => __e.map((__e) => marshalWriteSummary(__e)));
}
/**
 * bindTyped — inject handlers and get one TYPED callable per covered component. Runs the same
 * interpreter-free straight-line runner as bind() (boundary injection; no per-node dispatch, no
 * IR-walk), then materializes the component result into its declared outputType. The materialization
 * is tsc-checked; under TS erasure it is runtime-equivalent to bind() (same value, retyped).
 */
export function bindTyped(handlers) {
    const __runners = bind(handlers);
    return {
        "findAll": (input) => marshalOutput_findAll(__runners["findAll"](input)),
        "filterPaginateSort": (input) => marshalOutput_filterPaginateSort(__runners["filterPaginateSort"](input)),
        "findFirst": (input) => marshalOutput_findFirst(__runners["findFirst"](input)),
        "findUnique": (input) => marshalOutput_findUnique(__runners["findUnique"](input)),
        "nestedFindAll": (input) => marshalOutput_nestedFindAll(__runners["nestedFindAll"](input)),
        "nestedFindFirst": (input) => marshalOutput_nestedFindFirst(__runners["nestedFindFirst"](input)),
        "nestedFindUnique": (input) => marshalOutput_nestedFindUnique(__runners["nestedFindUnique"](input)),
        "nestedRelations": (input) => marshalOutput_nestedRelations(__runners["nestedRelations"](input)),
        "compositeRelations": (input) => marshalOutput_compositeRelations(__runners["compositeRelations"](input)),
        "create": (input) => marshalOutput_create(__runners["create"](input)),
        "update": (input) => marshalOutput_update(__runners["update"](input)),
        "upsert": (input) => marshalOutput_upsert(__runners["upsert"](input)),
        "createMany": (input) => marshalOutput_createMany(__runners["createMany"](input)),
        "upsertMany": (input) => marshalOutput_upsertMany(__runners["upsertMany"](input)),
        "updateMany": (input) => marshalOutput_updateMany(__runners["updateMany"](input)),
        "nestedCreate": (input) => marshalOutput_nestedCreate(__runners["nestedCreate"](input)),
        "nestedUpsert": (input) => marshalOutput_nestedUpsert(__runners["nestedUpsert"](input)),
        "nestedUpdate": (input) => marshalOutput_nestedUpdate(__runners["nestedUpdate"](input)),
        "delete": (input) => marshalOutput_delete(__runners["delete"](input)),
    };
}
/**
 * bindTypedAsync — as bindTyped, awaiting async handlers. Covered components' results are materialized
 * into their declared outputType after the straight-line async runner settles.
 */
export function bindTypedAsync(handlers) {
    const __runners = bindAsync(handlers);
    return {
        "findAll": async (input) => marshalOutput_findAll(await __runners["findAll"](input)),
        "filterPaginateSort": async (input) => marshalOutput_filterPaginateSort(await __runners["filterPaginateSort"](input)),
        "findFirst": async (input) => marshalOutput_findFirst(await __runners["findFirst"](input)),
        "findUnique": async (input) => marshalOutput_findUnique(await __runners["findUnique"](input)),
        "nestedFindAll": async (input) => marshalOutput_nestedFindAll(await __runners["nestedFindAll"](input)),
        "nestedFindFirst": async (input) => marshalOutput_nestedFindFirst(await __runners["nestedFindFirst"](input)),
        "nestedFindUnique": async (input) => marshalOutput_nestedFindUnique(await __runners["nestedFindUnique"](input)),
        "nestedRelations": async (input) => marshalOutput_nestedRelations(await __runners["nestedRelations"](input)),
        "compositeRelations": async (input) => marshalOutput_compositeRelations(await __runners["compositeRelations"](input)),
        "create": async (input) => marshalOutput_create(await __runners["create"](input)),
        "update": async (input) => marshalOutput_update(await __runners["update"](input)),
        "upsert": async (input) => marshalOutput_upsert(await __runners["upsert"](input)),
        "createMany": async (input) => marshalOutput_createMany(await __runners["createMany"](input)),
        "upsertMany": async (input) => marshalOutput_upsertMany(await __runners["upsertMany"](input)),
        "updateMany": async (input) => marshalOutput_updateMany(await __runners["updateMany"](input)),
        "nestedCreate": async (input) => marshalOutput_nestedCreate(await __runners["nestedCreate"](input)),
        "nestedUpsert": async (input) => marshalOutput_nestedUpsert(await __runners["nestedUpsert"](input)),
        "nestedUpdate": async (input) => marshalOutput_nestedUpdate(await __runners["nestedUpdate"](input)),
        "delete": async (input) => marshalOutput_delete(await __runners["delete"](input)),
    };
}
