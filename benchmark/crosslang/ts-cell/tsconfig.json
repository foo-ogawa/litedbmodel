{
  "// note": "The cell is COMPILED with tsc and run with plain node, not transformed on the fly. tsx cannot do both halves of what this cell needs: given a --tsconfig it stops resolving behavior-contracts' import-only exports, and without one it emits TC39 decorators, which the runtime @column/@hasMany (written against experimentalDecorators) cannot consume. Compiling also type-checks the cell, and matches the other languages, whose cells are built before they run. rootDir is benchmark/crosslang (main.ts imports the axis SSoT ../contract.ts) and outDir is benchmark/crosslang-build, the same depth, so every relative path in the emitted JS still resolves.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "rootDir": "..",
    "outDir": "../../crosslang-build"
  },
  "include": ["*.ts", "../contract.ts", "../orm-domain.ts", "../emit-setup.ts"]
}
