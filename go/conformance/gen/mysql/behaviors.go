// GENERATED by behavior-contracts (bc#90, go-typed-native — the RUNTIME-FREE covered read de-box) — DO NOT EDIT.
// Input: portable component-graph IR only. Every component here is a COVERED READ (sequential typed
// componentRef); it is emitted ONLY as the combined native ports IN + raw struct OUT + inline
// sequential runner below. There is NO generic Bind/dispatch/plan scaffolding AND NO bc-runtime import —
// a fully-covered module is FULLY NATIVE (zero bc-runtime reference; only the std-lib it uses).

package conformance

import (
	"github.com/foo-ogawa/litedbmodel/go/litedbmodel_runtime/wire"
	"github.com/foo-ogawa/litedbmodel/go/litedbmodel_runtime"
)

// ExpectedSpecVersions are the spec versions baked at generation time (provenance only — the module
// is runtime-free, so there is no runtime SpecVersions to skew-check against, bc#90 / change #5).
var ExpectedSpecVersions = map[string]int64{"behavior": 6, "expression": 2, "plan": 1}

// COMBINED READ layer (bc#90 — the RUNTIME-FREE read de-box; STRUCT-ONLY, FULLY-NATIVE surface). A
// covered read is a strictly-sequential typed componentRef chain (point reads + relation children
// children), emitted ONLY here: each node builds a native ports struct by direct field assignment (every
// port is a CONCRETE Go value — string / bool / []string projection / int64 limit — NO boxed Value, NO
// PortReader accessor); the runner calls the op-agnostic leaf TRANSPORT symbol (default Leaf_<comp>)
// DIRECTLY with the node's port fields spread (no per-node handler indirection, no interface, no generics —
// zero dynamic dispatch). A leaf is ONE transport, not one method per authored node. The transport returns
// the node's result WIRE (a single wire.WireValue); the runner de-boxes it INLINE (fully-unrolled
// probe/match — no decode helper, no output recursion) into the node's outType struct. The covered plane
// carries NO bc-runtime reference at all — failures use the LOCAL *BehaviorError (same codes, byte-equal to
// run_behavior). Exec is INLINE sequential (no plan driver) so a relation child reads the parent's REAL
// struct result via direct field access and the child-present decision is made from the real parent value —
// relation / connection converge with run_behavior (fixes #323/#74). The exposed API is the 1:1
// STRUCT-returning entry <Method>(<positional params>) (the In_<comp> box removed); the consumer supplies
// the leaf transport symbols (default same-package Leaf_<comp>, or a leafTransport import). This module is
// FULLY NATIVE: a naive grep for
// boxing primitives OR for the bc-runtime package finds nothing, by design (bc#90/runtime-free).

// Local concrete failure type (runtime-free) — a covered runner returns *BehaviorError (satisfies
// error) instead of a bc-runtime failure, so the fully-covered module imports ZERO bc runtime.
// ErrorDetail — the structured, recoverable payload a failure carries (scp-error.md "The Error
// Value"). The LEAF produces it at the wire boundary (the only party holding both the declared type
// and the raw wire datum); the runner transports it verbatim. Concrete fields — no boxed Value, no
// serialized blob in a string: ExpectedType is Portable Type Notation, a rendering of a STATICALLY
// DECLARED type, so nothing walks a type at runtime. Kind is the closed set (typeMismatch /
// missingField / overflow), empty when the failure is not about a datum.
type ErrorDetail struct {
	Kind           string
	Model          string
	Field          string
	ExpectedType   string
	ActualWireType string
	RawValue       string
	Context        map[string]string
}

// BehaviorError — the LOCAL concrete failure type for the covered read plane (runtime-free): a
// covered runner returns *BehaviorError (which satisfies error) instead of a bc-runtime failure, so
// the fully-covered module imports ZERO bc runtime. Codes match run_behavior verbatim (byte-equal).
// Detail carries the leaf's structured Error Value across the seam so a caller can log / skip /
// repair / migrate rather than parse a message (nil when the failure is not about a datum).
type BehaviorError struct {
	Code    string
	Message string
	Detail  *ErrorDetail
}

func (e *BehaviorError) Error() string { return e.Code + ": " + e.Message }

// FailureCode exposes the stable failure code (byte-equal to run_behavior) WITHOUT a bc-runtime type:
// a caller that wants the code type-asserts the small local interface { FailureCode() string } rather
// than a bc failure type — keeping this module runtime-free while a harness can still read the code.
func (e *BehaviorError) FailureCode() string { return e.Code }

// unknownComponent — the fail-closed failure for a catalog name with no handler (byte-equal to
// run_behavior). In the STATIC codegen model a missing impl is normally a compile error; this covers
// the scripted test glue's dynamic queue miss.
func unknownComponent(component string) *BehaviorError {
	return &BehaviorError{Code: "UNKNOWN_COMPONENT", Message: "component '" + component + "' has no handler (fail-closed)"}
}

// leafFailure — a failure the LEAF reports (test glue): the runner assigns the node's failure code
// from its Error Policy Kind, so what a leaf carries is its message and its structured Error Value.
func leafFailure(message string) *BehaviorError {
	return &BehaviorError{Code: "LEAF_FAILURE", Message: message}
}

// opFailed wraps a leaf failure as the node's Failure under its Error Policy Kind, carrying the
// leaf's structured detail through unchanged (the runner transports the Error Value, it does not
// synthesize or re-interpret it).
func opFailed(node string, policy string, e error) *BehaviorError {
	// The Kind names itself in the message, and 'retry' says it gave up — the runtime owns the attempts
	// (scp-error.md: tuning is Runtime-owned), so what the plan reports is the exhausted retry.
	where := "operation '" + node + "' failed under '" + policy + "' policy"
	if policy == "retry" {
		where += " (exhausted)"
	}
	if be, ok := e.(*BehaviorError); ok {
		return &BehaviorError{Code: "OP_FAILED", Message: where + ": " + be.Message, Detail: be.Detail}
	}
	return &BehaviorError{Code: "OP_FAILED", Message: where + ": " + e.Error()}
}

// deTypeMismatch / deMissingField — the de-box mismatch failures the generated decode
// produces (scp-error.md "The Error Value"). Codes are byte-equal to the runtime outType check
// (TYPE_MISMATCH / MISSING_PROP) so the covered read stays equivalent to run_behavior; detail.kind
// distinguishes typeMismatch / missingField. ErrorDetail is built by a POSITIONAL literal so
// the offending value is carried without naming the field (the covered module stays whole-file free of
// the boxed result-type token; its only permitted occurrence is the ErrorDetail field declaration).
func deTypeMismatch(model, field, expected, actualWire, raw string) *BehaviorError {
	return &BehaviorError{Code: "TYPE_MISMATCH", Message: "node '" + model + "': " + field + ": expected " + expected + ", got " + actualWire, Detail: &ErrorDetail{"typeMismatch", model, field, expected, actualWire, raw, nil}}
}

func deMissingField(model, field, expected string) *BehaviorError {
	return &BehaviorError{Code: "MISSING_PROP", Message: "node '" + model + "': " + field + ": required field is absent", Detail: &ErrorDetail{"missingField", model, field, expected, "", "", nil}}
}

// Typed struct declarations (outType-derived; hash-dedup — shared type plan).
type PostsRow struct {
	Author_id  *float64 // "author_id"
	Created_at *string  // "created_at"
	Id         *float64 // "id"
	Status     *string  // "status"
	Title      *string  // "title"
}

type PostsTopRow struct {
	Author_id  *float64 // "author_id"
	Created_at *string  // "created_at"
	Id         *float64 // "id"
	Status     *string  // "status"
	Title      *string  // "title"
}

type PageRow struct {
	Id    *float64 // "id"
	Title *string  // "title"
}

type PostsByIdsRow struct {
	Id    *float64 // "id"
	Title *string  // "title"
}

type FeedRow struct {
	Author_id *float64 // "author_id"
	Id        *float64 // "id"
	Status    *string  // "status"
	Title     *string  // "title"
}

type DynamicWherePlan struct {
	Frags []DynamicWhereFrag // "frags"
}

type DynamicWhereFrag struct {
	Params  []*wire.WireValue // "params"
	Skipped bool              // "skipped"
	Sql     string            // "sql"
}

type UsersWithPostsRow struct {
	Id    *float64                  // "id"
	Name  *string                   // "name"
	Posts []UsersWithPostsRow_posts // "posts"
}

type UsersWithPostsRow_posts struct {
	Author_id  *float64                       // "author_id"
	Created_at *string                        // "created_at"
	Id         *float64                       // "id"
	Status     *string                        // "status"
	Tags       []UsersWithPostsRow_posts_tags // "tags"
	Title      *string                        // "title"
}

type UsersWithPostsRow_posts_tags struct {
	Id      *float64 // "id"
	Label   *string  // "label"
	Post_id *float64 // "post_id"
}

type PostsWithAuthorRow struct {
	Author     *PostsWithAuthorRow_author // "author"
	Author_id  *float64                   // "author_id"
	Created_at *string                    // "created_at"
	Id         *float64                   // "id"
	Status     *string                    // "status"
	Title      *string                    // "title"
}

type PostsWithAuthorRow_author struct {
	Id         *float64 // "id"
	Name       *string  // "name"
	Post_count *float64 // "post_count"
}

type CapGuard struct {
	Limit    int64   // "limit"
	Model    *string // "model"
	Relation string  // "relation"
}

type UsersWithCappedPostsRow struct {
	CappedPosts []UsersWithCappedPostsRow_cappedPosts // "cappedPosts"
	Id          *float64                              // "id"
	Name        *string                               // "name"
}

type UsersWithCappedPostsRow_cappedPosts struct {
	Author_id  *float64 // "author_id"
	Created_at *string  // "created_at"
	Id         *float64 // "id"
	Status     *string  // "status"
	Title      *string  // "title"
}

type UsersWithUncappedPostsRow struct {
	Id            *float64                                  // "id"
	Name          *string                                   // "name"
	UncappedPosts []UsersWithUncappedPostsRow_uncappedPosts // "uncappedPosts"
}

type UsersWithUncappedPostsRow_uncappedPosts struct {
	Author_id  *float64 // "author_id"
	Created_at *string  // "created_at"
	Id         *float64 // "id"
	Status     *string  // "status"
	Title      *string  // "title"
}

type UsersWithTopPostsRow struct {
	Id       *float64                        // "id"
	Name     *string                         // "name"
	TopPosts []UsersWithTopPostsRow_topPosts // "topPosts"
}

type UsersWithTopPostsRow_topPosts struct {
	Author_id  *float64 // "author_id"
	Created_at *string  // "created_at"
	Id         *float64 // "id"
	Status     *string  // "status"
	Title      *string  // "title"
}

type WriteSummary struct {
	Changes         int64 // "changes"
	LastInsertRowid int64 // "lastInsertRowid"
}

type CreatePostReturningRow struct {
	Id    *float64 // "id"
	Title *string  // "title"
}

type RenamePostReturningRow struct {
	Id    *float64 // "id"
	Title *string  // "title"
}

type RemovePostReturningRow struct {
	Id    *float64 // "id"
	Title *string  // "title"
}

type RestatusPostsReturningRow struct {
	Id     *float64 // "id"
	Status *string  // "status"
}

type RemovePostsByAuthorReturningRow struct {
	Id    *float64 // "id"
	Title *string  // "title"
}

type TypedRowsRow struct {
	Flag  *float64 // "flag"
	Id    *float64 // "id"
	Label *string  // "label"
	Ts    *string  // "ts"
}

type CreateTagsRecord struct {
	Id      int64  // "id"
	Label   string // "label"
	Post_id int64  // "post_id"
}

type CreateTagsReturningRow struct {
	Id    *float64 // "id"
	Label *string  // "label"
}

type CreateTagsReturningRecord struct {
	Id      int64  // "id"
	Label   string // "label"
	Post_id int64  // "post_id"
}

type RelabelTagsReturningRow struct {
	Id    *float64 // "id"
	Label *string  // "label"
}

type RelabelTagsReturningRecord struct {
	Id    int64  // "id"
	Label string // "label"
}

type RemoveTagsReturningRow struct {
	Id    *float64 // "id"
	Label *string  // "label"
}

// Probe kind consts (the inline de-box compares against these).
// Probe kinds — the outcome of classifying one wire attribute against a declared field type.
const (
	probeGot    uint8 = 0 // present; the wire variant matches the declared type
	probeWrong  uint8 = 1 // present but a different wire variant (carries ActualWireType + Raw)
	probeAbsent uint8 = 2 // no attribute for this field
	probeNull   uint8 = 3 // present as the producer's null variant (opt → None; required → mismatch)
)

// Combined read runners (STRUCT-returning — the fully de-plumbed path).
// Posts — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func Posts(authorId int64) ([]PostsRow, error) {
	var t_n0 []PostsRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(authorId)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE author_id = ? ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]PostsRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 PostsRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 PostsRow
				p2 := p1.Got.ProbeFloat("author_id")
				if p2.Kind == probeGot {
					rec1.Author_id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("PostsRow", "author_id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("created_at")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Created_at = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("PostsRow", "created_at", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				p4 := p1.Got.ProbeFloat("id")
				if p4.Kind == probeGot {
					rec1.Id = &p4.Got
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("PostsRow", "id", "opt(float)", p4.ActualWireType, p4.Raw)
				}
				p5 := p1.Got.ProbeString("status")
				if p5.Kind == probeGot {
					sv5 := p5.Got
					rec1.Status = &sv5
				} else if p5.Kind == probeWrong {
					return nil, deTypeMismatch("PostsRow", "status", "opt(string)", p5.ActualWireType, p5.Raw)
				}
				p6 := p1.Got.ProbeString("title")
				if p6.Kind == probeGot {
					sv6 := p6.Got
					rec1.Title = &sv6
				} else if p6.Kind == probeWrong {
					return nil, deTypeMismatch("PostsRow", "title", "opt(string)", p6.ActualWireType, p6.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// PostsTop — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func PostsTop() ([]PostsTopRow, error) {
	var t_n0 []PostsTopRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status, created_at FROM conf_posts ORDER BY id ASC LIMIT 2")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]PostsTopRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 PostsTopRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 PostsTopRow
				p2 := p1.Got.ProbeFloat("author_id")
				if p2.Kind == probeGot {
					rec1.Author_id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("PostsTopRow", "author_id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("created_at")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Created_at = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("PostsTopRow", "created_at", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				p4 := p1.Got.ProbeFloat("id")
				if p4.Kind == probeGot {
					rec1.Id = &p4.Got
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("PostsTopRow", "id", "opt(float)", p4.ActualWireType, p4.Raw)
				}
				p5 := p1.Got.ProbeString("status")
				if p5.Kind == probeGot {
					sv5 := p5.Got
					rec1.Status = &sv5
				} else if p5.Kind == probeWrong {
					return nil, deTypeMismatch("PostsTopRow", "status", "opt(string)", p5.ActualWireType, p5.Raw)
				}
				p6 := p1.Got.ProbeString("title")
				if p6.Kind == probeGot {
					sv6 := p6.Got
					rec1.Title = &sv6
				} else if p6.Kind == probeWrong {
					return nil, deTypeMismatch("PostsTopRow", "title", "opt(string)", p6.ActualWireType, p6.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// Page — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func Page(limit int64, offset int64) ([]PageRow, error) {
	var t_n0 []PageRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(limit), wire.WireInt(offset)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, title FROM conf_posts ORDER BY id ASC LIMIT ? OFFSET ?")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]PageRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 PageRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 PageRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("PageRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("title")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Title = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("PageRow", "title", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// PostsByIds — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func PostsByIds(ids []int64) ([]PostsByIdsRow, error) {
	var t_n0 []PostsByIdsRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range ids { it0 = append(it0, wire.WireInt(e0)) }; return wire.WireListOf(it0) }()})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, title FROM conf_posts WHERE id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]PostsByIdsRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 PostsByIdsRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 PostsByIdsRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("PostsByIdsRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("title")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Title = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("PostsByIdsRow", "title", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// Feed — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func Feed(authorId int64, status *string, since *string) ([]FeedRow, error) {
	var t_n0 []FeedRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	pf_n0, pe_n0 := func() ([]wire.WireField, error) {
		ex_15 := wire.WireInt(authorId)
		ex_16 := DynamicWherePlan{Frags: []DynamicWhereFrag{DynamicWhereFrag{Params: []*wire.WireValue{&ex_15}, Skipped: false, Sql: "author_id = ?"}, DynamicWhereFrag{Params: []*wire.WireValue{func() *wire.WireValue { __oo := status; if __oo == nil { return nil }; __ov := (*__oo); __ob := wire.WireStr(__ov); return &__ob }()}, Skipped: (status == nil), Sql: "status = ?"}, DynamicWhereFrag{Params: []*wire.WireValue{func() *wire.WireValue { __oo := since; if __oo == nil { return nil }; __ov := (*__oo); __ob := wire.WireStr(__ov); return &__ob }()}, Skipped: (since == nil), Sql: "created_at >= ?"}}}
		return []wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status FROM conf_posts ORDER BY id ASC")}, wire.WireField{Key: "whereDynamic", Val: func() wire.WireValue { o0 := &ex_16; if o0 == nil { return wire.WireNull() }; return wire.WireRowOf([]wire.WireField{{Key: "frags", Val: func() wire.WireValue { it1 := []wire.WireValue{}; for _, e1 := range (*o0).Frags { it1 = append(it1, wire.WireRowOf([]wire.WireField{{Key: "params", Val: func() wire.WireValue { it3 := []wire.WireValue{}; for _, e3 := range e1.Params { it3 = append(it3, func() wire.WireValue { o4 := e3; if o4 == nil { return wire.WireNull() }; return (*o4) }()) }; return wire.WireListOf(it3) }()}, {Key: "skipped", Val: wire.WireBool(e1.Skipped)}, {Key: "sql", Val: wire.WireStr(e1.Sql)}})) }; return wire.WireListOf(it1) }()}}) }()}, wire.WireField{Key: "write", Val: wire.WireBool(false)}}, nil
	}()
	if pe_n0 != nil {
		return nil, pe_n0
	}
	payload_n0 := wire.WireRowOfFields(pf_n0)
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]FeedRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 FeedRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 FeedRow
				p2 := p1.Got.ProbeFloat("author_id")
				if p2.Kind == probeGot {
					rec1.Author_id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("FeedRow", "author_id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeFloat("id")
				if p3.Kind == probeGot {
					rec1.Id = &p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("FeedRow", "id", "opt(float)", p3.ActualWireType, p3.Raw)
				}
				p4 := p1.Got.ProbeString("status")
				if p4.Kind == probeGot {
					sv4 := p4.Got
					rec1.Status = &sv4
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("FeedRow", "status", "opt(string)", p4.ActualWireType, p4.Raw)
				}
				p5 := p1.Got.ProbeString("title")
				if p5.Kind == probeGot {
					sv5 := p5.Got
					rec1.Title = &sv5
				} else if p5.Kind == probeWrong {
					return nil, deTypeMismatch("FeedRow", "title", "opt(string)", p5.ActualWireType, p5.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{author_id:opt(float),id:opt(float),status:opt(string),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// UsersWithPosts — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func UsersWithPosts() ([]UsersWithPostsRow, error) {
	var t_n0 []wire.WireValue
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	var t_n1 []wire.WireValue
	produced_n1 := false
	_ = t_n1
	_ = produced_n1
	var t_n2 []wire.WireValue
	produced_n2 := false
	_ = t_n2
	_ = produced_n2
	var t_n3 []wire.WireValue
	produced_n3 := false
	_ = t_n3
	_ = produced_n3
	var t_n4 []wire.WireValue
	produced_n4 := false
	_ = t_n4
	_ = produced_n4
	var t_n5 []wire.WireValue
	produced_n5 := false
	_ = t_n5
	_ = produced_n5
	var t_n6 []UsersWithPostsRow
	produced_n6 := false
	_ = t_n6
	_ = produced_n6
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, name FROM conf_users ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		t_n0 = wire_n0.Items
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(value)")
	}
	produced_n0 = true
	// ── op 'n1' (pluck, parent:n0) ──
	if produced_n0 {
		payload_n1 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "col", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "rows", Val: wire.WireListOf(t_n0)}})
		wire_n1, wire_n1Err := litedbmodel_runtime.PluckKeys(payload_n1)
		if wire_n1Err != nil {
			return nil, opFailed("n1", "fail", wire_n1Err)
		}
		p1 := wire_n1.AsList()
		if p1.Kind == probeGot {
			t_n1 = wire_n1.Items
		} else if p1.Kind == probeWrong {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else if p1.Kind == probeNull {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else {
			return nil, deMissingField("n1", "n1", "arr(value)")
		}
		produced_n1 = true
	}
	// ── op 'n2' (executeSQL, parent:n1) ──
	if produced_n1 {
		payload_n2 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireListOf(t_n1)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
		wire_n2, wire_n2Err := litedbmodel_runtime.ExecuteSQL(payload_n2)
		if wire_n2Err != nil {
			return nil, opFailed("n2", "fail", wire_n2Err)
		}
		p2 := wire_n2.AsList()
		if p2.Kind == probeGot {
			t_n2 = wire_n2.Items
		} else if p2.Kind == probeWrong {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else if p2.Kind == probeNull {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else {
			return nil, deMissingField("n2", "n2", "arr(value)")
		}
		produced_n2 = true
	}
	// ── op 'n3' (pluck, parent:n2) ──
	if produced_n2 {
		payload_n3 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "col", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "rows", Val: wire.WireListOf(t_n2)}})
		wire_n3, wire_n3Err := litedbmodel_runtime.PluckKeys(payload_n3)
		if wire_n3Err != nil {
			return nil, opFailed("n3", "fail", wire_n3Err)
		}
		p3 := wire_n3.AsList()
		if p3.Kind == probeGot {
			t_n3 = wire_n3.Items
		} else if p3.Kind == probeWrong {
			return nil, deTypeMismatch("n3", "n3", "arr(value)", p3.ActualWireType, p3.Raw)
		} else if p3.Kind == probeNull {
			return nil, deTypeMismatch("n3", "n3", "arr(value)", p3.ActualWireType, p3.Raw)
		} else {
			return nil, deMissingField("n3", "n3", "arr(value)")
		}
		produced_n3 = true
	}
	// ── op 'n4' (executeSQL, parent:n3) ──
	if produced_n3 {
		payload_n4 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireListOf(t_n3)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, post_id, label FROM conf_tags WHERE post_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
		wire_n4, wire_n4Err := litedbmodel_runtime.ExecuteSQL(payload_n4)
		if wire_n4Err != nil {
			return nil, opFailed("n4", "fail", wire_n4Err)
		}
		p4 := wire_n4.AsList()
		if p4.Kind == probeGot {
			t_n4 = wire_n4.Items
		} else if p4.Kind == probeWrong {
			return nil, deTypeMismatch("n4", "n4", "arr(value)", p4.ActualWireType, p4.Raw)
		} else if p4.Kind == probeNull {
			return nil, deTypeMismatch("n4", "n4", "arr(value)", p4.ActualWireType, p4.Raw)
		} else {
			return nil, deMissingField("n4", "n4", "arr(value)")
		}
		produced_n4 = true
	}
	// ── op 'n5' (group, parent:n2) ──
	if produced_n2 {
		payload_n5 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "children", Val: wire.WireListOf(t_n4)}, wire.WireField{Key: "fk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"post_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "into", Val: wire.WireStr("tags")}, wire.WireField{Key: "parents", Val: wire.WireListOf(t_n2)}, wire.WireField{Key: "pk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "single", Val: wire.WireBool(false)}})
		wire_n5, wire_n5Err := litedbmodel_runtime.GroupChildren(payload_n5)
		if wire_n5Err != nil {
			return nil, opFailed("n5", "fail", wire_n5Err)
		}
		p5 := wire_n5.AsList()
		if p5.Kind == probeGot {
			t_n5 = wire_n5.Items
		} else if p5.Kind == probeWrong {
			return nil, deTypeMismatch("n5", "n5", "arr(value)", p5.ActualWireType, p5.Raw)
		} else if p5.Kind == probeNull {
			return nil, deTypeMismatch("n5", "n5", "arr(value)", p5.ActualWireType, p5.Raw)
		} else {
			return nil, deMissingField("n5", "n5", "arr(value)")
		}
		produced_n5 = true
	}
	// ── op 'n6' (group, parent:n0) ──
	if produced_n0 {
		payload_n6 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "children", Val: wire.WireListOf(t_n5)}, wire.WireField{Key: "fk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"author_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "into", Val: wire.WireStr("posts")}, wire.WireField{Key: "parents", Val: wire.WireListOf(t_n0)}, wire.WireField{Key: "pk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "single", Val: wire.WireBool(false)}})
		wire_n6, wire_n6Err := litedbmodel_runtime.GroupChildren(payload_n6)
		if wire_n6Err != nil {
			return nil, opFailed("n6", "fail", wire_n6Err)
		}
		p6 := wire_n6.AsList()
		if p6.Kind == probeGot {
			list6 := make([]UsersWithPostsRow, 0, p6.Got.Len())
			for i6 := 0; i6 < p6.Got.Len(); i6++ {
				var el6 UsersWithPostsRow
				p7 := p6.Got.ElemRow(i6)
				if p7.Kind == probeGot {
					var rec7 UsersWithPostsRow
					p8 := p7.Got.ProbeFloat("id")
					if p8.Kind == probeGot {
						rec7.Id = &p8.Got
					} else if p8.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithPostsRow", "id", "opt(float)", p8.ActualWireType, p8.Raw)
					}
					p9 := p7.Got.ProbeString("name")
					if p9.Kind == probeGot {
						sv9 := p9.Got
						rec7.Name = &sv9
					} else if p9.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithPostsRow", "name", "opt(string)", p9.ActualWireType, p9.Raw)
					}
					p10 := p7.Got.ProbeList("posts")
					if p10.Kind == probeGot {
						list10 := make([]UsersWithPostsRow_posts, 0, p10.Got.Len())
						for i10 := 0; i10 < p10.Got.Len(); i10++ {
							var el10 UsersWithPostsRow_posts
							p11 := p10.Got.ElemRow(i10)
							if p11.Kind == probeGot {
								var rec11 UsersWithPostsRow_posts
								p12 := p11.Got.ProbeFloat("author_id")
								if p12.Kind == probeGot {
									rec11.Author_id = &p12.Got
								} else if p12.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "author_id", "opt(float)", p12.ActualWireType, p12.Raw)
								}
								p13 := p11.Got.ProbeString("created_at")
								if p13.Kind == probeGot {
									sv13 := p13.Got
									rec11.Created_at = &sv13
								} else if p13.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "created_at", "opt(string)", p13.ActualWireType, p13.Raw)
								}
								p14 := p11.Got.ProbeFloat("id")
								if p14.Kind == probeGot {
									rec11.Id = &p14.Got
								} else if p14.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "id", "opt(float)", p14.ActualWireType, p14.Raw)
								}
								p15 := p11.Got.ProbeString("status")
								if p15.Kind == probeGot {
									sv15 := p15.Got
									rec11.Status = &sv15
								} else if p15.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "status", "opt(string)", p15.ActualWireType, p15.Raw)
								}
								p16 := p11.Got.ProbeList("tags")
								if p16.Kind == probeGot {
									list16 := make([]UsersWithPostsRow_posts_tags, 0, p16.Got.Len())
									for i16 := 0; i16 < p16.Got.Len(); i16++ {
										var el16 UsersWithPostsRow_posts_tags
										p17 := p16.Got.ElemRow(i16)
										if p17.Kind == probeGot {
											var rec17 UsersWithPostsRow_posts_tags
											p18 := p17.Got.ProbeFloat("id")
											if p18.Kind == probeGot {
												rec17.Id = &p18.Got
											} else if p18.Kind == probeWrong {
												return nil, deTypeMismatch("UsersWithPostsRow_posts_tags", "id", "opt(float)", p18.ActualWireType, p18.Raw)
											}
											p19 := p17.Got.ProbeString("label")
											if p19.Kind == probeGot {
												sv19 := p19.Got
												rec17.Label = &sv19
											} else if p19.Kind == probeWrong {
												return nil, deTypeMismatch("UsersWithPostsRow_posts_tags", "label", "opt(string)", p19.ActualWireType, p19.Raw)
											}
											p20 := p17.Got.ProbeFloat("post_id")
											if p20.Kind == probeGot {
												rec17.Post_id = &p20.Got
											} else if p20.Kind == probeWrong {
												return nil, deTypeMismatch("UsersWithPostsRow_posts_tags", "post_id", "opt(float)", p20.ActualWireType, p20.Raw)
											}
											el16 = rec17
										} else if p17.Kind == probeWrong {
											return nil, deTypeMismatch("UsersWithPostsRow_posts", "tags", "obj{id:opt(float),label:opt(string),post_id:opt(float)}", p17.ActualWireType, p17.Raw)
										} else if p17.Kind == probeNull {
											return nil, deTypeMismatch("UsersWithPostsRow_posts", "tags", "obj{id:opt(float),label:opt(string),post_id:opt(float)}", p17.ActualWireType, p17.Raw)
										} else {
											return nil, deMissingField("UsersWithPostsRow_posts", "tags", "obj{id:opt(float),label:opt(string),post_id:opt(float)}")
										}
										list16 = append(list16, el16)
									}
									rec11.Tags = list16
								} else if p16.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "tags", "arr(obj{id:opt(float),label:opt(string),post_id:opt(float)})", p16.ActualWireType, p16.Raw)
								} else if p16.Kind == probeNull {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "tags", "arr(obj{id:opt(float),label:opt(string),post_id:opt(float)})", p16.ActualWireType, p16.Raw)
								} else {
									return nil, deMissingField("UsersWithPostsRow_posts", "tags", "arr(obj{id:opt(float),label:opt(string),post_id:opt(float)})")
								}
								p21 := p11.Got.ProbeString("title")
								if p21.Kind == probeGot {
									sv21 := p21.Got
									rec11.Title = &sv21
								} else if p21.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithPostsRow_posts", "title", "opt(string)", p21.ActualWireType, p21.Raw)
								}
								el10 = rec11
							} else if p11.Kind == probeWrong {
								return nil, deTypeMismatch("UsersWithPostsRow", "posts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)}", p11.ActualWireType, p11.Raw)
							} else if p11.Kind == probeNull {
								return nil, deTypeMismatch("UsersWithPostsRow", "posts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)}", p11.ActualWireType, p11.Raw)
							} else {
								return nil, deMissingField("UsersWithPostsRow", "posts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)}")
							}
							list10 = append(list10, el10)
						}
						rec7.Posts = list10
					} else if p10.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithPostsRow", "posts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})", p10.ActualWireType, p10.Raw)
					} else if p10.Kind == probeNull {
						return nil, deTypeMismatch("UsersWithPostsRow", "posts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})", p10.ActualWireType, p10.Raw)
					} else {
						return nil, deMissingField("UsersWithPostsRow", "posts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})")
					}
					el6 = rec7
				} else if p7.Kind == probeWrong {
					return nil, deTypeMismatch("n6", "n6", "obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})}", p7.ActualWireType, p7.Raw)
				} else if p7.Kind == probeNull {
					return nil, deTypeMismatch("n6", "n6", "obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})}", p7.ActualWireType, p7.Raw)
				} else {
					return nil, deMissingField("n6", "n6", "obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})}")
				}
				list6 = append(list6, el6)
			}
			t_n6 = list6
		} else if p6.Kind == probeWrong {
			return nil, deTypeMismatch("n6", "n6", "arr(obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})})", p6.ActualWireType, p6.Raw)
		} else if p6.Kind == probeNull {
			return nil, deTypeMismatch("n6", "n6", "arr(obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})})", p6.ActualWireType, p6.Raw)
		} else {
			return nil, deMissingField("n6", "n6", "arr(obj{id:opt(float),name:opt(string),posts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),tags:arr(obj{id:opt(float),label:opt(string),post_id:opt(float)}),title:opt(string)})})")
		}
		produced_n6 = true
	}
	return t_n6, nil
}

// PostsWithAuthor — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func PostsWithAuthor() ([]PostsWithAuthorRow, error) {
	var t_n0 []wire.WireValue
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	var t_n1 []wire.WireValue
	produced_n1 := false
	_ = t_n1
	_ = produced_n1
	var t_n2 []wire.WireValue
	produced_n2 := false
	_ = t_n2
	_ = produced_n2
	var t_n3 []PostsWithAuthorRow
	produced_n3 := false
	_ = t_n3
	_ = produced_n3
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status, created_at FROM conf_posts ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		t_n0 = wire_n0.Items
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(value)")
	}
	produced_n0 = true
	// ── op 'n1' (pluck, parent:n0) ──
	if produced_n0 {
		payload_n1 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "col", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"author_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "rows", Val: wire.WireListOf(t_n0)}})
		wire_n1, wire_n1Err := litedbmodel_runtime.PluckKeys(payload_n1)
		if wire_n1Err != nil {
			return nil, opFailed("n1", "fail", wire_n1Err)
		}
		p1 := wire_n1.AsList()
		if p1.Kind == probeGot {
			t_n1 = wire_n1.Items
		} else if p1.Kind == probeWrong {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else if p1.Kind == probeNull {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else {
			return nil, deMissingField("n1", "n1", "arr(value)")
		}
		produced_n1 = true
	}
	// ── op 'n2' (executeSQL, parent:n1) ──
	if produced_n1 {
		payload_n2 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireListOf(t_n1)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, name, post_count FROM conf_users WHERE id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt)")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
		wire_n2, wire_n2Err := litedbmodel_runtime.ExecuteSQL(payload_n2)
		if wire_n2Err != nil {
			return nil, opFailed("n2", "fail", wire_n2Err)
		}
		p2 := wire_n2.AsList()
		if p2.Kind == probeGot {
			t_n2 = wire_n2.Items
		} else if p2.Kind == probeWrong {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else if p2.Kind == probeNull {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else {
			return nil, deMissingField("n2", "n2", "arr(value)")
		}
		produced_n2 = true
	}
	// ── op 'n3' (group, parent:n0) ──
	if produced_n0 {
		payload_n3 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "children", Val: wire.WireListOf(t_n2)}, wire.WireField{Key: "fk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "into", Val: wire.WireStr("author")}, wire.WireField{Key: "parents", Val: wire.WireListOf(t_n0)}, wire.WireField{Key: "pk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"author_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "single", Val: wire.WireBool(true)}})
		wire_n3, wire_n3Err := litedbmodel_runtime.GroupChildren(payload_n3)
		if wire_n3Err != nil {
			return nil, opFailed("n3", "fail", wire_n3Err)
		}
		p3 := wire_n3.AsList()
		if p3.Kind == probeGot {
			list3 := make([]PostsWithAuthorRow, 0, p3.Got.Len())
			for i3 := 0; i3 < p3.Got.Len(); i3++ {
				var el3 PostsWithAuthorRow
				p4 := p3.Got.ElemRow(i3)
				if p4.Kind == probeGot {
					var rec4 PostsWithAuthorRow
					p5 := p4.Got.ProbeRow("author")
					if p5.Kind == probeGot {
						var rec5 PostsWithAuthorRow_author
						p6 := p5.Got.ProbeFloat("id")
						if p6.Kind == probeGot {
							rec5.Id = &p6.Got
						} else if p6.Kind == probeWrong {
							return nil, deTypeMismatch("PostsWithAuthorRow_author", "id", "opt(float)", p6.ActualWireType, p6.Raw)
						}
						p7 := p5.Got.ProbeString("name")
						if p7.Kind == probeGot {
							sv7 := p7.Got
							rec5.Name = &sv7
						} else if p7.Kind == probeWrong {
							return nil, deTypeMismatch("PostsWithAuthorRow_author", "name", "opt(string)", p7.ActualWireType, p7.Raw)
						}
						p8 := p5.Got.ProbeFloat("post_count")
						if p8.Kind == probeGot {
							rec5.Post_count = &p8.Got
						} else if p8.Kind == probeWrong {
							return nil, deTypeMismatch("PostsWithAuthorRow_author", "post_count", "opt(float)", p8.ActualWireType, p8.Raw)
						}
						rec4.Author = &rec5
					} else if p5.Kind == probeWrong {
						return nil, deTypeMismatch("PostsWithAuthorRow", "author", "opt(obj{id:opt(float),name:opt(string),post_count:opt(float)})", p5.ActualWireType, p5.Raw)
					}
					p9 := p4.Got.ProbeFloat("author_id")
					if p9.Kind == probeGot {
						rec4.Author_id = &p9.Got
					} else if p9.Kind == probeWrong {
						return nil, deTypeMismatch("PostsWithAuthorRow", "author_id", "opt(float)", p9.ActualWireType, p9.Raw)
					}
					p10 := p4.Got.ProbeString("created_at")
					if p10.Kind == probeGot {
						sv10 := p10.Got
						rec4.Created_at = &sv10
					} else if p10.Kind == probeWrong {
						return nil, deTypeMismatch("PostsWithAuthorRow", "created_at", "opt(string)", p10.ActualWireType, p10.Raw)
					}
					p11 := p4.Got.ProbeFloat("id")
					if p11.Kind == probeGot {
						rec4.Id = &p11.Got
					} else if p11.Kind == probeWrong {
						return nil, deTypeMismatch("PostsWithAuthorRow", "id", "opt(float)", p11.ActualWireType, p11.Raw)
					}
					p12 := p4.Got.ProbeString("status")
					if p12.Kind == probeGot {
						sv12 := p12.Got
						rec4.Status = &sv12
					} else if p12.Kind == probeWrong {
						return nil, deTypeMismatch("PostsWithAuthorRow", "status", "opt(string)", p12.ActualWireType, p12.Raw)
					}
					p13 := p4.Got.ProbeString("title")
					if p13.Kind == probeGot {
						sv13 := p13.Got
						rec4.Title = &sv13
					} else if p13.Kind == probeWrong {
						return nil, deTypeMismatch("PostsWithAuthorRow", "title", "opt(string)", p13.ActualWireType, p13.Raw)
					}
					el3 = rec4
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("n3", "n3", "obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p4.ActualWireType, p4.Raw)
				} else if p4.Kind == probeNull {
					return nil, deTypeMismatch("n3", "n3", "obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p4.ActualWireType, p4.Raw)
				} else {
					return nil, deMissingField("n3", "n3", "obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")
				}
				list3 = append(list3, el3)
			}
			t_n3 = list3
		} else if p3.Kind == probeWrong {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p3.ActualWireType, p3.Raw)
		} else if p3.Kind == probeNull {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p3.ActualWireType, p3.Raw)
		} else {
			return nil, deMissingField("n3", "n3", "arr(obj{author:opt(obj{id:opt(float),name:opt(string),post_count:opt(float)}),author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")
		}
		produced_n3 = true
	}
	return t_n3, nil
}

// UsersWithCappedPosts — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func UsersWithCappedPosts() ([]UsersWithCappedPostsRow, error) {
	var t_n0 []wire.WireValue
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	var t_n1 []wire.WireValue
	produced_n1 := false
	_ = t_n1
	_ = produced_n1
	var t_n2 []wire.WireValue
	produced_n2 := false
	_ = t_n2
	_ = produced_n2
	var t_n3 []UsersWithCappedPostsRow
	produced_n3 := false
	_ = t_n3
	_ = produced_n3
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, name FROM conf_users ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		t_n0 = wire_n0.Items
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(value)")
	}
	produced_n0 = true
	// ── op 'n1' (pluck, parent:n0) ──
	if produced_n0 {
		payload_n1 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "col", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "rows", Val: wire.WireListOf(t_n0)}})
		wire_n1, wire_n1Err := litedbmodel_runtime.PluckKeys(payload_n1)
		if wire_n1Err != nil {
			return nil, opFailed("n1", "fail", wire_n1Err)
		}
		p1 := wire_n1.AsList()
		if p1.Kind == probeGot {
			t_n1 = wire_n1.Items
		} else if p1.Kind == probeWrong {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else if p1.Kind == probeNull {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else {
			return nil, deMissingField("n1", "n1", "arr(value)")
		}
		produced_n1 = true
	}
	// ── op 'n2' (executeSQL, parent:n1) ──
	if produced_n1 {
		pf_n2, pe_n2 := func() ([]wire.WireField, error) {
			ex_17 := "conf_posts"
			ex_18 := CapGuard{Limit: int64(2), Model: &ex_17, Relation: "cappedPosts"}
			ex_19 := DynamicWherePlan{Frags: []DynamicWhereFrag{}}
			return []wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "guard", Val: func() wire.WireValue { o0 := &ex_18; if o0 == nil { return wire.WireNull() }; return wire.WireRowOf([]wire.WireField{{Key: "limit", Val: wire.WireInt((*o0).Limit)}, {Key: "model", Val: func() wire.WireValue { o1 := (*o0).Model; if o1 == nil { return wire.WireNull() }; return wire.WireStr((*o1)) }()}, {Key: "relation", Val: wire.WireStr((*o0).Relation)}}) }()}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireListOf(t_n1)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC")}, wire.WireField{Key: "whereDynamic", Val: func() wire.WireValue { o0 := &ex_19; if o0 == nil { return wire.WireNull() }; return wire.WireRowOf([]wire.WireField{{Key: "frags", Val: func() wire.WireValue { it1 := []wire.WireValue{}; for _, e1 := range (*o0).Frags { it1 = append(it1, wire.WireRowOf([]wire.WireField{{Key: "params", Val: func() wire.WireValue { it3 := []wire.WireValue{}; for _, e3 := range e1.Params { it3 = append(it3, func() wire.WireValue { o4 := e3; if o4 == nil { return wire.WireNull() }; return (*o4) }()) }; return wire.WireListOf(it3) }()}, {Key: "skipped", Val: wire.WireBool(e1.Skipped)}, {Key: "sql", Val: wire.WireStr(e1.Sql)}})) }; return wire.WireListOf(it1) }()}}) }()}, wire.WireField{Key: "write", Val: wire.WireBool(false)}}, nil
		}()
		if pe_n2 != nil {
			return nil, pe_n2
		}
		payload_n2 := wire.WireRowOfFields(pf_n2)
		wire_n2, wire_n2Err := litedbmodel_runtime.ExecuteSQL(payload_n2)
		if wire_n2Err != nil {
			return nil, opFailed("n2", "fail", wire_n2Err)
		}
		p2 := wire_n2.AsList()
		if p2.Kind == probeGot {
			t_n2 = wire_n2.Items
		} else if p2.Kind == probeWrong {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else if p2.Kind == probeNull {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else {
			return nil, deMissingField("n2", "n2", "arr(value)")
		}
		produced_n2 = true
	}
	// ── op 'n3' (group, parent:n0) ──
	if produced_n0 {
		payload_n3 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "children", Val: wire.WireListOf(t_n2)}, wire.WireField{Key: "fk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"author_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "into", Val: wire.WireStr("cappedPosts")}, wire.WireField{Key: "parents", Val: wire.WireListOf(t_n0)}, wire.WireField{Key: "pk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "single", Val: wire.WireBool(false)}})
		wire_n3, wire_n3Err := litedbmodel_runtime.GroupChildren(payload_n3)
		if wire_n3Err != nil {
			return nil, opFailed("n3", "fail", wire_n3Err)
		}
		p3 := wire_n3.AsList()
		if p3.Kind == probeGot {
			list3 := make([]UsersWithCappedPostsRow, 0, p3.Got.Len())
			for i3 := 0; i3 < p3.Got.Len(); i3++ {
				var el3 UsersWithCappedPostsRow
				p4 := p3.Got.ElemRow(i3)
				if p4.Kind == probeGot {
					var rec4 UsersWithCappedPostsRow
					p5 := p4.Got.ProbeList("cappedPosts")
					if p5.Kind == probeGot {
						list5 := make([]UsersWithCappedPostsRow_cappedPosts, 0, p5.Got.Len())
						for i5 := 0; i5 < p5.Got.Len(); i5++ {
							var el5 UsersWithCappedPostsRow_cappedPosts
							p6 := p5.Got.ElemRow(i5)
							if p6.Kind == probeGot {
								var rec6 UsersWithCappedPostsRow_cappedPosts
								p7 := p6.Got.ProbeFloat("author_id")
								if p7.Kind == probeGot {
									rec6.Author_id = &p7.Got
								} else if p7.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithCappedPostsRow_cappedPosts", "author_id", "opt(float)", p7.ActualWireType, p7.Raw)
								}
								p8 := p6.Got.ProbeString("created_at")
								if p8.Kind == probeGot {
									sv8 := p8.Got
									rec6.Created_at = &sv8
								} else if p8.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithCappedPostsRow_cappedPosts", "created_at", "opt(string)", p8.ActualWireType, p8.Raw)
								}
								p9 := p6.Got.ProbeFloat("id")
								if p9.Kind == probeGot {
									rec6.Id = &p9.Got
								} else if p9.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithCappedPostsRow_cappedPosts", "id", "opt(float)", p9.ActualWireType, p9.Raw)
								}
								p10 := p6.Got.ProbeString("status")
								if p10.Kind == probeGot {
									sv10 := p10.Got
									rec6.Status = &sv10
								} else if p10.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithCappedPostsRow_cappedPosts", "status", "opt(string)", p10.ActualWireType, p10.Raw)
								}
								p11 := p6.Got.ProbeString("title")
								if p11.Kind == probeGot {
									sv11 := p11.Got
									rec6.Title = &sv11
								} else if p11.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithCappedPostsRow_cappedPosts", "title", "opt(string)", p11.ActualWireType, p11.Raw)
								}
								el5 = rec6
							} else if p6.Kind == probeWrong {
								return nil, deTypeMismatch("UsersWithCappedPostsRow", "cappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p6.ActualWireType, p6.Raw)
							} else if p6.Kind == probeNull {
								return nil, deTypeMismatch("UsersWithCappedPostsRow", "cappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p6.ActualWireType, p6.Raw)
							} else {
								return nil, deMissingField("UsersWithCappedPostsRow", "cappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")
							}
							list5 = append(list5, el5)
						}
						rec4.CappedPosts = list5
					} else if p5.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithCappedPostsRow", "cappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p5.ActualWireType, p5.Raw)
					} else if p5.Kind == probeNull {
						return nil, deTypeMismatch("UsersWithCappedPostsRow", "cappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p5.ActualWireType, p5.Raw)
					} else {
						return nil, deMissingField("UsersWithCappedPostsRow", "cappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")
					}
					p12 := p4.Got.ProbeFloat("id")
					if p12.Kind == probeGot {
						rec4.Id = &p12.Got
					} else if p12.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithCappedPostsRow", "id", "opt(float)", p12.ActualWireType, p12.Raw)
					}
					p13 := p4.Got.ProbeString("name")
					if p13.Kind == probeGot {
						sv13 := p13.Got
						rec4.Name = &sv13
					} else if p13.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithCappedPostsRow", "name", "opt(string)", p13.ActualWireType, p13.Raw)
					}
					el3 = rec4
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("n3", "n3", "obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)}", p4.ActualWireType, p4.Raw)
				} else if p4.Kind == probeNull {
					return nil, deTypeMismatch("n3", "n3", "obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)}", p4.ActualWireType, p4.Raw)
				} else {
					return nil, deMissingField("n3", "n3", "obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)}")
				}
				list3 = append(list3, el3)
			}
			t_n3 = list3
		} else if p3.Kind == probeWrong {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)})", p3.ActualWireType, p3.Raw)
		} else if p3.Kind == probeNull {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)})", p3.ActualWireType, p3.Raw)
		} else {
			return nil, deMissingField("n3", "n3", "arr(obj{cappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}),id:opt(float),name:opt(string)})")
		}
		produced_n3 = true
	}
	return t_n3, nil
}

// UsersWithUncappedPosts — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func UsersWithUncappedPosts() ([]UsersWithUncappedPostsRow, error) {
	var t_n0 []wire.WireValue
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	var t_n1 []wire.WireValue
	produced_n1 := false
	_ = t_n1
	_ = produced_n1
	var t_n2 []wire.WireValue
	produced_n2 := false
	_ = t_n2
	_ = produced_n2
	var t_n3 []UsersWithUncappedPostsRow
	produced_n3 := false
	_ = t_n3
	_ = produced_n3
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, name FROM conf_users ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		t_n0 = wire_n0.Items
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(value)")
	}
	produced_n0 = true
	// ── op 'n1' (pluck, parent:n0) ──
	if produced_n0 {
		payload_n1 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "col", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "rows", Val: wire.WireListOf(t_n0)}})
		wire_n1, wire_n1Err := litedbmodel_runtime.PluckKeys(payload_n1)
		if wire_n1Err != nil {
			return nil, opFailed("n1", "fail", wire_n1Err)
		}
		p1 := wire_n1.AsList()
		if p1.Kind == probeGot {
			t_n1 = wire_n1.Items
		} else if p1.Kind == probeWrong {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else if p1.Kind == probeNull {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else {
			return nil, deMissingField("n1", "n1", "arr(value)")
		}
		produced_n1 = true
	}
	// ── op 'n2' (executeSQL, parent:n1) ──
	if produced_n1 {
		payload_n2 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireListOf(t_n1)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, author_id, title, status, created_at FROM conf_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
		wire_n2, wire_n2Err := litedbmodel_runtime.ExecuteSQL(payload_n2)
		if wire_n2Err != nil {
			return nil, opFailed("n2", "fail", wire_n2Err)
		}
		p2 := wire_n2.AsList()
		if p2.Kind == probeGot {
			t_n2 = wire_n2.Items
		} else if p2.Kind == probeWrong {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else if p2.Kind == probeNull {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else {
			return nil, deMissingField("n2", "n2", "arr(value)")
		}
		produced_n2 = true
	}
	// ── op 'n3' (group, parent:n0) ──
	if produced_n0 {
		payload_n3 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "children", Val: wire.WireListOf(t_n2)}, wire.WireField{Key: "fk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"author_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "into", Val: wire.WireStr("uncappedPosts")}, wire.WireField{Key: "parents", Val: wire.WireListOf(t_n0)}, wire.WireField{Key: "pk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "single", Val: wire.WireBool(false)}})
		wire_n3, wire_n3Err := litedbmodel_runtime.GroupChildren(payload_n3)
		if wire_n3Err != nil {
			return nil, opFailed("n3", "fail", wire_n3Err)
		}
		p3 := wire_n3.AsList()
		if p3.Kind == probeGot {
			list3 := make([]UsersWithUncappedPostsRow, 0, p3.Got.Len())
			for i3 := 0; i3 < p3.Got.Len(); i3++ {
				var el3 UsersWithUncappedPostsRow
				p4 := p3.Got.ElemRow(i3)
				if p4.Kind == probeGot {
					var rec4 UsersWithUncappedPostsRow
					p5 := p4.Got.ProbeFloat("id")
					if p5.Kind == probeGot {
						rec4.Id = &p5.Got
					} else if p5.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithUncappedPostsRow", "id", "opt(float)", p5.ActualWireType, p5.Raw)
					}
					p6 := p4.Got.ProbeString("name")
					if p6.Kind == probeGot {
						sv6 := p6.Got
						rec4.Name = &sv6
					} else if p6.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithUncappedPostsRow", "name", "opt(string)", p6.ActualWireType, p6.Raw)
					}
					p7 := p4.Got.ProbeList("uncappedPosts")
					if p7.Kind == probeGot {
						list7 := make([]UsersWithUncappedPostsRow_uncappedPosts, 0, p7.Got.Len())
						for i7 := 0; i7 < p7.Got.Len(); i7++ {
							var el7 UsersWithUncappedPostsRow_uncappedPosts
							p8 := p7.Got.ElemRow(i7)
							if p8.Kind == probeGot {
								var rec8 UsersWithUncappedPostsRow_uncappedPosts
								p9 := p8.Got.ProbeFloat("author_id")
								if p9.Kind == probeGot {
									rec8.Author_id = &p9.Got
								} else if p9.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithUncappedPostsRow_uncappedPosts", "author_id", "opt(float)", p9.ActualWireType, p9.Raw)
								}
								p10 := p8.Got.ProbeString("created_at")
								if p10.Kind == probeGot {
									sv10 := p10.Got
									rec8.Created_at = &sv10
								} else if p10.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithUncappedPostsRow_uncappedPosts", "created_at", "opt(string)", p10.ActualWireType, p10.Raw)
								}
								p11 := p8.Got.ProbeFloat("id")
								if p11.Kind == probeGot {
									rec8.Id = &p11.Got
								} else if p11.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithUncappedPostsRow_uncappedPosts", "id", "opt(float)", p11.ActualWireType, p11.Raw)
								}
								p12 := p8.Got.ProbeString("status")
								if p12.Kind == probeGot {
									sv12 := p12.Got
									rec8.Status = &sv12
								} else if p12.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithUncappedPostsRow_uncappedPosts", "status", "opt(string)", p12.ActualWireType, p12.Raw)
								}
								p13 := p8.Got.ProbeString("title")
								if p13.Kind == probeGot {
									sv13 := p13.Got
									rec8.Title = &sv13
								} else if p13.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithUncappedPostsRow_uncappedPosts", "title", "opt(string)", p13.ActualWireType, p13.Raw)
								}
								el7 = rec8
							} else if p8.Kind == probeWrong {
								return nil, deTypeMismatch("UsersWithUncappedPostsRow", "uncappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p8.ActualWireType, p8.Raw)
							} else if p8.Kind == probeNull {
								return nil, deTypeMismatch("UsersWithUncappedPostsRow", "uncappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p8.ActualWireType, p8.Raw)
							} else {
								return nil, deMissingField("UsersWithUncappedPostsRow", "uncappedPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")
							}
							list7 = append(list7, el7)
						}
						rec4.UncappedPosts = list7
					} else if p7.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithUncappedPostsRow", "uncappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p7.ActualWireType, p7.Raw)
					} else if p7.Kind == probeNull {
						return nil, deTypeMismatch("UsersWithUncappedPostsRow", "uncappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p7.ActualWireType, p7.Raw)
					} else {
						return nil, deMissingField("UsersWithUncappedPostsRow", "uncappedPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")
					}
					el3 = rec4
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("n3", "n3", "obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}", p4.ActualWireType, p4.Raw)
				} else if p4.Kind == probeNull {
					return nil, deTypeMismatch("n3", "n3", "obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}", p4.ActualWireType, p4.Raw)
				} else {
					return nil, deMissingField("n3", "n3", "obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}")
				}
				list3 = append(list3, el3)
			}
			t_n3 = list3
		} else if p3.Kind == probeWrong {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})", p3.ActualWireType, p3.Raw)
		} else if p3.Kind == probeNull {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})", p3.ActualWireType, p3.Raw)
		} else {
			return nil, deMissingField("n3", "n3", "arr(obj{id:opt(float),name:opt(string),uncappedPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})")
		}
		produced_n3 = true
	}
	return t_n3, nil
}

// UsersWithTopPosts — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func UsersWithTopPosts() ([]UsersWithTopPostsRow, error) {
	var t_n0 []wire.WireValue
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	var t_n1 []wire.WireValue
	produced_n1 := false
	_ = t_n1
	_ = produced_n1
	var t_n2 []wire.WireValue
	produced_n2 := false
	_ = t_n2
	_ = produced_n2
	var t_n3 []UsersWithTopPostsRow
	produced_n3 := false
	_ = t_n3
	_ = produced_n3
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, name FROM conf_users ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		t_n0 = wire_n0.Items
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(value)", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(value)")
	}
	produced_n0 = true
	// ── op 'n1' (pluck, parent:n0) ──
	if produced_n0 {
		payload_n1 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "col", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "rows", Val: wire.WireListOf(t_n0)}})
		wire_n1, wire_n1Err := litedbmodel_runtime.PluckKeys(payload_n1)
		if wire_n1Err != nil {
			return nil, opFailed("n1", "fail", wire_n1Err)
		}
		p1 := wire_n1.AsList()
		if p1.Kind == probeGot {
			t_n1 = wire_n1.Items
		} else if p1.Kind == probeWrong {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else if p1.Kind == probeNull {
			return nil, deTypeMismatch("n1", "n1", "arr(value)", p1.ActualWireType, p1.Raw)
		} else {
			return nil, deMissingField("n1", "n1", "arr(value)")
		}
		produced_n1 = true
	}
	// ── op 'n2' (executeSQL, parent:n1) ──
	if produced_n1 {
		payload_n2 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireListOf(t_n1)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("WITH ranked AS (SELECT *, ROW_NUMBER() OVER (PARTITION BY author_id ORDER BY id ASC) AS _rn FROM conf_posts WHERE author_id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt)) SELECT * FROM ranked WHERE _rn <= 1")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
		wire_n2, wire_n2Err := litedbmodel_runtime.ExecuteSQL(payload_n2)
		if wire_n2Err != nil {
			return nil, opFailed("n2", "fail", wire_n2Err)
		}
		p2 := wire_n2.AsList()
		if p2.Kind == probeGot {
			t_n2 = wire_n2.Items
		} else if p2.Kind == probeWrong {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else if p2.Kind == probeNull {
			return nil, deTypeMismatch("n2", "n2", "arr(value)", p2.ActualWireType, p2.Raw)
		} else {
			return nil, deMissingField("n2", "n2", "arr(value)")
		}
		produced_n2 = true
	}
	// ── op 'n3' (group, parent:n0) ──
	if produced_n0 {
		payload_n3 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "children", Val: wire.WireListOf(t_n2)}, wire.WireField{Key: "fk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"author_id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "into", Val: wire.WireStr("topPosts")}, wire.WireField{Key: "parents", Val: wire.WireListOf(t_n0)}, wire.WireField{Key: "pk", Val: func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range []string{"id"} { it0 = append(it0, wire.WireStr(e0)) }; return wire.WireListOf(it0) }()}, wire.WireField{Key: "single", Val: wire.WireBool(false)}})
		wire_n3, wire_n3Err := litedbmodel_runtime.GroupChildren(payload_n3)
		if wire_n3Err != nil {
			return nil, opFailed("n3", "fail", wire_n3Err)
		}
		p3 := wire_n3.AsList()
		if p3.Kind == probeGot {
			list3 := make([]UsersWithTopPostsRow, 0, p3.Got.Len())
			for i3 := 0; i3 < p3.Got.Len(); i3++ {
				var el3 UsersWithTopPostsRow
				p4 := p3.Got.ElemRow(i3)
				if p4.Kind == probeGot {
					var rec4 UsersWithTopPostsRow
					p5 := p4.Got.ProbeFloat("id")
					if p5.Kind == probeGot {
						rec4.Id = &p5.Got
					} else if p5.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithTopPostsRow", "id", "opt(float)", p5.ActualWireType, p5.Raw)
					}
					p6 := p4.Got.ProbeString("name")
					if p6.Kind == probeGot {
						sv6 := p6.Got
						rec4.Name = &sv6
					} else if p6.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithTopPostsRow", "name", "opt(string)", p6.ActualWireType, p6.Raw)
					}
					p7 := p4.Got.ProbeList("topPosts")
					if p7.Kind == probeGot {
						list7 := make([]UsersWithTopPostsRow_topPosts, 0, p7.Got.Len())
						for i7 := 0; i7 < p7.Got.Len(); i7++ {
							var el7 UsersWithTopPostsRow_topPosts
							p8 := p7.Got.ElemRow(i7)
							if p8.Kind == probeGot {
								var rec8 UsersWithTopPostsRow_topPosts
								p9 := p8.Got.ProbeFloat("author_id")
								if p9.Kind == probeGot {
									rec8.Author_id = &p9.Got
								} else if p9.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithTopPostsRow_topPosts", "author_id", "opt(float)", p9.ActualWireType, p9.Raw)
								}
								p10 := p8.Got.ProbeString("created_at")
								if p10.Kind == probeGot {
									sv10 := p10.Got
									rec8.Created_at = &sv10
								} else if p10.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithTopPostsRow_topPosts", "created_at", "opt(string)", p10.ActualWireType, p10.Raw)
								}
								p11 := p8.Got.ProbeFloat("id")
								if p11.Kind == probeGot {
									rec8.Id = &p11.Got
								} else if p11.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithTopPostsRow_topPosts", "id", "opt(float)", p11.ActualWireType, p11.Raw)
								}
								p12 := p8.Got.ProbeString("status")
								if p12.Kind == probeGot {
									sv12 := p12.Got
									rec8.Status = &sv12
								} else if p12.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithTopPostsRow_topPosts", "status", "opt(string)", p12.ActualWireType, p12.Raw)
								}
								p13 := p8.Got.ProbeString("title")
								if p13.Kind == probeGot {
									sv13 := p13.Got
									rec8.Title = &sv13
								} else if p13.Kind == probeWrong {
									return nil, deTypeMismatch("UsersWithTopPostsRow_topPosts", "title", "opt(string)", p13.ActualWireType, p13.Raw)
								}
								el7 = rec8
							} else if p8.Kind == probeWrong {
								return nil, deTypeMismatch("UsersWithTopPostsRow", "topPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p8.ActualWireType, p8.Raw)
							} else if p8.Kind == probeNull {
								return nil, deTypeMismatch("UsersWithTopPostsRow", "topPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}", p8.ActualWireType, p8.Raw)
							} else {
								return nil, deMissingField("UsersWithTopPostsRow", "topPosts", "obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)}")
							}
							list7 = append(list7, el7)
						}
						rec4.TopPosts = list7
					} else if p7.Kind == probeWrong {
						return nil, deTypeMismatch("UsersWithTopPostsRow", "topPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p7.ActualWireType, p7.Raw)
					} else if p7.Kind == probeNull {
						return nil, deTypeMismatch("UsersWithTopPostsRow", "topPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})", p7.ActualWireType, p7.Raw)
					} else {
						return nil, deMissingField("UsersWithTopPostsRow", "topPosts", "arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})")
					}
					el3 = rec4
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("n3", "n3", "obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}", p4.ActualWireType, p4.Raw)
				} else if p4.Kind == probeNull {
					return nil, deTypeMismatch("n3", "n3", "obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}", p4.ActualWireType, p4.Raw)
				} else {
					return nil, deMissingField("n3", "n3", "obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})}")
				}
				list3 = append(list3, el3)
			}
			t_n3 = list3
		} else if p3.Kind == probeWrong {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})", p3.ActualWireType, p3.Raw)
		} else if p3.Kind == probeNull {
			return nil, deTypeMismatch("n3", "n3", "arr(obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})", p3.ActualWireType, p3.Raw)
		} else {
			return nil, deMissingField("n3", "n3", "arr(obj{id:opt(float),name:opt(string),topPosts:arr(obj{author_id:opt(float),created_at:opt(string),id:opt(float),status:opt(string),title:opt(string)})})")
		}
		produced_n3 = true
	}
	return t_n3, nil
}

// CreatePost — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func CreatePost(id int64, authorId int64, title string, status string, createdAt string) ([]WriteSummary, error) {
	var t_n0 []WriteSummary
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(authorId), wire.WireStr(createdAt), wire.WireInt(id), wire.WireStr(status), wire.WireStr(title)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("INSERT INTO conf_posts (author_id, created_at, id, status, title) VALUES (?, ?, ?, ?, ?)")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]WriteSummary, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 WriteSummary
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 WriteSummary
				p2 := p1.Got.ProbeInt("changes")
				if p2.Kind == probeGot {
					rec1.Changes = p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else if p2.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "changes", "int")
				}
				p3 := p1.Got.ProbeInt("lastInsertRowid")
				if p3.Kind == probeGot {
					rec1.LastInsertRowid = p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else if p3.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "lastInsertRowid", "int")
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{changes:int,lastInsertRowid:int}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RenamePost — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RenamePost(title string, id int64) ([]WriteSummary, error) {
	var t_n0 []WriteSummary
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireStr(title), wire.WireInt(id)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("UPDATE conf_posts SET title = ? WHERE id = ?")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]WriteSummary, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 WriteSummary
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 WriteSummary
				p2 := p1.Got.ProbeInt("changes")
				if p2.Kind == probeGot {
					rec1.Changes = p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else if p2.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "changes", "int")
				}
				p3 := p1.Got.ProbeInt("lastInsertRowid")
				if p3.Kind == probeGot {
					rec1.LastInsertRowid = p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else if p3.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "lastInsertRowid", "int")
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{changes:int,lastInsertRowid:int}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RemovePost — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RemovePost(id int64) ([]WriteSummary, error) {
	var t_n0 []WriteSummary
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(id)})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("DELETE FROM conf_posts WHERE id = ?")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]WriteSummary, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 WriteSummary
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 WriteSummary
				p2 := p1.Got.ProbeInt("changes")
				if p2.Kind == probeGot {
					rec1.Changes = p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else if p2.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "changes", "int")
				}
				p3 := p1.Got.ProbeInt("lastInsertRowid")
				if p3.Kind == probeGot {
					rec1.LastInsertRowid = p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else if p3.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "lastInsertRowid", "int")
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{changes:int,lastInsertRowid:int}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")
	}
	produced_n0 = true
	return t_n0, nil
}

// CreatePostReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func CreatePostReturning(id int64, authorId int64, title string, status string, createdAt string) ([]CreatePostReturningRow, error) {
	var t_n0 []CreatePostReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(authorId), wire.WireStr(createdAt), wire.WireInt(id), wire.WireStr(status), wire.WireStr(title)})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("INSERT INTO conf_posts (author_id, created_at, id, status, title) VALUES (?, ?, ?, ?, ?) RETURNING id, title /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]CreatePostReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 CreatePostReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 CreatePostReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("CreatePostReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("title")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Title = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("CreatePostReturningRow", "title", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RenamePostReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RenamePostReturning(title string, id int64) ([]RenamePostReturningRow, error) {
	var t_n0 []RenamePostReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireStr(title), wire.WireInt(id)})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("UPDATE conf_posts SET title = ? WHERE id = ? RETURNING id, title /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]RenamePostReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 RenamePostReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 RenamePostReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("RenamePostReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("title")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Title = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("RenamePostReturningRow", "title", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RemovePostReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RemovePostReturning(id int64) ([]RemovePostReturningRow, error) {
	var t_n0 []RemovePostReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(id)})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("DELETE FROM conf_posts WHERE id = ? RETURNING id, title /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]RemovePostReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 RemovePostReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 RemovePostReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("RemovePostReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("title")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Title = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("RemovePostReturningRow", "title", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RestatusPostsReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RestatusPostsReturning(status string, authorId int64) ([]RestatusPostsReturningRow, error) {
	var t_n0 []RestatusPostsReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireStr(status), wire.WireInt(authorId)})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("UPDATE conf_posts SET status = ? WHERE author_id = ? RETURNING id, status /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]RestatusPostsReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 RestatusPostsReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 RestatusPostsReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("RestatusPostsReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("status")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Status = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("RestatusPostsReturningRow", "status", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),status:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),status:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),status:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),status:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),status:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),status:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RemovePostsByAuthorReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RemovePostsByAuthorReturning(authorId int64) ([]RemovePostsByAuthorReturningRow, error) {
	var t_n0 []RemovePostsByAuthorReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{wire.WireInt(authorId)})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("DELETE FROM conf_posts WHERE author_id = ? RETURNING id, title /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]RemovePostsByAuthorReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 RemovePostsByAuthorReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 RemovePostsByAuthorReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("RemovePostsByAuthorReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("title")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Title = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("RemovePostsByAuthorReturningRow", "title", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),title:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),title:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),title:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// TypedRows — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func TypedRows() ([]TypedRowsRow, error) {
	var t_n0 []TypedRowsRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("SELECT id, ts, flag, label FROM conf_typed ORDER BY id ASC")}, wire.WireField{Key: "write", Val: wire.WireBool(false)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]TypedRowsRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 TypedRowsRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 TypedRowsRow
				p2 := p1.Got.ProbeFloat("flag")
				if p2.Kind == probeGot {
					rec1.Flag = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("TypedRowsRow", "flag", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeFloat("id")
				if p3.Kind == probeGot {
					rec1.Id = &p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("TypedRowsRow", "id", "opt(float)", p3.ActualWireType, p3.Raw)
				}
				p4 := p1.Got.ProbeString("label")
				if p4.Kind == probeGot {
					sv4 := p4.Got
					rec1.Label = &sv4
				} else if p4.Kind == probeWrong {
					return nil, deTypeMismatch("TypedRowsRow", "label", "opt(string)", p4.ActualWireType, p4.Raw)
				}
				p5 := p1.Got.ProbeString("ts")
				if p5.Kind == probeGot {
					sv5 := p5.Got
					rec1.Ts = &sv5
				} else if p5.Kind == probeWrong {
					return nil, deTypeMismatch("TypedRowsRow", "ts", "opt(string)", p5.ActualWireType, p5.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{flag:opt(float),id:opt(float),label:opt(string),ts:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// CreateTags — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func CreateTags(rows []CreateTagsRecord) ([]WriteSummary, error) {
	var t_n0 []WriteSummary
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range rows { it0 = append(it0, wire.WireRowOf([]wire.WireField{{Key: "id", Val: wire.WireInt(e0.Id)}, {Key: "label", Val: wire.WireStr(e0.Label)}, {Key: "post_id", Val: wire.WireInt(e0.Post_id)}})) }; return wire.WireListOf(it0) }()})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("INSERT INTO conf_tags (id, label, post_id) SELECT JSON_UNQUOTE(jt.id), JSON_UNQUOTE(jt.label), JSON_UNQUOTE(jt.post_id) FROM JSON_TABLE(?, '$[*]' COLUMNS(id JSON PATH '$.id', label JSON PATH '$.label', post_id JSON PATH '$.post_id')) jt")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]WriteSummary, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 WriteSummary
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 WriteSummary
				p2 := p1.Got.ProbeInt("changes")
				if p2.Kind == probeGot {
					rec1.Changes = p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else if p2.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "changes", "int")
				}
				p3 := p1.Got.ProbeInt("lastInsertRowid")
				if p3.Kind == probeGot {
					rec1.LastInsertRowid = p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else if p3.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "lastInsertRowid", "int")
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{changes:int,lastInsertRowid:int}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RemoveTags — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RemoveTags(ids []int64) ([]WriteSummary, error) {
	var t_n0 []WriteSummary
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range ids { it0 = append(it0, wire.WireInt(e0)) }; return wire.WireListOf(it0) }()})}, wire.WireField{Key: "returning", Val: wire.WireBool(false)}, wire.WireField{Key: "sql", Val: wire.WireStr("DELETE FROM conf_tags WHERE id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt)")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]WriteSummary, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 WriteSummary
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 WriteSummary
				p2 := p1.Got.ProbeInt("changes")
				if p2.Kind == probeGot {
					rec1.Changes = p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else if p2.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "changes", "int", p2.ActualWireType, p2.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "changes", "int")
				}
				p3 := p1.Got.ProbeInt("lastInsertRowid")
				if p3.Kind == probeGot {
					rec1.LastInsertRowid = p3.Got
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else if p3.Kind == probeNull {
					return nil, deTypeMismatch("WriteSummary", "lastInsertRowid", "int", p3.ActualWireType, p3.Raw)
				} else {
					return nil, deMissingField("WriteSummary", "lastInsertRowid", "int")
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{changes:int,lastInsertRowid:int}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{changes:int,lastInsertRowid:int}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{changes:int,lastInsertRowid:int})")
	}
	produced_n0 = true
	return t_n0, nil
}

// CreateTagsReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func CreateTagsReturning(rows []CreateTagsReturningRecord) ([]CreateTagsReturningRow, error) {
	var t_n0 []CreateTagsReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range rows { it0 = append(it0, wire.WireRowOf([]wire.WireField{{Key: "id", Val: wire.WireInt(e0.Id)}, {Key: "label", Val: wire.WireStr(e0.Label)}, {Key: "post_id", Val: wire.WireInt(e0.Post_id)}})) }; return wire.WireListOf(it0) }()})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("INSERT INTO conf_tags (id, label, post_id) SELECT JSON_UNQUOTE(jt.id), JSON_UNQUOTE(jt.label), JSON_UNQUOTE(jt.post_id) FROM JSON_TABLE(?, '$[*]' COLUMNS(id JSON PATH '$.id', label JSON PATH '$.label', post_id JSON PATH '$.post_id')) jt RETURNING id, label /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]CreateTagsReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 CreateTagsReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 CreateTagsReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("CreateTagsReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("label")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Label = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("CreateTagsReturningRow", "label", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),label:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RelabelTagsReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RelabelTagsReturning(rows []RelabelTagsReturningRecord) ([]RelabelTagsReturningRow, error) {
	var t_n0 []RelabelTagsReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range rows { it0 = append(it0, wire.WireRowOf([]wire.WireField{{Key: "id", Val: wire.WireInt(e0.Id)}, {Key: "label", Val: wire.WireStr(e0.Label)}})) }; return wire.WireListOf(it0) }()})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("UPDATE conf_tags AS u JOIN JSON_TABLE(?, '$[*]' COLUMNS(id JSON PATH '$.id', label JSON PATH '$.label')) AS v ON u.id = JSON_UNQUOTE(v.id) SET u.label = JSON_UNQUOTE(v.label) RETURNING id, label /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]RelabelTagsReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 RelabelTagsReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 RelabelTagsReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("RelabelTagsReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("label")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Label = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("RelabelTagsReturningRow", "label", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),label:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// RemoveTagsReturning — the STRUCT-RETURNING combined read (bc#77/#87): the fully
// de-plumbed path. Ports are a native struct (direct field assignment); the handler
// result is materialized straight into the node's outType struct. Node results are typed
// struct locals; a relation child reads the parent's REAL struct result via direct field
// access (child-present decision from the real parent value — relation / connection
// converge). A strictly-sequential plan is a plain statement list (no plan driver); a
// real-concurrency stage (bc#87) is EXPLICIT static parallel orchestration — one goroutine
// per statically-known parallel op, bounded by the static plan.concurrency via a semaphore
// of known size, with a sync.WaitGroup; preflight + interpret are committed in ascending
// index order so the observed value / op multiset / failure precedence byte-match run_behavior
// (the goroutine spawn is the ONLY runtime element — the WHAT is baked). The output is a typed
// struct/value assembled by struct literal + field access — the consumer keeps it native.
func RemoveTagsReturning(ids []int64) ([]RemoveTagsReturningRow, error) {
	var t_n0 []RemoveTagsReturningRow
	produced_n0 := false
	_ = t_n0
	_ = produced_n0
	// ── op 'n0' (executeSQL) ──
	payload_n0 := wire.WireRowOfFields([]wire.WireField{wire.WireField{Key: "bigint", Val: wire.WireBool(false)}, wire.WireField{Key: "params", Val: wire.WireListOf([]wire.WireValue{func() wire.WireValue { it0 := []wire.WireValue{}; for _, e0 := range ids { it0 = append(it0, wire.WireInt(e0)) }; return wire.WireListOf(it0) }()})}, wire.WireField{Key: "returning", Val: wire.WireBool(true)}, wire.WireField{Key: "sql", Val: wire.WireStr("DELETE FROM conf_tags WHERE id IN (SELECT JSON_UNQUOTE(v) FROM JSON_TABLE(?, '$[*]' COLUMNS(v JSON PATH '$')) jt) RETURNING id, label /*scp:pk=id;ai=*/")}, wire.WireField{Key: "write", Val: wire.WireBool(true)}})
	wire_n0, wire_n0Err := litedbmodel_runtime.ExecuteSQL(payload_n0)
	if wire_n0Err != nil {
		return nil, opFailed("n0", "fail", wire_n0Err)
	}
	p0 := wire_n0.AsList()
	if p0.Kind == probeGot {
		list0 := make([]RemoveTagsReturningRow, 0, p0.Got.Len())
		for i0 := 0; i0 < p0.Got.Len(); i0++ {
			var el0 RemoveTagsReturningRow
			p1 := p0.Got.ElemRow(i0)
			if p1.Kind == probeGot {
				var rec1 RemoveTagsReturningRow
				p2 := p1.Got.ProbeFloat("id")
				if p2.Kind == probeGot {
					rec1.Id = &p2.Got
				} else if p2.Kind == probeWrong {
					return nil, deTypeMismatch("RemoveTagsReturningRow", "id", "opt(float)", p2.ActualWireType, p2.Raw)
				}
				p3 := p1.Got.ProbeString("label")
				if p3.Kind == probeGot {
					sv3 := p3.Got
					rec1.Label = &sv3
				} else if p3.Kind == probeWrong {
					return nil, deTypeMismatch("RemoveTagsReturningRow", "label", "opt(string)", p3.ActualWireType, p3.Raw)
				}
				el0 = rec1
			} else if p1.Kind == probeWrong {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", p1.ActualWireType, p1.Raw)
			} else if p1.Kind == probeNull {
				return nil, deTypeMismatch("n0", "n0", "obj{id:opt(float),label:opt(string)}", p1.ActualWireType, p1.Raw)
			} else {
				return nil, deMissingField("n0", "n0", "obj{id:opt(float),label:opt(string)}")
			}
			list0 = append(list0, el0)
		}
		t_n0 = list0
	} else if p0.Kind == probeWrong {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", p0.ActualWireType, p0.Raw)
	} else if p0.Kind == probeNull {
		return nil, deTypeMismatch("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})", p0.ActualWireType, p0.Raw)
	} else {
		return nil, deMissingField("n0", "n0", "arr(obj{id:opt(float),label:opt(string)})")
	}
	produced_n0 = true
	return t_n0, nil
}

// ComponentNamesNativeRaw — covered reads exposed on the combined native path (declaration order).
// Each is a 1:1 entry <Method>(<positional params>) -> (T, error): a native return; the consumer keeps the
// model native (no Value serialization on the read hot path — the boxing residue bc#77 removes) and
// supplies the op-agnostic leaf transport symbols the entry calls. See INTEGRATION.md §6.
var ComponentNamesNativeRaw = []string{"posts", "postsTop", "page", "postsByIds", "feed", "usersWithPosts", "postsWithAuthor", "usersWithCappedPosts", "usersWithUncappedPosts", "usersWithTopPosts", "createPost", "renamePost", "removePost", "createPostReturning", "renamePostReturning", "removePostReturning", "restatusPostsReturning", "removePostsByAuthorReturning", "typedRows", "createTags", "removeTags", "createTagsReturning", "relabelTagsReturning", "removeTagsReturning"}
